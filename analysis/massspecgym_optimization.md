# MassSpecGym 单 baseline 优化实验索引

目标、比较协议和验收条件见[阶段计划](../docs/paper-change-plans/2026-09-07-MassSpecGym全量重训.md)。
本索引记录优化决策，不替代外部运行目录中的配置、状态、原始日志及 SHA-256 工件。
每轮“改动＋结果”的一句话记录见[attempts.md](../attempts.md)，与本索引同步维护。

## A01：按完整验证检索选择 alignment checkpoint

- 状态：2026-09-09 15:58:57 在第 19 轮按 patience5 早停，四阶段全部完成，入口与训练进程
  已退出；16:00 独立完成审计通过。选中 epoch14，但未通过相对 r4 的整体改善标准，保留 r4。
- 固定起点：v1.5 r4 alignment seed42，原按验证对比损失选中 epoch15；来源见
  [准确率排查](massspecgym_v15_regression_audit.md)。不重新执行旧 12 组矩阵。
- 假设：验证对比损失最优的 epoch 未必具有最好的候选检索排名；直接观察完整验证检索
  可以改善选优，并区分 checkpoint 选择问题与表示/训练目标问题。
- 主要改变：每个完整 epoch 后，重新编码全部 Mass 验证候选与 query，按 Top-1、MRR
  顺序选 checkpoint，同时保存 Top-1/5/10/20/MRR 的非支配候选文件和完整轨迹。
- 固定条件：seed42、随机初始化、原有两塔结构/损失/增强/AdamW/学习率/温度/batch128；
  每轮 194,119 个 train query，19,423 个有效 val query，最多 100 epochs、patience5。
  验证索引和 DataLoader 使用独立 RNG，不应额外消耗训练随机数流。
- 评价基线：使用同一验证索引，对 r4 原 checkpoint 做一次 fresh encoding；避免把旧 fp16
  exact-SMILES cache 指标直接与新 float32、二维多正例指标比较。索引保留源顺序与重复条目，
  只沿用已审计的无效图排除，所有无正例 query 保留分母。
- 同分规则：主指标对应 `torchmetrics==1.8.2` 的逐 query CPU `argsort(descending=True)`，
  padding 先移除；稳定排序另存敏感性视图。MRR 按同一排名扩展，不声称它是官方已报告指标。
- 资源：单卡显式 CUDA，物理 GPU 0/1 择一等待；分子编码 batch512、谱图 batch128、CPU workers4。
  新验证索引实测 827,600 个唯一分子、19,423 个 query，全部 query 自然包含正例；
  记录 6 个源候选图排除条目，没有多正例 query。逐轮记录验证耗时，
  先实测再决定是否需要图缓存；不为省时减少 query 或候选。
- 队列阶段：导入已审计数据 → CPU 验证索引 → 固定 checkpoint 验证 → 单 seed alignment。
  该队列不包含 test 推理或 reranker，完成只表示 A01 候选完成。
- 保留规则：依据完整验证 Top-k/MRR 的改善和代价决定是否成为新 incumbent；若只有选优
  改善而表示能力仍不足，下一项按计划考虑参数、预训练来源及候选对比目标，不转向 test 调参。
  A01 完成后还要比较其自身轨迹的验证 loss 最低轮与检索最优轮：r4 与 A01 的早期逐轮 loss
  已观察到差异，相同 seed/超参数不代表逐位相同的训练轨迹，不能把跨运行差异全部归因于选优规则。
- 验证：53 项专项通过；全仓 239 通过、1 跳过、1 个既有内部路径审计失败；Ruff、compileall、
  diff 检查通过。新增指标测试使用真实 `torchmetrics 1.8.2`，覆盖同分、多正例和空正例。

运行位置、固定源码与启动记录集中在阶段计划第 10 节；原始索引回执为运行根中的
`validation/mass_val_topk256.json`。固定 r4 checkpoint 的统一验证基线如下；来源为运行根
`baseline_validation/metrics.json` 及 `baseline_epoch000.pt`，完整编码耗时 176.17 秒。

| 评价对象 | Top-1 (%) | Top-5 (%) | Top-10 (%) | Top-20 (%) | MRR |
| --- | ---: | ---: | ---: | ---: | ---: |
| r4 固定 alignment，完整有效 val | 5.4008 | 20.5324 | 31.9415 | 44.2465 | 0.140237 |
| A01 同轨迹最低验证 loss，epoch10 | 4.7058 | 16.0274 | 25.1557 | 36.9716 | 0.115843 |
| A01 非支配候选，epoch8 | 4.8396 | 18.1331 | 29.2591 | 41.2655 | 0.125659 |
| A01 检索选优及非支配候选，epoch14 | 5.6737 | 18.0714 | 28.4560 | 40.5962 | 0.130524 |

以上均为同一完整有效验证索引上的 fresh 自有 loader 推理；没有运行 test，也不称为官方 loader
测试结果。完成审计核对全部 19 轮每轮训练/验证计数为 194,119 / 19,423，独立从每轮保存分数
重算全部排名、Top-k/MRR 与同分敏感性指标，检查输入指纹、早停、最终选优和权重关联。
最终非支配集合仅有 epoch8、epoch14；其余完整轨迹及排名保存在原运行根。

决策：epoch14 相对 r4 的 Top-1 增加 0.273 个百分点，但 Top-5/10/20 分别下降
2.461/3.486/3.650 个百分点，MRR 下降 0.009713；epoch8 各项也未超过 r4。
按预先声明的 Top-k 0.2 个百分点、MRR 0.002 容差，两者均不能替换 incumbent。
保留 r4，同时继续使用完整验证检索选优：在 A01 已观察到的同一轨迹中，epoch14 相对最低
验证 loss 的 epoch10，各项 Top-k/MRR 均改善。这不证明对旧早停轨迹重跑的反事实收益，
也不把 r4 与 A01 的跨运行差异全部归因于选优规则；下一项只改变训练 batch 组成。

完成后的统一核验入口为 `audit_alignment_optimization.py`，使用说明见 GPU/tmux 指南。
它检查完整状态、输入与配置、逐轮覆盖、保存分数/排名/指标、最终选择和 Pareto 权重，
按预设容差报告与 baseline 和同轨迹最低 loss 轮的差异；不会自行替换 incumbent。
14 项合成审计测试通过；全仓 273 通过、1 跳过、1 个相同的既有路径审计失败，
Ruff、compileall、diff 检查通过。实现提交 `0e2eaf3`，另对真实固定 baseline 的全部 19,423 条
保存分数核验通过；对仍在训练的 A01 实测拒绝生成完成回执。现已对完整 A01 正式核验通过，
回执与基线决策位置见阶段计划第 10 节；审计只验证保存分数，没有额外 GPU 推理。

## A02：质量邻近分组的全量 batch

- 状态：2026-09-09 18:09:37 完成，在第27轮按 patience5 正常早停，进程退出、pane exit0。
  独立 CPU 完成审计通过；选中 epoch22，是最终唯一非支配候选，全部指标通过整体改善规则。
  已将其设为下一轮比较 baseline，保留原 r4；这不是 test 或 SOTA 达标结论。
  验证索引 SHA-256 与 A01 完全相同，query/候选/图排除及自然正例覆盖一致。
  固定源码、运行根、专用 socket 和后续状态记录见阶段计划第 10 节。
- 假设：随机 in-batch 负例很少要求区分相近质量结构，训练判别任务与自然候选检索存在差距。
  完整 train 元数据的超几何期望显示，batch128 中约 3.10% 的 query 遇到 10 ppm 内的
  不同二维身份负例，batch512 约 10.42%；10 ppm 是诊断阈值，并非源候选成员资格的完整复现。
  精确数值、短尾批处理和可重跑脚本见外部 `alignment_inputs_20260909` 审计。
- 唯一改动：训练 batch 的组装顺序。按训练分子的计算精确质量排序，质量同分时随机打乱，
  循环平移后形成 32-query 小组，再随机组合 4 组为 batch128；尾部完整保留。
  每个 epoch 使用独立确定的 seed，全部 194,119 条 query 恰好出现一次。
  小组内提供邻近质量比较，小组间提供更广泛的负例；保存每轮质量/顺序 SHA-256 与唯一覆盖数，
  训练完成审计核对实际 batching 与预检配置。
- 固定：A01 的模型、初始化、loss、batch128、优化器、学习率、增强、验证检索与选优规则。
  不使用验证/测试标签组 batch，不改变评价候选，也不增加每步编码的分子数。
- 限制：相近质量不等于困难负例或源候选身份；该方法不保证性能提升，仍须完成实际训练验证。
  若效果不足，再考虑自然候选对比目标和经过来源审计的预训练；不把 sampler 检查写成模型结果。

完整训练首轮 CPU 顺序重建（seed42、batch128，源数据/实现/顺序哈希和脚本见计划中的
`mass_batching_20260909` 回执）：

| 输入组成指标 | 随机 batch | 32-query 质量小组 |
| --- | ---: | ---: |
| query 数 / 唯一 query 数 | 194119 / 194119 | 194119 / 194119 |
| 遇到 10 ppm 内负例的 query 比例 | 3.1218% | 69.3873% |
| 每 query 的 10 ppm 内负例谱图数 | 0.0336 | 8.2344 |
| 遇到同分子式负例的 query 比例 | 2.8302% | 60.0013% |
| 每 batch 不同二维身份数的中位数 | 126 | 23 |
| 完全无负例的 batch 数 | 0 | 0 |

同二维身份均按正例排除于负例统计。分组增加邻近质量比较，也减少 batch 内身份多样性，
不能仅凭比例增加推断 Top-k 会提高。专项 60 项通过；全仓此前 257 通过、1 跳过、
1 个相同的既有路径审计失败，随后新增完成审计的两项测试亦通过；静态检查通过。

完整27轮训练和验证的实际计数均为194,119 / 19,423；独立审计从全部保存分数重新计算排名、
Top-k/MRR 与同分敏感性视图，并核对质量分组覆盖、输入/配置、早停、最终选择及权重。
按 Top-1/MRR 选中 epoch22，其最终验证结果如下：

| 验证快照 | Top-1 % | Top-5 % | Top-10 % | Top-20 % | MRR |
| --- | --- | --- | --- | --- | --- |
| A02 前置 r4 baseline | 5.4008 | 20.5324 | 31.9467 | 44.2517 | 0.140229 |
| A02 epoch22，最终选中 | 7.5735 | 27.9257 | 40.9103 | 54.7598 | 0.181600 |
| 相对 A02 前置 r4 baseline 的差值，Top-k 为百分点 | +2.1727 | +7.3933 | +8.9636 | +10.5082 | +0.041371 |
| A02 同轨迹最低验证 loss，epoch26 | 7.4448 | 25.3668 | 37.8778 | 52.8137 | 0.172892 |

全部 Top-k 增幅超过0.2个百分点，MRR增幅超过0.002，未出现实质退步，因此按既定规则晋升。
唯一最终 Pareto 候选也是 epoch22；它的各项指标也高于同轨迹最低 loss 的 epoch26。
这是单 seed、同协议完整有效验证上的已观察改善，不是统计显著性或测试泛化证明。
完整轨迹和权重保留，完成回执及独立决策见阶段计划第10节。固定源码未保存资源峰值，
审计明确标为 `not_recorded`；不得用验证耗时或外部 GPU 快照补造峰值数据。

## A03：关闭训练分子图扰动，保留谱图增强

- 状态：A02 完整审计及晋升决策后，于2026-09-09 18:32:27单次派发；18:36:36完成CPU
  准备，18:41:36完成baseline完整重编码验证，随后训练阶段重新等待；18:43:40在物理GPU0
  的严格cuda:0上启动alignment。20:10核验已完成19轮完整检索验证，第20轮验证编码仍推进。
  验证索引SHA与A01/A02一致，逐轮训练/验证覆盖及资源文件持续生成；尚无最终收益结论。
  正式源码为干净的固定 `c50ddf0`，原运行工件保留。
- 假设：对分子图删除原子和独立丢弃有向边的增强，可能不利于相近结构的精细区分。
  完整 train CPU 诊断见 D04；图扰动不是已证实的低准确率原因，收益须由完整验证确认。
- 唯一主要改动：使用 `--no-alignment-mol-augmentation`，将 node/edge 扰动率同时置零。
  保留谱图增强及其概率、两塔结构、损失、初始化、优化器、学习率、batch128 与完整验证选优。
  实际增强配置保存于既有 selection 全配置快照，完成审计新增与预检配置的一致性检查。
- Parent 与 batching：最终固定 A02 epoch22 为比较 checkpoint，沿用 mass_blocks、block32、
  batch128。旧 r4/random 预检仅保留历史追溯，正式重做的预检已绑定 A02 完成审计及权重。
  相对 A02 的训练配置仅两项图扰动率归零；新增资源记录和未启用的谱图控制配置单独注明。
- 训练仍随机初始化，比较 checkpoint 不作为初始化；seed42、train194119、有效 val19423、
  无 test 调参、最多100 epochs/patience5，CUDA 0/1 逐阶段择一，输入协议不变。
  不新增参数或每 batch 图数量；完整图的实际训练开销待首轮测量，不预设速度或指标改善。
- 验证：65 项专项通过；全仓 292 通过、1 跳过、1 个相同既有路径审计失败，静态检查通过。
  真数据 dry-run 对比同一命令的默认配置，只有两项图扰动率变化；完整输入、源码与临时
  排队命令保存于阶段计划第10节指向的预检文件；正式配置随后在固定源码上再次通过检查。
- 资源记录实现 `c50ddf0` 已就绪，正式 alignment 逐轮保存分段墙钟耗时、实际 query 吞吐、
  参数量、显式 CUDA 上本进程的 PyTorch 显存峰值和主进程生命周期峰值 RSS，完成审计核对
  独立 JSON 与 selection 一致性。CPU 记录不覆盖 workers，GPU 记录不覆盖其它进程，
  不作为整机资源或部署延迟结论；细节见[运行指南](../docs/gpu_tmux_monitor_zh.md)。
  53 项专项通过；全仓 302 通过、1 跳过、1 个相同既有路径审计失败，静态检查通过。
  此前 A01/A02 固定源码未记录的峰值保持 `not_recorded`。
- A03 源码已冻结为 `c50ddf04d2be4fedc007365e511a95f36e298c08`，独立 worktree 干净，
  入口 `--help` 与 Python/PyTorch/PyG/RDKit/matchms/资源记录模块导入通过。
  正式配置、输入指纹、parent决策与启动命令均已保存；worktree和运行位置见阶段计划第10节。
  单次派发不证明训练完成，后续以阶段日志、进程和逐轮资源记录核验，不预设关闭图增强的收益。

首轮完整实测（`resources/stage2_epoch001.json`，2026-09-09 18:48:30）：

| 项目 | 测量值 |
| --- | ---: |
| 参数量 / 可训练参数量 | 9,046,985 / 9,046,985 |
| 训练query数 / 有效验证query数 | 194,119 / 19,423 |
| 训练阶段 / 对比验证 / 完整候选检索验证 | 77.67 / 4.49 / 162.48 秒 |
| 整轮（含checkpoint与选优） | 244.72 秒 |
| 训练阶段实际query吞吐 | 2,499.15 / 秒 |
| 整轮本进程PyTorch CUDA分配 / 保留峰值 | 1.594 / 1.881 GiB |
| 主进程生命周期峰值RSS | 4.365 GiB |

CPU峰值包括此前准备，不覆盖worker；CUDA峰值覆盖本轮训练、验证与选优，不是整机峰值，
也没有分阶段显存测量。此轮约三分之二耗时来自完整检索验证；不据此减少query或候选。
按首轮耗时，额外5轮约20.4分钟；实际剩余epoch取决于后续改善及patience，不能当成完成时间承诺。
截至20:10，前19轮完整验证已记录；完成审计仍待训练结束。
当前选中epoch16的验证Top-1/5/10/20为6.8836/25.7684/40.3285/54.8782%，MRR为0.171265；
Top-1/MRR仍低于A02比较基线，Top-20微小差异未超过晋升容差，继续完整轨迹后判断收益。

## D01：固定 baseline 的完整验证错误诊断

2026-09-09 对 A01 前置 r4 baseline 的已完成 float32 分数进行 CPU 分析，覆盖全部 19,423 个
有效验证 query。独立按源候选顺序和既定 CPU argsort 重算排名，逐条与保存排名一致，
Top-k/MRR 与原回执一致；没有重新推理、训练、过滤候选或修改元数据。
输入/脚本/逐 query 输出哈希与依赖版本见计划第 10 节的 `validation_errors_20260909` 回执。

| 原始 adduct 分组 | query 数 | Top-1 (%) | Top-10 (%) | MRR |
| --- | ---: | ---: | ---: | ---: |
| [M+H]+ | 14686 | 5.8151 | 30.5801 | 0.137715 |
| [M+Na]+ | 4737 | 4.1165 | 36.1621 | 0.148056 |

- 钠加合物的 Top-1 较低，但 Top-5/10/20 与 MRR 更高；这些是受结构、仪器和谱图分布等因素
  共同影响的描述性分组，不能推出加入 adduct 特征的因果收益，也不能把它作为低准确率的唯一原因。
- 错误 Top-1 中 39.15% 与真值同分子式。这支持继续检查结构判别能力，但不证明具体损失或结构
  改动必然有效；真值分子式仅用于事后错误分类，没有送入 Mass 模型或重排器。
- 仅从实测 `precursor_mz`、记录的 adduct 和固定版本 matchms 加合物表推导中性质量，忽略
  来源可能不清的 `parent_mass`；没有使用目标结构来修复输入。真值质量误差中位数为 0.148 ppm，
  804 个 query（4.14%）超过 10 ppm，最大约 1824 ppm。直接追加 10 ppm 硬过滤会损失这些真值，
  因而不实施该过滤。未来若试质量一致性特征，应保留完整候选并使用可处理异常值的软打分。
- 仪器/碰撞能量缺失的 query 同样保留于总体与分组分母；详细分组在原始回执，不据这些分组
  改变训练/验证排除规则。当前诊断不替代官方候选生成协议，也不外推为 test 表现。

诊断时的决策是维持 A01 原定规则、A02 只改变 batch 组成；A01 完成后的实际决策见上。
元数据分支和软质量特征保留为后续假设，暂不同时加入本轮，也不据静态诊断宣布性能改善。

## D02：固定基线重复编码的数值差异

A02 前置验证继续使用 r4 checkpoint，与 A01 的索引、model/validation 配置一致。
独立 CPU 核验两次全部保存分数的排名与指标均通过：Top-1/5 相同，A02 的 Top-10/20 各多
命中 1 个 query（0.00515 个百分点），MRR 少 0.000008311。有效候选分数最大绝对差为
6.855e-7；122 个 query 排名不同，最大变动 28 位。差异远小于预设指标容差，不作为模型改善，
也不归因于某个尚未单独核验的 CUDA 操作。完整差异和输入哈希见计划中的重复基线审计回执。

## D03 准备：完整验证的谱图输入敏感性检查

实现、使用方式与解释边界见 GPU/tmux 指南。新增显式 `permuted` / `constant` 输入模式，
正常验证默认路径不变；控制单独保存，完整训练审计拒绝把它用作普通选优证据。
14 项新增测试覆盖真实小模型 forward、固定候选/无正例分母、排列复现与 RNG、checkpoint
指纹、输入拒绝、独立分数审计和已有工件保护。全仓 287 通过、1 跳过、1 个相同既有路径
审计失败；Ruff、compileall、diff 检查通过。

对真实 19,423 条验证索引的 CPU 构造预检通过：候选/标签/源位置哈希与训练 RNG 不变；
置换没有自身 donor，37 个 donor 偶然具有相同二维身份，已保留统计。常量模式全部 query
的输入张量相同。这只是输入构造核验，尚无正式控制推理、控制性能或 SOTA 审计结论；
不插入正在运行的 A02 队列。回执和可重跑脚本位置见阶段计划第 10 节。

## D04：图扰动与多正例损失下界的完整训练输入诊断

对全部 194,119 条训练 query、25,046 个唯一训练 SMILES 进行 CPU 诊断，未使用验证/测试
来选择增强参数。随机和质量分组的首轮顺序重建 SHA 与原 CPU batch 审计相同；随机顺序
仅是诊断重建，不宣称是未保存排列的 A01 实际训练顺序。

当前损失对每个 anchor 的所有正例平均负 log probability；若一个 batch 中该身份有 m 个
正例，其不受模型约束的 logit 最优下界为 log(m)。按训练日志的 batch 平均方式，重建的
random 首轮下界为 0.02430，mass_blocks 为 2.26986。因此不能因 A02 的 train loss 更高，
直接认定优化失败或改坏了数据；也不把减去该下界后的数值当成新选优指标，仍看完整检索。

现有图增强概率为0.5，node/edge 丢弃率各0.1。按各训练结构的原子/键数量计算，预期
49.6791% 的训练 query 会改变图，平均每 query 移除1.2883个原子（包含未增强的 query）。
每个唯一 SMILES 做一次固定 seed 抽样、按完整 query 频次加权的诊断中，49.7061% 改变图，
47.7650% 出现仅保留一个方向的边。该抽样不是训练 worker 的逐次重放。
代码在删节点/边后保留原始度数、环与图尺寸特征；这描述了训练扰动视图，尚不能证明
它损伤检索。A03 将用单项关闭图扰动检验该假设，不同时改变 loss、谱图增强或网络结构。

## D05：轻量指纹表示在完整验证候选中的歧义

使用已固定的 A01 验证索引，在 CPU 上为全部 827,600 个候选 SMILES 生成 Morgan radius-2
bit 指纹（RDKit 2026.03.1，禁用手性，2048/4096 位）。保留全部 19,423 条有效验证 query、
既定二维身份和候选顺序，不使用测试集，不运行模型或改变 A02/A03 配置。

| 表示 | 存在不同二维结构但相同指纹的 query | 比例 | 最多同指纹负例数 | 全池 packed 字节对应 MiB |
| --- | --- | --- | --- | --- |
| Morgan radius-2 / 2048 | 451 | 2.3220% | 160 | 202.05 |
| Morgan radius-2 / 4096 | 451 | 2.3220% | 160 | 404.10 |

两种位宽的 358 对歧义分子对完全一致。独立程序重算全部 query 计数，并对涉及的387个分子
重新生成 InChIKey 和原始指纹，确认每对是不同二维身份、相同位向量，不仅比较 SHA。
这358对中，取消有限位宽折叠后的 radius-2 特征集合仍全部相同，稀疏计数仅区分1对，
原子/键数量也全部相同。该局部分析不等于对另一个表示完成全候选评价。

结论限定为表示可区分性：完全相同的单一指纹经过共享确定性分子塔，仍会得到相同表示；
这不约束 GINE 融合或只将指纹用于辅助监督的模型。扩大到4096位未消除观察到的完全碰撞，
**不证明两种位宽的学习效果等价**。后续若选择指纹塔，2048位可作为低成本起点，仍须正式训练验证。
未生成检索成绩；回执中的随机同分 oracle 假设正确指纹组排在其它候选之前，不能当成准确率、
官方排序结果或可达到的性能。正式模型仍须接受既定完整验证和最终审计。

该 CPU 生成与扫描共132.35秒、2个worker；不是训练或部署耗时。全池 float32 展开分别需要
6.31/12.63 GiB，后续实现应评估按batch解包与上卡。此次保存的是指纹载荷 SHA、逐query计数、
全部歧义对、原始位向量重核验回执及脚本，**不是可直接复用的训练指纹特征缓存**。
工件位置与校验值见阶段计划第10节。

## D06：自然候选监督的全量训练输入准备

对固定 v1.5 数据执行 CPU 准备，覆盖全部194,119条训练query、25,046个目标SMILES及
22,742个训练二维身份。按目标共享候选列表，保留每条原始query的映射，不采样或丢弃query。
全部5,711,753个候选SMILES重新计算二维身份，共5,407,045个候选二维身份；
25,046个有效目标列表共6,350,650条候选，按训练query重复加权后为49,126,117条。

全部query恰好有一个自然二维正例，均有不同二维身份的负例，没有forcing或正例补入。
不同二维负例数最少11、中位数252、最多255；163,090条query（84.0155%）的候选池包含
重复二维身份，重复条目数中位数3、均值8.587、最多120。负例预算不足时应保留实际可用
候选和query，不能为了凑固定负例数引入正例、无意重复同身份或丢弃困难query。
是否按二维身份抽负例需在后续单项实验卡中明确，评价候选顺序和分母保持既定协议。

保留官方原始候选顺序，只应用已有图审计的52条排除（按目标列表计数）；来源与位置逐条匹配。
独立程序读回序列化工件，对全部query、全部目标列表和6,350,650条候选逐条比较原始源文件，
验证映射、顺序、图排除和正例mask；正例mask同时符合二维身份以及该固定源的唯一exact target。
合成检查覆盖重复query、二维同身份、原始顺序和禁止排除目标；没有模型训练或推理。

官方训练候选中有467个结构也是有效验证目标，446个也是测试目标。它们来自训练query的自然
候选，不包含对应验证/测试谱图或配对监督；此处只作交集审计，没有据held-out身份过滤候选。
如果后续用这些候选训练分子塔，需如实披露无标签结构接触，不能声称分子塔从未接触这些结构。
训练正例身份与验证/测试正例身份仍无交集，序列化训练元数据不含held-out监督字段。

CPU准备共698.16秒、4个worker；紧凑元数据429,398,384字节（409.51 MiB）。工件只含
SMILES、二维身份、候选/来源位置/正例mask和query映射，不是TokenSet、图张量或模型embedding
缓存，也尚未被训练入口使用。仅作容量计算：全池512维float32需10.89 GiB，2048维需43.58 GiB；
后续训练须按batch组织，不能将这些字节估算称为实际GPU峰值或训练速度。
加载与采样接口已实现为 `SpecEmbedding/utils/training_candidates.py`，提交 `01024f5`。
它先核验准备/独立复核回执、全部源文件和元数据，再逐条比较全量query与候选；读取结束再次
检查输入未变。采样先在不同负例二维身份中无放回抽取，再在该身份的源条目中均匀抽取；
排除所有正例别名，小候选池保留query和实际可用负例。使用query/seed/epoch决定的私有RNG，
明确设置负例预算与有限候选池缓存容量；不改变评价候选的重复条目或源顺序。
23项新增合成测试通过；全仓325通过、1跳过、1个相同既有路径审计失败，Ruff/compileall/diff
检查通过。后续数据/损失组件见D07；正式训练入口尚未启用，不是新模型实验。
真实全量CPU采样预检于18:35完成：seed42/epoch1、诊断负例预算16、候选池缓存容量128。
全部194,119条query逐条核验，共3,105,854个抽样负例；10条query仅有11个可用负例，
其余各16个，全部query保留。未发现同身份假负例或单query内重复负例，所有抽样SMILES
索引及位置均与原候选匹配；全局Python/NumPy/Torch RNG、源元数据不变，缓存淘汰后
重复抽样一致。三个保存数组的SHA与大小再次读取核验通过。
完整源文件核验加载21.86秒，抽样及独立逐条检查24.87秒；这不是训练吞吐或GPU开销测量，
预算16尚未被选作正式训练超参数。回执、完整抽样数组及可重跑脚本位置见计划第10节。
正式运行仍保持全量、显式CUDA与单项优化规则。
工件位置、源指纹与独立复核回执见阶段计划第10节。

## D07 准备：自然候选监督的数据与训练组件

实现提交`bee5b63`，使用方式与边界见GPU/tmux指南。新增数据包装器将完整谱图Dataset的
二维身份分组顺序映射回原始query；检查完整唯一覆盖、源SMILES以及tokenization/候选manifest
一致。正、负分子图均显式绑定已审计构图策略，跨spawn序列化后也不依赖worker全局默认值。
负图采用正图相同的增强设置，有限缓存只保存未增强的CPU图，读取时克隆，不缓存学习中embedding。

训练组件在原双向in-batch loss之上添加显式权重的query内CE：每条query使用原正例图表示，
与该query自然池中抽取的不同二维负例比较；全部同身份别名在采样前排除。无负例query的
候选CE为零，仍保留在所有query的均值分母和原in-batch监督中。该项是抽取负例池上的目标，
不宣称是完整候选softmax的无偏估计；正式评价仍使用完整既定列表。
两塔及可学习温度均获得候选项梯度，不新增模型参数，不修改质谱预训练架构或检索余弦打分。

每轮重设采样epoch，拒绝persistent workers和丢尾批，训练后核验每个原始query恰好一次，
保存负例总数、最少/最多/零负例query数、query均值候选loss及抽样顺序SHA。
验证保持原有完整检索路径。当前负图batch的反向图全部保留，正式资源开销尚未测量；
不预设16个负例或任何权重为最终超参数。

23项新测试覆盖独立数值参考与梯度差分、空负例分母、不同二维别名、分组/原始索引错配、
真实小GINE/谱图塔反传、spawn传输、图缓存不被增强污染、完整合成训练/选优/重载，以及
总数正确但重复query的拒绝。全仓348通过、1跳过、1个相同既有路径审计失败；静态检查通过。
最初spawn测试因沙箱禁止共享张量所需的本机socket操作而阻塞，限定测试超时后取得明确
PermissionError；同一测试在主机CPU环境通过，未为绕过问题删除多进程验证或改变A03。

真实全量query/token绑定预检于19:09完成，覆盖194,119条原始训练谱图：分组后的每个位置
与原始索引都不同，显式映射逐条验证了目标二维身份、SMILES、m/z、强度、mask和候选目标行。
全部原始query恰好一次，保存映射读回一致；约72.57秒，只有CPU处理，没有模型forward。
预检回执、源码/数据指纹与完整映射位置见阶段计划第10节。
2026-09-09正式配置、CLI、输入指纹和完成审计已集成为`67787bc`：默认不启用，仅在优化分支
显式指定完整训练候选及已准备数据时激活；训练Dataset实际raw-query映射必须与预检一致，
每轮保存真实query顺序，完成审计逐query重放负例采样、源位置、批次边界与覆盖。
新增18项参数/衔接/完整训练重放检查通过；全仓366通过、1跳过、1个相同既有路径审计失败，
Ruff/compileall/diff通过。新固定源码的真实全量dry-run通过，194,119条query的预检映射
与D07独立实际Dataset映射SHA一致。下一轮[实验卡](../attempt_4.md)已准备，等待A03完成后的
最终基线/配置决策；尚未派发候选监督训练，不把组件、CPU测试或预检写成模型收益。

## D08 准备：单次尝试耗时与验证 batch 提速核验

2026-09-09 用户询问 batch/学习率提速，要求避免以较大质量风险换速度。当前 A03 训练
batch128、学习率1e-4；完整验证分子 batch512、谱图 batch128、workers4。不修改正在运行
的固定源码或配置。A02 的 `status.json` 记录入口16:01:24开始、18:09:37完成，合计128分13秒，
其中 alignment 阶段116分53秒、27个epoch；该总时长含运行内CPU准备、基线验证和GPU门槛等待，
不含运行结束后的独立完成审计，GPU繁忙时额外排队没有固定上限。

A03截至19:31:40的前11份 `resources/stage2_epochNNN.json` 平均：训练81.58秒、对比验证4.26秒、
完整检索验证171.78秒、选优/保存0.06秒，整轮257.69秒（4.29分钟）。这些是当前9,046,985参数
双塔的实测，不用于预估尚未运行的自然候选监督开销。按相同速度，20/30个epoch分别约86/129分钟，
还需准备、基线验证、排队与独立审计。已记录的整轮本进程CUDA分配峰值最大1.649 GiB，
保留峰值1.904 GiB；显存余量不证明吞吐会随batch线性增加。

若只有训练部分达到2倍吞吐，整轮理论上缩短15.83%；若只有完整检索验证达到2倍，整轮缩短
33.33%。两者均为保持其余耗时不变的条件计算，**不是已实现或承诺的提速**。完整验证包含构图、
编码、传输和排序，尚未分别测量CPU/GPU瓶颈；不能据总耗时断言增大batch必然有效。
训练batch会改变in-batch负例池、质量小组组成及每轮更新步数；提高学习率不直接减少单步计算量，
也未证明会减少本项目达到同等检索质量所需的epoch，因此暂不为提速改这两项。

下一项工程测量仅比较验证分子batch512与1024，脚本、独立配置及输入指纹已准备，
仍待A03结束，尚未派发：

- 固定已审计的A02 epoch22权重、同一完整验证索引、规范图策略、谱图batch128、workers4和
  现有精度；全部19,423条query、827,600个分子重新编码，不减少候选或验证频率。
- 用干净固定源码、独立诊断目录，单卡显式CUDA、每阶段GPU门槛复查；顺序512→1024→512，
  两次512控制用于观察时间漂移；这是一个batch改动的工程测量，不是新模型训练或seed矩阵。
- 保存每次同步墙钟时长、显存峰值、所有分数和排名，独立重算全量指标并检查输入/权重哈希。
  预先固定数值检查：有效分数最大绝对差不超过1e-5，各Top-k命中数差不超过2条query，
  MRR绝对差不超过1e-4，同时报告全部排名变化；这不是逐位一致或统计等价性结论。
- 初步采用条件：两次512耗时相对均值的极差不超过10%，1024比两次512均值至少节省10%，
  且上述数值/完整性检查全部通过；否则保持512，先根据瓶颈证据决定是否有必要继续测量。
  验证通过也只用于后续新run，既有实验及其逐轮选优轨迹保持原样。

批大小变化可能带来浮点舍入差异，原文依据为[PyTorch数值精度说明](https://docs.pytorch.org/docs/main/notes/numerical_accuracy.html)
（2026-09-09核验）；本项目同权重重复编码的既有微小排名波动另见D02。当前没有新batch实测收益。

用户随后询问CPU并行化/迁移GPU。19:39的只读核验确认当前CPU亲和性包含128个逻辑CPU，
验证确有4个存活的DataLoader worker；进程`ps %CPU`是生命周期平均值，不作为区间利用率或
CPU瓶颈证明。代码`StrictValidationMolecules.__getitem__`在每次验证重做SMILES解析和构图，
没有复用验证图缓存；`AlignmentRetrievalValidator`将本轮分子embedding逐batch拷回CPU，
随后按query取候选embedding再传GPU评分。前13轮日志中，完整分子阶段平均162.05秒，
其后谱图编码、候选评分和排名约10.03秒；前者同时包含CPU构图/组batch/等待、GPU编码与传输，
不能全部归为CPU耗时，须进一步测量各段。

后续可独立核验的工程方向如下，不同时叠加、尚未修改正式实现：

- 将验证workers4增加到8，检查真实加载等待、吞吐和内存；继续保持输出顺序和全部候选。
  RDKit的Python构图采用多进程，不能只换成普通Python线程就预设并行收益，依据为
  [RDKit线程说明](https://www.rdkit.org/docs/RDKit_Book.html#thread-safety-and-the-rdkit)。
- 完整预计算并缓存规范的节点/边图张量，按候选源顺序、SMILES、构图源码、RDKit版本和图策略
  固定指纹；每轮读取相同图，避免反复解析。优先使用紧凑数组/共享文件，避免worker各自复制
  整个Python图对象列表。必须逐图验证与原构图完全一致，保留无效图审计和所有query。
  这是确定性输入缓存；模型权重变化后embedding仍须重新编码，不能跨epoch复用旧embedding。
- 本轮827,600×512维float32分子embedding载荷为1.5785 GiB（不含模型/临时图和分数），
  可评估留在选定GPU上直接索引候选和评分，避免每个query重复传输候选embedding；按当前
  19,423×256的矩阵宽度，该重复H2D载荷约9.4839 GiB/轮，属于字节计算而非已测传输耗时。
  最终主排名仍使用原CPU逐query排序与同分规则，不因工程提速改变评价协议。
  DataLoader固定内存与异步传输可另行测量，正确同步和实际收益依据
  [PyTorch传输指南](https://docs.pytorch.org/tutorials/intermediate/pinmem_nonblock.html)，不直接照搬文档倍速。

这些是可行性与瓶颈定位结果，不是新模型成绩或已实现加速；A03与既有输入/源码不变。

## R01：候选对比目标与轻量分子表示的研究依据

当前 `ContrastiveAlignmentLoss` 仍是双向 in-batch 多正例损失；A02 增加邻近质量负例的出现
机会，没有直接引入每条训练 query 的官方候选。后续仍须区分这两种训练目标。

- [JESTR v1，§2.3](https://arxiv.org/html/2411.14464v1) 在训练后期加入候选与谱图的余弦排斥
  正则项，并按目标的指纹相似度选择候选。这提供候选内区分的设计线索；原文实验不是本地
  MassSpecGym v1.5 统一复现，不能将报告收益直接当作预期增益。
- [MSAlign v1，§2/§3](https://arxiv.org/html/2605.19752v1) 采用候选对比目标，冻结 DreaMS/
  ChemBERTa 后训练小型投影，可预计算固定特征。其低训练开销依赖冻结编码器，不能继承给
  本项目每轮更新的两塔。这里借鉴目标与计算组织，不默认引入其外部预训练权重或不同 split。

下一步候选研究仍遵循单项闭环：A03 完成审计后，按其结果选择自然候选监督或轻量指纹分子塔
中的一个主要改动，不同时替换两塔、损失和预训练。若采用训练候选，须重新核验全部train query
的候选二维身份，排除同身份假负例、记录外部候选结构来源，不引入验证/测试监督；每条query仍
参与每个正式epoch。候选负例预算属于待验证超参数，不能借此减少训练query或改变评价分母。
