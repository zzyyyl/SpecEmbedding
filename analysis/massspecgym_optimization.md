# MassSpecGym 单 baseline 优化实验索引

目标、比较协议和验收条件见[阶段计划](../docs/paper-change-plans/2026-09-07-MassSpecGym全量重训.md)。
本索引记录优化决策，不替代外部运行目录中的配置、状态、原始日志及 SHA-256 工件。

## A01：按完整验证检索选择 alignment checkpoint

- 状态：2026-09-09 15:58:57 在第 19 轮按 patience5 早停，四阶段全部完成，入口与训练进程
  已退出；16:00 独立完成审计通过。选中 epoch14，但未通过相对 r4 的整体改善标准，保留 r4。
- 固定起点：v1.5 r4 alignment seed42，原按验证对比损失选中 epoch15；来源见
  [准确率排查](massspecgym_v15_regression_audit.md)。不重新执行旧 12 组矩阵。
- 假设：验证对比损失最优的 epoch 未必具有最好的候选检索排名；直接观察完整验证检索
  可以改善选优，并区分 checkpoint 选择问题与表示/训练目标问题。
- 主要改变：每个完整 epoch 后，重新编码全部 Mass 验证候选与 query，按 Top-1、MRR
  顺序选 checkpoint，同时保存 Top-1/5/10/20/MRR 的非支配候选文件和完整轨迹。
- 固定条件：seed42、随机初始化、原有两塔结构/损失/增强/AdamW/学习率/温度/batch128；
  每轮 194,119 个 train query，19,423 个有效 val query，最多 100 epochs、patience5。
  验证索引和 DataLoader 使用独立 RNG，不应额外消耗训练随机数流。
- 评价基线：使用同一验证索引，对 r4 原 checkpoint 做一次 fresh encoding；避免把旧 fp16
  exact-SMILES cache 指标直接与新 float32、二维多正例指标比较。索引保留源顺序与重复条目，
  只沿用已审计的无效图排除，所有无正例 query 保留分母。
- 同分规则：主指标对应 `torchmetrics==1.8.2` 的逐 query CPU `argsort(descending=True)`，
  padding 先移除；稳定排序另存敏感性视图。MRR 按同一排名扩展，不声称它是官方已报告指标。
- 资源：单卡显式 CUDA，物理 GPU 0/1 择一等待；分子编码 batch512、谱图 batch128、CPU workers4。
  新验证索引实测 827,600 个唯一分子、19,423 个 query，全部 query 自然包含正例；
  记录 6 个源候选图排除条目，没有多正例 query。逐轮记录验证耗时，
  先实测再决定是否需要图缓存；不为省时减少 query 或候选。
- 队列阶段：导入已审计数据 → CPU 验证索引 → 固定 checkpoint 验证 → 单 seed alignment。
  该队列不包含 test 推理或 reranker，完成只表示 A01 候选完成。
- 保留规则：依据完整验证 Top-k/MRR 的改善和代价决定是否成为新 incumbent；若只有选优
  改善而表示能力仍不足，下一项按计划考虑参数、预训练来源及候选对比目标，不转向 test 调参。
  A01 完成后还要比较其自身轨迹的验证 loss 最低轮与检索最优轮：r4 与 A01 的早期逐轮 loss
  已观察到差异，相同 seed/超参数不代表逐位相同的训练轨迹，不能把跨运行差异全部归因于选优规则。
- 验证：53 项专项通过；全仓 239 通过、1 跳过、1 个既有内部路径审计失败；Ruff、compileall、
  diff 检查通过。新增指标测试使用真实 `torchmetrics 1.8.2`，覆盖同分、多正例和空正例。

运行位置、固定源码与启动记录集中在阶段计划第 10 节；原始索引回执为运行根中的
`validation/mass_val_topk256.json`。固定 r4 checkpoint 的统一验证基线如下；来源为运行根
`baseline_validation/metrics.json` 及 `baseline_epoch000.pt`，完整编码耗时 176.17 秒。

| 评价对象 | Top-1 (%) | Top-5 (%) | Top-10 (%) | Top-20 (%) | MRR |
| --- | ---: | ---: | ---: | ---: | ---: |
| r4 固定 alignment，完整有效 val | 5.4008 | 20.5324 | 31.9415 | 44.2465 | 0.140237 |
| A01 同轨迹最低验证 loss，epoch10 | 4.7058 | 16.0274 | 25.1557 | 36.9716 | 0.115843 |
| A01 非支配候选，epoch8 | 4.8396 | 18.1331 | 29.2591 | 41.2655 | 0.125659 |
| A01 检索选优及非支配候选，epoch14 | 5.6737 | 18.0714 | 28.4560 | 40.5962 | 0.130524 |

以上均为同一完整有效验证索引上的 fresh 自有 loader 推理；没有运行 test，也不称为官方 loader
测试结果。完成审计核对全部 19 轮每轮训练/验证计数为 194,119 / 19,423，独立从每轮保存分数
重算全部排名、Top-k/MRR 与同分敏感性指标，检查输入指纹、早停、最终选优和权重关联。
最终非支配集合仅有 epoch8、epoch14；其余完整轨迹及排名保存在原运行根。

决策：epoch14 相对 r4 的 Top-1 增加 0.273 个百分点，但 Top-5/10/20 分别下降
2.461/3.486/3.650 个百分点，MRR 下降 0.009713；epoch8 各项也未超过 r4。
按预先声明的 Top-k 0.2 个百分点、MRR 0.002 容差，两者均不能替换 incumbent。
保留 r4，同时继续使用完整验证检索选优：在 A01 已观察到的同一轨迹中，epoch14 相对最低
验证 loss 的 epoch10，各项 Top-k/MRR 均改善。这不证明对旧早停轨迹重跑的反事实收益，
也不把 r4 与 A01 的跨运行差异全部归因于选优规则；下一项只改变训练 batch 组成。

完成后的统一核验入口为 `audit_alignment_optimization.py`，使用说明见 GPU/tmux 指南。
它检查完整状态、输入与配置、逐轮覆盖、保存分数/排名/指标、最终选择和 Pareto 权重，
按预设容差报告与 baseline 和同轨迹最低 loss 轮的差异；不会自行替换 incumbent。
14 项合成审计测试通过；全仓 273 通过、1 跳过、1 个相同的既有路径审计失败，
Ruff、compileall、diff 检查通过。实现提交 `0e2eaf3`，另对真实固定 baseline 的全部 19,423 条
保存分数核验通过；对仍在训练的 A01 实测拒绝生成完成回执。现已对完整 A01 正式核验通过，
回执与基线决策位置见阶段计划第 10 节；审计只验证保存分数，没有额外 GPU 推理。

## A02：质量邻近分组的全量 batch

- 状态：A01 完成审计与保留 r4 决策后，于 2026-09-09 16:01:20 单次派发固定 `fc42b27`
  队列；16:12:44 启动 alignment，首轮已完成全量训练/验证，继续后续 epochs。
  验证索引 SHA-256 与 A01 完全相同，query/候选/图排除及自然正例覆盖一致；最终收益未定。
  固定源码、运行根、专用 socket 和后续状态记录见阶段计划第 10 节。
- 假设：随机 in-batch 负例很少要求区分相近质量结构，训练判别任务与自然候选检索存在差距。
  完整 train 元数据的超几何期望显示，batch128 中约 3.10% 的 query 遇到 10 ppm 内的
  不同二维身份负例，batch512 约 10.42%；10 ppm 是诊断阈值，并非源候选成员资格的完整复现。
  精确数值、短尾批处理和可重跑脚本见外部 `alignment_inputs_20260909` 审计。
- 唯一改动：训练 batch 的组装顺序。按训练分子的计算精确质量排序，质量同分时随机打乱，
  循环平移后形成 32-query 小组，再随机组合 4 组为 batch128；尾部完整保留。
  每个 epoch 使用独立确定的 seed，全部 194,119 条 query 恰好出现一次。
  小组内提供邻近质量比较，小组间提供更广泛的负例；保存每轮质量/顺序 SHA-256 与唯一覆盖数，
  训练完成审计核对实际 batching 与预检配置。
- 固定：A01 的模型、初始化、loss、batch128、优化器、学习率、增强、验证检索与选优规则。
  不使用验证/测试标签组 batch，不改变评价候选，也不增加每步编码的分子数。
- 限制：相近质量不等于困难负例或源候选身份；该方法不保证性能提升，仍须完成实际训练验证。
  若效果不足，再考虑自然候选对比目标和经过来源审计的预训练；不把 sampler 检查写成模型结果。

完整训练首轮 CPU 顺序重建（seed42、batch128，源数据/实现/顺序哈希和脚本见计划中的
`mass_batching_20260909` 回执）：

| 输入组成指标 | 随机 batch | 32-query 质量小组 |
| --- | ---: | ---: |
| query 数 / 唯一 query 数 | 194119 / 194119 | 194119 / 194119 |
| 遇到 10 ppm 内负例的 query 比例 | 3.1218% | 69.3873% |
| 每 query 的 10 ppm 内负例谱图数 | 0.0336 | 8.2344 |
| 遇到同分子式负例的 query 比例 | 2.8302% | 60.0013% |
| 每 batch 不同二维身份数的中位数 | 126 | 23 |
| 完全无负例的 batch 数 | 0 | 0 |

同二维身份均按正例排除于负例统计。分组增加邻近质量比较，也减少 batch 内身份多样性，
不能仅凭比例增加推断 Top-k 会提高。专项 60 项通过；全仓此前 257 通过、1 跳过、
1 个相同的既有路径审计失败，随后新增完成审计的两项测试亦通过；静态检查通过。

首轮完整验证于 16:17:45 完成：Top-1/5/10/20 为 2.2448/9.5866/18.1383/27.5756%，
MRR 为 0.073935；实际每轮训练/验证计数为 194,119 / 19,423。该首轮快照不替代收敛结果，
不与已收敛 r4 的差距混写为本方案最终失败；继续既定 patience 和选优规则。

## D01：固定 baseline 的完整验证错误诊断

2026-09-09 对 A01 前置 r4 baseline 的已完成 float32 分数进行 CPU 分析，覆盖全部 19,423 个
有效验证 query。独立按源候选顺序和既定 CPU argsort 重算排名，逐条与保存排名一致，
Top-k/MRR 与原回执一致；没有重新推理、训练、过滤候选或修改元数据。
输入/脚本/逐 query 输出哈希与依赖版本见计划第 10 节的 `validation_errors_20260909` 回执。

| 原始 adduct 分组 | query 数 | Top-1 (%) | Top-10 (%) | MRR |
| --- | ---: | ---: | ---: | ---: |
| [M+H]+ | 14686 | 5.8151 | 30.5801 | 0.137715 |
| [M+Na]+ | 4737 | 4.1165 | 36.1621 | 0.148056 |

- 钠加合物的 Top-1 较低，但 Top-5/10/20 与 MRR 更高；这些是受结构、仪器和谱图分布等因素
  共同影响的描述性分组，不能推出加入 adduct 特征的因果收益，也不能把它作为低准确率的唯一原因。
- 错误 Top-1 中 39.15% 与真值同分子式。这支持继续检查结构判别能力，但不证明具体损失或结构
  改动必然有效；真值分子式仅用于事后错误分类，没有送入 Mass 模型或重排器。
- 仅从实测 `precursor_mz`、记录的 adduct 和固定版本 matchms 加合物表推导中性质量，忽略
  来源可能不清的 `parent_mass`；没有使用目标结构来修复输入。真值质量误差中位数为 0.148 ppm，
  804 个 query（4.14%）超过 10 ppm，最大约 1824 ppm。直接追加 10 ppm 硬过滤会损失这些真值，
  因而不实施该过滤。未来若试质量一致性特征，应保留完整候选并使用可处理异常值的软打分。
- 仪器/碰撞能量缺失的 query 同样保留于总体与分组分母；详细分组在原始回执，不据这些分组
  改变训练/验证排除规则。当前诊断不替代官方候选生成协议，也不外推为 test 表现。

诊断时的决策是维持 A01 原定规则、A02 只改变 batch 组成；A01 完成后的实际决策见上。
元数据分支和软质量特征保留为后续假设，暂不同时加入本轮，也不据静态诊断宣布性能改善。

## D02：固定基线重复编码的数值差异

A02 前置验证继续使用 r4 checkpoint，与 A01 的索引、model/validation 配置一致。
独立 CPU 核验两次全部保存分数的排名与指标均通过：Top-1/5 相同，A02 的 Top-10/20 各多
命中 1 个 query（0.00515 个百分点），MRR 少 0.000008311。有效候选分数最大绝对差为
6.855e-7；122 个 query 排名不同，最大变动 28 位。差异远小于预设指标容差，不作为模型改善，
也不归因于某个尚未单独核验的 CUDA 操作。完整差异和输入哈希见计划中的重复基线审计回执。

## D03 准备：完整验证的谱图输入敏感性检查

实现、使用方式与解释边界见 GPU/tmux 指南。新增显式 `permuted` / `constant` 输入模式，
正常验证默认路径不变；控制单独保存，完整训练审计拒绝把它用作普通选优证据。
14 项新增测试覆盖真实小模型 forward、固定候选/无正例分母、排列复现与 RNG、checkpoint
指纹、输入拒绝、独立分数审计和已有工件保护。全仓 287 通过、1 跳过、1 个相同既有路径
审计失败；Ruff、compileall、diff 检查通过。

对真实 19,423 条验证索引的 CPU 构造预检通过：候选/标签/源位置哈希与训练 RNG 不变；
置换没有自身 donor，37 个 donor 偶然具有相同二维身份，已保留统计。常量模式全部 query
的输入张量相同。这只是输入构造核验，尚无正式控制推理、控制性能或 SOTA 审计结论；
不插入正在运行的 A02 队列。回执和可重跑脚本位置见阶段计划第 10 节。
