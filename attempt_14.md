# A14：GINE局部层之间的分子内全局上下文反馈

- 状态：**准备中，未启动**。在A10训练期间预登记；最终parent须等A13完整独立审计后
  决定，独立组件、正式路径接入及A04条件全量CPU预检已完成；没有运行目录、队列或检索成绩。
- 唯一主要假设：在局部消息传递之间汇总并反馈同一分子的全局上下文，可能帮助节点在
  后续层中结合远处结构信息，改善完整验证Top-1，并按既定Top-k/MRR整体标准验收。

## 依据与解释边界

当前GINE有4层局部消息传递，最后做全图均值汇聚。完整训练目标图统计及范围见
[D26](analysis/massspecgym_optimization.md#d26完整训练目标图的局部传播覆盖)：多数图的
直径超过4，支持研究更早的全局信息交换；但最终汇聚已经看到全部节点，4跳覆盖率不是
模型可见信息比例、准确率上界或已证实的性能瓶颈。

[Gilmer等，ICML 2017，§5.2](https://proceedings.mlr.press/v70/gilmer17a/gilmer17a.pdf)
研究与所有节点相连的master node以传递全图信息；其量子化学任务结果不代表质谱收益。
[GraphGPS v4](https://arxiv.org/abs/2205.12454v4)将局部消息传递与全局注意力作为不同
组件组合。本轮借鉴图内局部/全局信息交互，选择下述轻量汇总反馈，不称为这两套模型
或GLACIER的复现，也不由文献推断本地改善幅度。

## 固定改动

保留parent的4层GINE、各层原局部残差、dropout、最终均值汇聚与图大小特征。只在第
1、2、3层的原局部更新结束后，各加入一个独立的图内上下文分支，再送入下一局部层：

```text
g_l = mean(h_l[v] for v in the same molecule)
u_l = W2_l(ReLU(W1_l(LayerNorm(g_l))))
h_l[v] = h_l[v] + u_l
```

LayerNorm无可学习仿射参数，epsilon继承分子塔配置；W1/W2均含bias，隐藏宽度固定64。
每个W2的weight/bias初始化为零，保证分支初始恒等；W1用隔离的CPU RNG初始化，新增
分支不扰动原参数初值或原训练随机序列。新增参数总数在分子宽度128时预计为
`3 × (128×64 + 64 + 64×128 + 128) = 49,728`，须由实际构造核验。

只汇总同一分子当前前向中的节点，不能跨图混合、持久保存全局状态或接收谱图/候选排名
作为条件。分支无额外dropout、辅助损失、虚拟边、距离编码或节点两两注意力矩阵；所有
分子仍独立映射至共享嵌入空间，以原余弦相似度检索。SpecEmbedding谱图预训练结构不改。

## Parent、参数与数据

Parent为A13完整独立审计后唯一保留的incumbent；若A13未晋升，继承其已审计parent。
先确认其GINE仍为4层、宽度128，且没有同类上下文分支；来源不符或晋升存在歧义时先
排查，不能静默修改假设。继承已晋升的谱图汇聚、加合物条件层、图+指纹分支、候选
采样及CE权重，不预先假定后续尝试会晋升，也不一次叠加其它未晋升改动。

从随机初始化完整训练；parent checkpoint仅用于fresh完整验证比较。固定seed42、
batch128、质量block32、CE16、lr1e-4、100轮上限/patience5，保留原增强、优化器、
调度和梯度裁剪。训练分母194,119、有效验证分母19,423及六条排除不变，使用MassSpecGym
v1.5自然Mass候选与二维身份；不引入外部训练样本或真值分子式，不用test选参数。

## 缓存、存储与资源

输入图的原子/键特征、顺序、增强与构造代码不变；固定图、峰token、Morgan、候选采样
及已继承元数据缓存在来源有效时可复用。新分支不需要全对最短路径或全局节点缓存，D26
统计仅作诊断，不进入训练输入。模型配置、输入预检和checkpoint必须记录完整新构造，
原模型回执不能代替新模型预检；权重变化后学习embedding全部重新编码。

新数据、缓存、临时文件和工件仍在`/data1/${USER}/SpecEmbedding/`，源码冻结在外部
repos目录；不修改活跃worktree。若任何输入构造变化，停用对应旧缓存，确认无活跃引用
后清理失效载荷并保留审计记录。

固定宽度时汇总/广播随节点数线性增长；这只是结构复杂度判断。实现后核验额外参数、
实际激活/显存和完整训练/验证耗时；D26只覆盖目标图，不能用其最大尺寸代表所有负例。
继续显式cuda:N、GPU0/1择一等待及既定逐阶段门槛，模型变化后重新评估显存准入，不能
用缩减训练样本或静默改变batch来解决资源问题。

## 启动依赖与验收

先核验零分支下原参数/RNG和输出一致，再用独立公式检查非零反馈、图间隔离、原子排列
等变/汇聚不变、单节点和增强后断连图、分块编码、非零梯度与实际优化器更新。接通严格
配置构造、保存重载、完整训练计数及已有谱图/分子分支组合后，执行完整CPU输入预检。
零初始化W2使W1首步梯度为零属于设计预期，须在后续更新确认分支能学习。

在真实A13入口、trainer和独立完成审计出现后才绑定PID/starttime及固定来源；A13完成
审计并确定parent后生成最终配置和全量预检，再单次派发。本方案登记不改变A10训练或
现有后续队列，不预填未来依赖。失败保留日志和工件，先排查原因。

训练结束核验全部实际样本、保存的完整验证排名、选优权重和Pareto候选，按现有整体
Top-k/MRR晋升标准决定是否保留。资源检查、图诊断及组件测试都不是模型收益；未晋升的
已完成尝试在`attempts.md`加删除线，保留结果与原因。

当前结果：**无**。正式接入及条件输入预检已准备；GPU资源测量、最终绑定和训练均待。

## 独立组件准备（2026-09-11）

实现提交`077a7d6`新增`SpecEmbedding/models_graph_context.py`，提供图内均值上下文、零输出初始化分支及
独立双塔模型；`params.yaml`登记未激活预设。共用GINE只增加局部层之间的默认恒等扩展
点，原图+指纹路径可继承同一插入方式，保持原节点/键特征、局部残差和最终汇聚。
SpecEmbedding谱图预训练共享源码、图构造及缓存源码未改。该组件提交当时正式构造器
仍拒绝新字段；后续正式接入及全量条件预检见下。

新增43项及受影响路径合计225项CPU检查通过，覆盖16种图/谱图继承组合下原权重初值、
RNG及双塔输出完全一致；实际fresh训练入口的构造顺序也核验一致。独立逐边聚合与逐图
均值/归一化公式核对全部4层非零前向及梯度，包含单节点、无边断连图、长链和环。
另验证图间零梯度、同图非局部信息反馈、原子重标号、图顺序与分块编码、两步对比更新、
图+指纹分支同时训练及带完整配置的严格保存/重载；缺权重和未支持配置均拒绝。

宽度128、4层及隐藏宽度64的实际新增参数数目为49,728，三个分支参数独立；W1首步
零梯度、后续非零更新符合预登记初始化。参数计数与CPU检查不代表真实GPU显存、整轮
耗时或检索效果。全仓919通过、1跳过，唯一失败仍为原四份历史记录的路径审计；
Ruff、compileall及diff检查通过，全部测试临时工件位于外部存储根。

这一步没有激活正式配置或创建新训练/等待队列；最终仍须按A13完成证据绑定parent。
检查来源与回执SHA集中于阶段计划第10节。

## 正式接入与A04条件全量预检（2026-09-11）

实现提交`4a2c3bd`将显式`model.graph_global_context`接入正式构造、fresh训练入口和
完整完成审计，支持继承GINE及图+指纹分支、谱图汇聚、Q/K归一化和加合物条件。
启用时要求完整自然候选监督、绑定的完整验证及按自身配置加载的独立parent基线；
旧的直接图+指纹构造器拒绝静默忽略上下文配置。选优权重及所有Pareto权重均严格重载，
即使重新计算文件SHA，缺少上下文权重仍会失败。

新增正式配置检查41项通过；实际合成训练覆盖候选采样/权重与既有分支组合，验证完整
query计数、非零参数更新、保存分数重放和严格重载，另覆盖完整完成审计的拒绝路径。
首轮新测试误把均匀采样写成未支持字段而失败，已保留日志并改正测试fixture，生产
协议未放宽。最终全仓972通过、1跳过，唯一失败仍为相同四份历史记录的路径审计；
Ruff、compileall及diff检查通过。这些CPU合成检查不代表真实数据训练成绩。

固定源码位于外部`repos/SpecEmbedding-opt-a14-20260911`，detached实现提交且干净。
以已审计A04为条件分支，实际完整CPU预检及09:35独立读回通过：重新核验19项输入和
6份准备文件，覆盖194,119条训练query、19,423条有效验证query及827,600个验证分子。
模型仅增加登记的上下文配置，训练/增强/tokenizer及完整候选输入回执与A04一致；
实际构造9,096,713个参数，新增49,728，原权重初值/RNG和三个分支的零输出均核验通过。
原SpecEmbedding预训练共享源码及固定输入缓存构造未改，没有新增学习embedding缓存。

预检使用隐藏CUDA的`--dry-run`，没有执行模型训练或推理，也没有创建模型运行目录。
这只验证A04条件分支，未确定最终parent；A13真实依赖、最终绑定/衔接准备、正式GPU
成本与模型实验仍待。路径、输入和检查回执SHA统一见阶段计划第10节。
