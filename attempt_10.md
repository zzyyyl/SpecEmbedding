# A10：在固定负例预算内增加结构相似的自然负例

- 状态：**训练中**。A09完整审计未晋升后，原队列绑定A04并通过最终全量预检，06:13
  完成fresh基线验证，06:16启动GPU训练、06:17进入stage2；07:48已完成八轮，均记录
  完整训练/验证计数，尚无最终选优或晋升结论。
- 唯一主要假设：把每query最多16个均匀自然负例，改为结构相似池与其余池的混合采样，
  可能让同样的候选编码预算提供更有用的区分信号；是否改善完整验证Top-k/MRR仍待训练。

原入口仍为PID3756121/starttime1455331545，实际训练PID3805869/starttime1456674958，
物理GPU1按UUID映射为cuda:0，nvidia-smi已确认该PID实际占用目标GPU；显式CUDA及完整
外部存储环境与运行配置相符。27项输入及最终文件SHA、实际运行manifest与预检一致，
仅继承A04后变更已登记的负例混合采样；batch128、block32、CE16/权重1、lr1e-4及完整
训练/验证协议保持。独立完成审计等待进程已单次部署，06:43实际进程、环境和输入复核
通过；后续A11衔接状态见其[实验卡](attempt_11.md)，不将等待进程称为完成审计。

## 训练中途记录（2026-09-11）

以下为原始完整验证日志与资源JSON记录，尚未经过本轮独立完成审计；不提前决定晋升。

| Epoch | 验证完成时间 | Top-1 (%) | Top-5 (%) | Top-10 (%) | Top-20 (%) | MRR | 整轮分钟 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 06:29:18 | 4.2630 | 17.3506 | 29.9542 | 45.1012 | 0.121243 | 11.64 |
| 2 | 06:41:27 | 4.7058 | 21.5518 | 34.4128 | 48.8236 | 0.138126 | 12.14 |
| 3 | 06:53:26 | 7.8979 | 26.0258 | 42.0326 | 54.4097 | 0.179131 | 11.98 |
| 4 | 07:05:16 | 8.2377 | 26.0413 | 41.5950 | 54.5642 | 0.182431 | 11.84 |
| 5 | 07:16:59 | 10.5082 | 31.0405 | 45.7705 | 57.6224 | 0.212056 | 11.71 |
| 6 | 07:28:21 | 10.2250 | 29.8924 | 46.0536 | 57.5864 | 0.208217 | 11.37 |
| 7 | 07:38:28 | 9.0563 | 31.8025 | 46.4192 | 61.3345 | 0.203899 | 10.12 |
| 8 | 07:48:35 | 11.7232 | 34.5055 | 50.5689 | 62.2664 | 0.234142 | 10.10 |

前八轮分别记录全部194,119条train、19,423条val和827,600个验证分子重编码，9,046,985个
可训练参数及原GPU UUID一致；前八轮PyTorch最大allocated/reserved为3.65/7.42 GiB，
是该进程的allocator峰值，不是整卡用量或独占算力证明。原训练继续既定早停流程。

完成审计使用原固定`a8ad5f2`源码，绑定实际入口及trainer的PID/starttime；仅在两者结束且
全部阶段完成后执行一次CPU审计、工件重算与严格checkpoint构造。13项生命周期/parser
检查通过，真实未完成探测exit1且未创建完成目录；43项固定输入、原派发claim/runner_exec
及最终预检证据已核验。实际审计等待PID3810420/starttime1456826568，专用socket与SHA
集中于阶段计划。日志、锁和失败保护保留，不能重复派发或把此准备写成审计完成。

## 实际依赖绑定与队列（2026-09-11）

A09正式训练已产生后，核验原入口、直接训练子进程及独立CPU审计的真实PID/starttime、
精确命令、完整存储环境和独立tmux pane，绑定43项不可变文件。实际来源与预登记相符，
A09/A10固定源码均未改动；没有用预填PID或GPU等待代替训练依赖。

按原已验证组件执行实际绑定检查，确认parent未就绪并保持等待，再准备新队列，02:32:17
单次启动；02:32:47独立核验真实进程、空CUDA、环境及全部原依赖通过。此时没有A10最终
配置目录或模型目录，parent尚未确定。回执及所有固定文件SHA集中于阶段计划第10节。

队列在A09入口、训练及审计全部结束且完成证据通过后，按既定规则确定parent、重新执行
最终全量预检，再进入逐阶段GPU等待；失败或未选中候选的晋升歧义保留日志并停止。
没有自动重试、覆写工件或扩大实验矩阵；当前结果仍为无。

## 依据与边界

A04是目前已审计保留的基线，其候选CE实验取得了明显改善；这支持继续研究训练负例，
不能说明增加结构相似度必然有效。[JESTR正式论文](https://academic.oup.com/bioinformatics/article/41/7/btaf354/8165426)
第2.3节按目标的Tanimoto相似度排列候选，用于训练末段的额外正则化；其同分子式PubChem
候选、损失形式及末3%训练安排与本轮不同。本轮只借鉴结构相似候选的选择思路，不引入
额外候选、真值分子式条件或该论文的整套训练安排。

该选择依据固定分子结构，不能称为按模型分数挖掘的难负例。两塔仍独立映射到共享嵌入
空间，以原对比学习及候选CE训练，按归一化嵌入相似度检索；结构相似度只服务训练采样，
不加入验证打分或候选排序特征。完整输入证据维护于[诊断D22](analysis/massspecgym_optimization.md#d22全训练自然负例的结构相似度诊断)。

## 预先固定的采样规则

对原始训练query对应的自然Mass候选池，保留已审计的原始分子、源位置与二维身份：

1. 排除全部目标二维身份，将其余源条目按二维身份分组，得到N个不同负例身份；全部组
   按二维身份键建立规范序列，组内条目仍按原始源位置排列。
2. 对每个源条目，以既有无手性Morgan radius2、2048位指纹与**原始目标精确SMILES**
   的指纹计算Tanimoto；组分数为该组全部源条目相似度的算术均值。
   使用整数交集/并集及精确分数求均值、排序，避免浮点舍入随别名数改变同分关系；
   相同组分数按二维身份键排序，不能用官方源位置作相似度同分破局。
3. 取前min(32,N)个组组成相似池，均匀无放回抽min(8,B)个组，其中B=min(16,N)。
   再从全部尚未选中的组均匀无放回抽满剩余预算；剩余池保留规范身份顺序，包含未选中的相似池成员。
4. 私有NumPy RNG仍由seed、epoch、原始query索引确定；两段抽取后随机打乱已选组，
   每个组仍均匀抽一个原始源条目。明确固定随机调用顺序并保存可独立重放的采样摘要。
   不改变全局训练/增强RNG，不使用模型当前权重或验证/test误差选择负例。

N不足16时保留query并用全部可用负例；N为0时保留原in-batch监督与query分母，候选CE为0。
相同指纹不能合并不同二维标签，也不能排除指纹相同的真实负例；同身份多条源表示保留。
预算、CE权重/温度/公式与启用轮次继承parent，只有上述选择分布改变；不宣称该采样是完整
自然候选softmax的无偏估计。32与8为本轮固定设计，不同时扫描池宽、比例、负例数或学习率。

## Parent与继承参数

Parent是A09完整审计后唯一保留的incumbent：其选中checkpoint通过既定整体改善标准则
继承A09，否则继承其已审计比较parent；只有未选中Pareto候选达标时，先解决选优和权重
绑定歧义。任何前序未完成、失败或审计不一致状态都不能触发本轮。

从随机初始化训练，parent权重只作fresh比较；完整继承其谱图Q/K等已晋升开关、GINE或
GINE+指纹结构、增强、优化器和调度。不修改SpecEmbedding质谱预训练架构。
保持seed42、batch128、质量block32、学习率1e-4、CE16、100轮上限和patience5。
正式配置统一写入最终params，逐项比对parent只新增采样策略及其固定输入绑定。

固定MassSpecGym v1.5全部194,119条train、既定6条排除后的19,423条val、原自然候选和
二维身份，无forcing；验证每轮完整重编码与既定Top-1/MRR选优、Top-k Pareto保留规则不变。
本轮不做seed矩阵，不用test选择结构或采样参数。

## 缓存、存储与开销

原Morgan及规范图的构造参数、分子顺序不变，验证来源后复用；不缓存学习中embedding。
新增训练候选相似度/组顺序缓存应放在外部项目存储根下独立的`training_candidate_cache/`
版本目录，绑定数据与完整元数据SHA、目标精确SMILES、二维身份、候选源位置、指纹来源、
精确均值/同分策略与实现版本。任何相关构造变化均使对应派生缓存失效，不能用目录存在
代替核验；清理时保留仍被活跃任务引用的旧工件。不会使构造不变的固定图/指纹缓存失效。

相似度仅在CPU准备，训练按已核验的组索引采样；每query的候选编码上限仍为16，不额外
编码候选来找高模型分数，也不把完整固定指纹库搬上GPU。若最终parent不使用指纹分支，
Morgan只用于CPU采样准备，不改变其分子塔输入。实际CPU读取、GPU峰值和完整训练耗时
须重新测量；相同预算不是等耗时或提速保证。

## 启动依赖、验证与验收

独立全训练输入诊断、采样/缓存/完成审计接入检查及真实全量缓存和A04条件预检已通过。
正式入口须拒绝缺失或不匹配的新策略来源，不能按旧均匀采样静默运行。
需覆盖精确同分/别名数量、正例全排除、指纹碰撞、小池和空池、源顺序置换、私有RNG、
真实worker跨epoch与全query重放；未启用时旧采样结果逐字节不变。

在A09训练期间完成组件和全量条件预检；A09审计结束后才绑定最终parent、固定源码和
单次派发队列。继续GPU0/1择一、显式cuda:N、显存准入和逐阶段等待，不修改现有A08/A09
源码、队列或单次派发锁。

完成后独立核对逐轮完整query、负例来源及数量、全部Top-1/5/10/20和MRR、选优权重与
成本，沿用既定整体晋升标准；未晋升则在attempts.md加删除线并保留原因。结构相似负例
可能增加不易区分的异构体或指纹碰撞，也会改变梯度分布，不能预设Top-1收益或最终SOTA达标。
最终模型结果：**无**；训练中途日志见上节。

## 准备记录

2026-09-10，D22已核验全部训练候选的固定指纹交并集、二维组均值及原采样重放；
完整输入分布、指纹碰撞和原始工件见诊断索引与阶段计划，不能作为本轮模型结果。
首版诊断的浮点同分问题已保留失败记录，精确分数版在新目录独立复核通过；22项合成
检查覆盖RDKit零位向量语义、重复别名同分、有限池期望及正例排除，Ruff和源码编译通过。
当时只完成了采样设计的输入依据，正式训练数据路径、worker、checkpoint及完成审计尚未接入。

随后已实现[正式采样与缓存入口](docs/structural_sampling_zh.md)：旧schema1均匀采样保留，
新策略必须显式配置并绑定经完整审计的精确排序缓存；训练及完成审计分别实现采样重放。
验证覆盖大池、小池、空负例、指纹碰撞、源位置/精确同分、全局RNG保持、旧缓存隔离、
缺失/失效来源拒绝，以及GINE真实小模型训练与GINE+指纹的增强、spawn跨epoch和梯度。
全仓701通过、1跳过、1个相同既有路径审计失败，Ruff、外部字节码目录compileall和diff
检查通过。SpecEmbedding预训练源码未改变，A08/A09固定源码、队列与输入缓存未修改。
上述实现已提交为`a8ad5f2`并推送，固定源码工作树干净。全量相似度缓存构建、逐位独立
审计及完整来源重载已通过，覆盖全部194,119条训练query、25,046个目标候选池和
6,350,650个源条目；完整负例二维组顺序为6,148,837组，训练worker映射的组顺序文件
为12,823,680 bytes。具体来源、路径和回执集中于阶段计划，不把缓存构建耗时算成训练提速。

21:57:32基于已审计A04配置的真实全量CPU条件预检通过，27项输入均重新核对，schema2
采样配置和全部派生/固定指纹来源进入正式清单；训练、增强、模型和负例预算与A04相同，
只增加采样策略，存储路径和GPU等待设置按既定规则更新。没有模型运行目录或GPU派发。
此预检验证A04分支可用，不能选定A10最终parent；仍须等A09完整审计并重新核验最终配置。

22:03:51完整真实输入抽样复核通过：对全部194,119条query执行seed42/epoch1新采样器，
与独立源分组/RNG重放逐项一致，共3,105,854个负例，与原CE16的小池预算相符；每query
来自相似前32组的负例平均8.8384个、最少8个。正例全排除、二维身份不重复、源位置及
全局Python/NumPy/PyTorch随机状态均核验。缓存交并集与D22独立诊断逐字节相同，43项
输入在前后均重新核对；抽样数组、脚本和回执另行读回通过。这里只是CPU采样与输入准备，
不代表完整训练、吞吐提升或检索收益；来源集中于阶段计划。

22:28:27已归档仅CPU的最终parent绑定入口，85项检查通过，覆盖GINE与GINE+指纹继承、
谱图开关保持、原CE16参数保留、完整输入来源、真实CLI及阶段命令构造、失败/旧输出拒绝、
原进程仍活跃时等待和未选中Pareto候选晋升歧义。继承指纹分支时，baseline重编码和正式
训练分别显式绑定既有完整指纹来源；仅新增结构采样，不能因更换parent遗漏分子输入。
旧GINE允许省略模型type字段，配置保持原样，不能为了测试方便新增模型改动。

真实宿主机检查返回等待A09，已核对原衔接PID/starttime及固定队列回执；没有最终配置
目录或模型派发。归档脚本和7项文件、4项固定参考另行哈希读回通过，Ruff及源码编译通过。
最终入口只生成审计后的配置与完整预检，不负责启动训练；A09真实启动后还需绑定其原
训练及独立完成审计进程，才能绑定并部署A10单次衔接队列。不能用准备回执代替A09完成审计。

23:17:57已归档单次衔接组件，24项合成检查通过，覆盖三种已审计parent分支、预检命令
一致性、证据变更/重复输出拒绝、前序及预检失败保留、永久单次claim与独立tmux真实
参数/环境传递。实际A09完成依赖SHA须在部署时绑定，不能预填未来trainer或审计PID。
真实未绑定检查确认缺少该文件时拒绝创建队列、socket、最终配置或模型目录；11项
归档文件和3项固定参考已另行读回。Ruff和源码编译通过，没有启动A10等待进程或GPU任务。

后续使用归档的`deploy_queue.py --binding-sha256 <实际SHA> --check-only`先核验真实依赖，
再用同一绑定生成待派发队列；完整读回后由该队列的`successor.py --launch`单次启动。
环境继承已审计条件预检的外部存储与空CUDA设置。等待程序仅在原A09入口、训练及审计
全部完成且通过核验后调用既有最终绑定/全量预检，再exec唯一正式入口，后者逐GPU阶段
重新等待。路径和SHA集中于阶段计划，尚未执行上述部署；准备完成不代表模型结果。
