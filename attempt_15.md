# A15：谱图注意力输出的按头动态门控

- 状态：**准备中，未启动**。在A10训练期间预登记；最终parent须等A14完整独立审计后
  决定。目前没有组件实现、运行目录、队列、GPU成本或检索成绩。
- 唯一主要假设：按当前峰表示动态调节不同注意力头的输出，可能改善谱图表示的区分能力，
  提高完整验证Top-1，并按既定Top-k/MRR整体标准验收。

## 依据与边界

[Gated Attention预印本v1，§2.2及Table 1](https://arxiv.org/html/2505.06708v1)比较注意力
不同位置和粒度的门控，包含在SDPA输出、输出投影之前，按头计算乘性sigmoid门控。
门控以pre-normalization后的当前token表示为输入。本轮选择轻量按头版本，并作下述
恒等初始化适配；不是论文整套语言模型复现，不据其成绩预估本地收益或训练提速。

现有谱图塔已具备self-attention；没有证据证明它存在有害attention sink。母离子token
有物理意义，注意力集中于它不自动代表缺陷。本轮不删除、强制降权母离子或筛掉其它峰。
仍由两塔独立映射至共享空间，以归一化嵌入相似度检索，不引入查询—候选交互。

## 固定改动

只在谱图Transformer的每层SDPA之后、拼接各头并执行原输出投影之前加入门控。
令该层原pre-norm峰表示为X，原注意力头输出为Z，逐峰逐头计算：

```text
g[b, peak, head] = 2 * sigmoid(X[b, peak] @ W_gate[head])
Z_gated[b, head, peak, :] = g[b, peak, head] * Z[b, head, peak, :]
```

每层W_gate独立、无bias，全部零初始化；初始g=1，范围为(0,2)。与原文通常的(0,1)
sigmoid不同，固定倍数2用于保持原注意力分支初始幅度，不能称为原配置复现。
不增加归一化、dropout、辅助损失、温度或学习率改动；保留原Q/K/V、输出投影、FFN、
残差、掩码和汇聚规则。新参数初始化不得消耗原有随机数流。

按当前已审计构造的宽度512、16头、4层，新增参数预计为
`4 × 16 × 512 = 32,768`；须由实际构造核验。不改SpecEmbedding预训练共享类，
下游变体独立定义；正式入口必须记录完整门控配置并严格重载，不能由推理快速路径跳过。

## Parent、参数与数据

Parent为A14完整审计后唯一保留的incumbent；A14未晋升则继承其已审计parent。只继承
此前真正晋升的图上下文、图+指纹、谱图汇聚/QK/加合物及候选采样/CE权重；不叠加未晋升
改动。确认谱图塔仍为512宽、16头、4层且无同类门控；不符时先排查，不能静默换假设。

从随机初始化完整训练，parent checkpoint仅用于fresh完整验证比较。固定seed42、
batch128、质量block32、CE16、lr1e-4及100轮/patience5，继承优化器、调度、梯度裁剪
和增强。MassSpecGym v1.5自然Mass候选、二维身份、194,119条train和19,423条有效val
及六条排除不变；不使用真值分子式、外部配对训练样本或test调参。

## 缓存、存储与资源

门控只作用于当次前向的学习表示，不增加原始峰、图或指纹字段。输入构造和来源仍有效时，
现有固定缓存可复用；若继承加合物条件层，须同时绑定其已审计元数据。学习embedding
随权重重新编码，不持久缓存门控值或沿用旧模型分数。构造/版本发生变化时按受影响字段
停用旧输入缓存，确认无活跃引用后清理并保留审计记录。

全部新工件仍存于外部项目根`/data1/${USER}/SpecEmbedding/`，源码冻结在外部repos目录，
不改活跃worktree。继续显式cuda:N、GPU0/1择一及既定逐阶段等待。额外参数少不意味着
训练更快：显式门控可能改变可用融合内核，须测量实际显存、完整训练/验证耗时后评估准入，
不缩减训练数据或静默改变batch。

## 启动依赖与验收

先检查零门控下原权重/初始化RNG和有效峰输出，再用独立逐头矩阵公式验证非零门控、
梯度和真实两步优化更新；覆盖训练/eval/inference_mode、布尔及规范浮点padding、
单峰、无padding、谱图/峰顺序和分块编码。原pre-norm、QK、汇聚及加合物组合均需核验，
检查门控值范围与掩码后的统计；这些统计不能替代检索指标或证明噪声抑制。

接通构造、训练CLI、保存重载和完成审计后执行完整CPU输入预检。A14实际训练与独立
审计身份出现后才绑定PID/starttime；前序全部完成并确定parent后再生成最终配置、
重新预检和单次派发。不得预填未来PID、移除单次锁或自动重试失败任务。

训练结束核验全量计数、完整验证分数、选优及Pareto权重和资源记录，按当前Top-k/MRR
整体规则晋升；未晋升的已完成尝试在总表加删除线。当前结果：**无**。
