# A04：增加自然训练候选监督

- 状态：2026-09-09 21:08:20单次派发，CPU准备及GPU0上的baseline完整验证均已完成；
  21:17:15重新通过GPU门槛，在物理GPU1的严格cuda:0上训练；2026-09-10 02:19:33完成
  前20轮，02:27核验原任务继续，当前选优为epoch20；该固定验证快照另经独立CPU重算。
- 主目标：改善MassSpecGym v1.5 Mass检索的Top-1/5/10/20，MRR辅助；只使用验证集选优。
- Parent：已审计的A02 epoch22；A03完整27轮及全部保留候选审计完成，均存在实质退步，
  2026-09-09已保存继续保留A02的唯一决策。训练从随机初始化开始，比较checkpoint不作为
  初始化权重；分子图增强沿用A02的node_drop_rate=edge_mask_rate=0.1。

## 假设与唯一主要改动

现有质量邻近batch仍未直接要求模型区分每条谱图的自然检索候选；在原双向in-batch损失上
增加query内候选交叉熵，以对齐训练判别任务与完整候选检索。保留现有两塔和余弦检索方式，
不增加模型参数，不修改SpecEmbedding预训练模型结构。

每条训练query从其已审计的自然候选池中，按不同二维身份均匀、无放回抽取最多16个负例；
同一身份有多个源条目时再均匀抽取其中一个，排除所有真值二维身份别名。正例使用原配对分子
的当前塔表示，负例经当前分子塔参与反向传播。原损失和新增候选损失的权重均为1。
这是抽取候选上的监督目标，不宣称是完整256候选softmax的无偏估计。

## 固定条件与资源

- seed42，batch128，mass_blocks/block32，AdamW学习率1e-4、weight decay1e-4，最多100轮，
  patience5，按完整验证Top-1/MRR选优并保留Top-k/MRR Pareto候选。
- 全部194,119条train query每轮恰好一次；有效验证19,423条，保留既定6条排除；没有test调参、
  reranker矩阵或额外seed。自然候选无forcing，不按目标分子式过滤Mass候选。
- 谱图增强、图增强和其余训练配置沿用最终parent；自然候选负图使用相同图增强策略。
  增加负图会增加增强调用次数，不声称原正例增强的逐次随机实现与parent完全一致。
- 负例池LRU128，未增强负图缓存每worker最多10,000个，原训练workers4；负例采样用独立
  seed/epoch/raw-query RNG，新增候选负图的反向激活目前全部保留，不宣称已实现显存分块。
- 当前元数据覆盖所有query且均有自然正例；10条query只有11个不同二维负例，其余可取满16，
  这些query全部保留。若未来输入存在空负例池，也保留query和原in-batch监督，候选CE为零。
- 物理GPU0/1择一、UUID映射显式cuda:0，各GPU阶段执行原空闲门槛；首轮完整测量新增图的
  时长、吞吐和显存，之前A02/A03的每轮4.3分钟不作为A04速度承诺。
- 完整512→1024→512测量通过预定完整性/数值/耗时门槛，最终验证分子batch设为1024，
  谱图batch128、验证workers4不变；这是单独注明的工程改动，不修改旧run的源码/配置。
- 复用已核验的完整CPU验证索引及相邻回执，逐字节复制到新run并重新检查来源/配置/完整性；
  不复用模型embedding，各checkpoint及每轮验证仍重新编码。真实全量导入核验见D08。

训练的无标签自然候选结构与部分held-out目标身份有交集（详见优化索引D06），但没有引入
held-out谱图—结构配对或正例标签；这一候选结构暴露需在后续对标时披露，不能隐去。

## 已准备的证据与衔接规则

- 数据/候选元数据及独立全量采样、实际谱图/token映射核验见[优化索引D06/D07](analysis/massspecgym_optimization.md)。
- 正式CLI、输入指纹、实际query顺序文件和完整采样重放已接入；候选监督实现为`67787bc`，
  CPU验证索引复用为`82c20dd`，未使用的A04 worktree确认干净且无进程后已更新；固定worktree
  与预检位置见[阶段计划第10节](docs/paper-change-plans/2026-09-07-MassSpecGym全量重训.md)。
- 最新全仓382通过、1跳过、1个相同既有路径审计失败；检查覆盖真实小模型训练、计数正确
  但重复query、设置/顺序/采样指纹被篡改，以及索引复制期间源变化和已有目标拒绝；
  Ruff、compileall、diff检查通过。
- 真数据dry-run已核验全部训练候选、源码与GPU池，预计算的query映射SHA与独立D07完整映射
  一致；包含CPU索引复用的新预检通过，与旧预检的训练配置、候选来源及两个GPU阶段命令
  相同；这是输入证据，不是训练收益。最终使用A02和验证batch1024的全量预检于21:05通过，
  与此前预检的运行配置相比仅验证分子batch改变，配置来源和指纹重新保存。
- A03训练期间已提前完成上述准备，结束后的完整审计与A02基线决策也已完成；独立验证batch
  测量已在衔接期间完成，结果通过预设门槛后采用1024，最终指纹核验通过；模型训练仍串行，
  不因GPU空闲而跳过协议核验或重复派发。
- 出现NaN、OOM、指纹变化或覆盖错误时保留失败工件、确定原因后使用新版本/新目录；不得
  自动降低负例数、训练条数或精度继续运行。完成后在attempts.md用一句话记录结果和基线决策。

## 中途资源观察

截至02:19:33，前20轮已完成，最近资源回执仍记录train194,119、val19,423。当前选优epoch20的完整
验证快照记录Top-1/5/10/20为17.14%/39.79%/53.08%/66.76%，MRR0.2867；各项超过本run
重新编码的A02比较基线，但仍是中途结果，完整训练、最终选优与晋升审计尚未结束。
epoch19/20先后刷新最佳，第19轮Top-1为16.51%、MRR0.2852；按原patience5继续训练，
不提前停止或改配置。最近epoch18/19/20总耗时约757/773/941秒，第20轮又增至15.7分钟；
epoch20训练705.83秒、完整检索验证229.33秒。训练阶段占主要时间，不能套用A03“验证约占三分之二”的比例，
更不能把所有等待都归为CPU瓶颈；新缓存只影响未来run。
epoch7的短时资源观察发现数据worker忙于CPU工作且GPU1有另一计算进程，详情及完整训练流
缓存容量模拟见优化索引D10；无法据此量化整轮CPU等待比例或竞争带来的速度损失。

## 固定前四轮的中途独立审计

22:34:57完成CPU审计，对固定baseline及epoch1–4保存分数独立重算全部19,423条query的
二维排名和Top-k/MRR，并核对每轮实际query顺序恰好覆盖194,119条、计数及文件指纹。
该审计固定到epoch4，不能将后续epoch5–20的日志成绩写成已通过此审计；没有重新运行模型。

epoch4的Top-1为8.85%、MRR为0.1862，相对baseline净增247条Top-1命中，其中新增1,358条、
丢失1,111条。Top-5/20分别净少102/57条，按既定规则为取舍，不能以该快照提前晋升。
错误Top-1中与真值同分子式的比例从46.28%变为41.62%；这是事后描述，不能证明误差原因，
也不用于按真值分子式过滤候选。多条谱图可能属于同一分子，不将这些计数作为独立样本显著性。

审计回执、逐query配对排名及所固定的checkpoint/输入SHA集中于阶段计划第10节。
完整自然负例采样重放仍留待最终审计；当前只核对顺序和已记录计数，不把它写成完成审计。

2026-09-10另完成固定epoch20及baseline的完整保存分数重算，配合全部train/val元数据
诊断，见[优化索引D14](analysis/massspecgym_optimization.md)。Top-1净增加1,859条命中；
该项不覆盖epoch5–19全部轨迹，也不包含最终权重选择或负例重放，不能据此提前晋升。

运行位置、专用socket、最终预检和派发指纹集中在阶段计划第10节。A04固定源码与配置保持
不变；下一轮及缓存准备见[A05实验卡](attempt_5.md)。完整结果与独立完成审计仍待训练结束。
