\documentclass[runningheads]{llncs}

% Double-blind submission source. External results remain reported-only unless
% reproduced under the same software and candidate protocol. Venue-specific
% page, format, disclosure, and anonymity checks are applied at submission.

\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,fit,positioning}
\usepackage{hyperref}

\hypersetup{
  hidelinks,
  pdfauthor={},
  pdfsubject={},
  pdfkeywords={mass spectrometry, molecule retrieval, learning to rank}
}

\newcommand{\method}{SpecEmbedding-Rerank}
\newcommand{\base}{SpecMolAlign}
\newcommand{\zs}{\mathbf{z}_s}
\newcommand{\zm}{\mathbf{z}_m}
\newcommand{\candidates}{\mathcal{C}}

\title{Supervised Non-Generative Reranking for Fixed-Candidate MS/MS Molecule Retrieval}
\titlerunning{Spectrum-Conditioned Molecular Reranking}

% Keep these fields empty for double-blind review.
\author{}
\authorrunning{}
\institute{}

\begin{document}

\raggedbottom
\setlength{\emergencystretch}{3em}

\maketitle

\begin{abstract}
\begin{sloppypar}
Retrieving molecular structures from tandem mass spectra is difficult because
mass- and formula-constrained candidate pools contain chemically similar hard
negatives. We study whether supervised, non-generative second-stage reranking
can improve a fixed candidate list without generating new structures. \method{}
first aligns a peak-sequence spectrum encoder with a molecular graph encoder and
reuses saved embeddings for the supplied pool of at most 256 candidates. It
then performs pointwise coarse scoring followed by a spectrum-conditioned
relative candidate module on the coarse top 40. The relative preference is
constructed as an antisymmetric pair difference and is equivariant to
candidate-list permutation; no retrieval-rank feature or training-time
positive insertion is used. We compare the relative model with a near-
parameter-matched no-rank pointwise control. On MassSpecGym mass- and
formula-conditioned candidate pools, both supervised rerankers improve the
fixed cross-modal retriever in the saved-embedding, cache-local evaluation,
while pointwise is slightly stronger in the main R@1/MRR summaries. We
therefore present the relative branch as a formally defined candidate-
interaction mechanism and study its empirical boundary, rather than claiming
a universal relation-module or external-baseline advantage. The method is
closed-set and non-generative: it improves the ordering of supplied candidates
but cannot recover a molecule absent from that pool.
\end{sloppypar}
\keywords{Data mining for bioinformatics \and Tandem mass spectrometry \and
Molecule retrieval \and Learning to rank \and Multimodal representation
learning}
\end{abstract}

\section{Introduction}

MS/MS is widely used to identify small molecules in metabolomics,
natural-product discovery, and related biomedical applications.
An observed spectrum is only a partial, condition-dependent view of a molecular
structure: different molecules can yield similar fragments, while the same
molecule can produce different spectra across instruments, adducts, and
collision energies. Consequently, structure annotation is naturally a
large-scale data mining problem in which a query spectrum must retrieve and rank
a correct structure from a chemically constrained candidate pool.

MassSpecGym provides structure-disjoint data splits and standardized
mass- and formula-conditioned candidate pools for this problem
\cite{bushuiev2024massspecgym}. Recent methods mainly address the cross-modal
gap between spectra and molecules. JESTR learns a spectrum--molecule joint space
with contrastive multiview coding and ranks candidates directly by cosine
similarity \cite{kalia2025jestr,tian2020cmc}. GLMR instead retrieves contextual
candidates, generates a refined molecule with a conditional language model, and
reranks candidates by molecule--molecule similarity \cite{zhang2026glmr}.

Both approaches leave an important data mining question open. Candidate-wise
cosine scoring does not explicitly train a second-stage objective over the
retrieved set, while generative retrieval adds an autoregressive decoder and
makes ranking depend on the quality and validity of a generated structure. We
therefore ask two related questions: can a supervised second-stage ranker
separate hard candidates, and does spectrum-conditioned relative comparison
add value beyond a near-parameter-matched pointwise scorer? The first question tests
the value of learning to reorder a fixed list; the second tests whether
candidate interaction is needed for that gain. Within each paired comparison,
the candidate pool and first-stage representations are held fixed so that the
comparison concerns the second-stage ranking mechanism; the cross-alignment
matrix separately measures sensitivity to different saved first-stage
checkpoints.

Building on the peak-sequence spectrum architecture introduced by SpecEmbedding
\cite{xiong2025specembedding}, we construct \method{}, a two-stage retrieval and
reranking framework. The first stage trains that spectrum tower from scratch and
aligns it with a GINE molecular graph encoder. The second stage scores the full
supplied pool with an absolute branch, then refines only a coarse top-$K_r$
subset with spectrum-conditioned candidate relations. Pair preferences are
formed by subtracting the score of the reversed ordered pair, which gives
antisymmetry without a rank embedding. Training uses only naturally present
positives; the implementation never repairs a truncated list by inserting its
target. A pointwise model with the same absolute branch provides the primary
capacity-controlled comparison, while the earlier candidate-set Transformer is
kept only as a historical reference.

Our contributions are:

{\raggedright
\begin{itemize}
  \item We formulate a direct, closed-set, non-generative second-stage learning-
  to-rank framework for hard candidates, using full-pool coarse scoring followed
  by spectrum-conditioned relative candidate comparison.
  \item We define a rank-free relative branch with antisymmetric pair
  preferences, candidate-list permutation equivariance, and no training-time
  positive forcing, together with a near-parameter-matched pointwise control.
  \item We systematically examine the framework on MassSpecGym mass- and
  formula-conditioned pools, three saved alignment checkpoints, and a 30-run
  relative-module ablation matrix: supervised reranking improves the base
  retriever, while the evidence does not establish an independent advantage
  of relative interactions over pointwise scoring.
\end{itemize}
\par}

\paragraph{AI-assistance disclosure.}
OpenAI Codex assisted throughout all sections with drafting and language
revision, and during the project with code editing, experiment orchestration,
and consistency audits. Reported observations and numerical results came from
the stated software pipeline, not model generation. The authors reviewed all
AI-assisted text and code, verified the technical claims, citations, saved
artifacts, and reported results, and assume full responsibility for the
manuscript's accuracy, completeness, and originality.

\section{Related Work}

\subsection{Annotation Paradigms}

Computational MS/MS annotation differs in retrieved objects and ranking
evidence. Candidate-score correction predates our work: MetFusion combines
in-silico fragmentation with spectral-library evidence
\cite{gerlich2013metfusion}; MS2Query reranks its top 2,000 MS2DeepScore matches
with a five-feature random forest \cite{dejonge2023ms2query}; and LC-MS2Struct
combines an MS/MS scorer with retention order to jointly rank candidates from
one LC analysis \cite{bach2022lcms2struct}. We instead rank structures in
query-specific MassSpecGym pools from frozen spectrum--molecule
representations, without spectral-library-neighbor or retention-time evidence
\cite{bushuiev2024massspecgym}.

Within structure-database retrieval, CSI:FingerID and MIST predict molecular
fingerprints from spectra \cite{duhrkop2015csifingerid,goldman2023mist}.
CMSSP and JESTR learn joint spectrum--molecule spaces; MVP extends contrastive
alignment across molecular graphs, fingerprints, individual spectra, and
consensus spectra \cite{chen2024cmssp,kalia2025jestr,chen2026mvp}. These methods
order candidates by predicted-fingerprint scores or cross-modal similarity.
SpecEmbedding instead studies spectrum--spectrum representation learning
\cite{xiong2025specembedding}; we reuse only its peak-sequence architecture,
train it from scratch against molecular graphs, and learn a complementary
second-stage order within a retrieved hard-candidate list.

Other paradigms use different intermediate objects. MassFormer predicts tandem
spectra from molecular graphs \cite{young2024massformer}, while MSNovelist
generates structures from MS/MS-derived fingerprints
\cite{stravs2022msnovelist}. GLMR conditions its molecular decoder on retrieved
candidates and reranks them by similarity to the generated molecule
\cite{zhang2026glmr}. Our closed-set method does not decode a structure or
return a molecule outside the first-stage list; it directly optimizes the
scores of available candidates.

\subsection{Learning to Rank Candidate Sets}

Learning-to-rank objectives define supervision over preferences or complete
lists rather than treating similarity sorting as a fixed post-processing step.
RankNet learns pairwise preferences, whereas ListNet defines a probability
model over a ranked list \cite{burges2005ranknet,cao2007listnet}. Ranking
functions can also exploit relations among competing items. Set Transformer
provides attention blocks for set-structured inputs
\cite{lee2019settransformer}, while SetRank uses self-attention to model
cross-document interactions \cite{pang2020setrank}. We apply these principles
to cached cross-modal embeddings with explicit spectrum--candidate pair
features and first-stage score and rank priors. Table~\ref{tab:method_comparison}
positions the resulting query-specific, non-generative second stage among
selected related MassSpecGym approaches. The contribution is neither reranking
nor self-attention itself; residual scoring and candidate interaction are
evaluated design choices rather than assumed sources of gain.

\begin{table}[t]
  \centering
  \caption{Mechanistic comparison with selected related MassSpecGym approaches.}
  \label{tab:method_comparison}
  \footnotesize
  \begin{tabular}{>{\raggedright\arraybackslash}p{1.1cm}
                  >{\raggedright\arraybackslash}p{2.1cm}
                  >{\raggedright\arraybackslash}p{2.1cm}
                  >{\raggedright\arraybackslash}p{1.5cm}
                  >{\raggedright\arraybackslash}p{2.5cm}}
    \toprule
    Model & Ranking signal & Candidate interaction & Generates & Training stages \\
    \midrule
    JESTR & Spectrum--candidate cosine & None at inference & No
      & InfoNCE; optional late-stage candidate regularization \\
    GLMR & Generated--candidate cosine & Retrieved candidates condition the generator & Yes
      & Dual-path InfoNCE plus generation \\
    Ours & Residual score from pair features and retrieval priors
      & Spectrum-conditioned antisymmetric pair interaction & No
      & Cross-modal InfoNCE plus listwise/pairwise reranking \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Problem Formulation}

Let $s$ be an MS/MS spectrum, $m^+$ its correct molecule, and
$\mathcal{D}_s$ a query-specific mass- or formula-conditioned candidate
pool. A base retriever assigns

\begin{equation}
  b(s,m) = \operatorname{sim}(f_s(s), f_m(m))
\end{equation}

to every $m\in\mathcal{D}_s$ and returns the top-$K$ list
$\candidates_s=(m_1,\ldots,m_K)$. The reranker learns scores
$g(s,m_i,\candidates_s)$ such that $m^+$ is ordered as high as possible.
Because it cannot return a molecule outside $\candidates_s$, end-to-end
Recall@$k$ is upper-bounded by

\begin{equation}
  U_K = \frac{1}{N}\sum_{n=1}^{N}
  \mathbb{I}[m_n^+\in\candidates_{s_n}].
  \label{eq:upper_bound}
\end{equation}

Define the within-list top-$k$ accuracy conditional on first-stage coverage as

\begin{equation}
  A_{k\mid K} =
  \frac{\sum_{n=1}^{N}
  \mathbb{I}[m_n^+\in\candidates_{s_n},\,
  \operatorname{rank}_g(m_n^+)\le k]}
  {\sum_{n=1}^{N}\mathbb{I}[m_n^+\in\candidates_{s_n}]},
  \qquad
  \mathrm{Recall@}k=U_K A_{k\mid K}\le U_K.
  \label{eq:coverage_decomposition}
\end{equation}

A reranker applied to a fixed cache can therefore change only
$A_{k\mid K}$, not $U_K$. We report metrics over all queries, assigning zero
credit when the positive is absent rather than conditioning the primary metric
on successful first-stage retrieval.

\section{Method}

\subsection{Overview}

The framework has three steps: cross-modal alignment, full-pool retrieval, and
supervised coarse-to-fine relative reranking. Spectrum and molecule encoders are
frozen while the reranker is trained, so their embeddings can be computed once
and reused.

\begin{figure}[t]
  \centering
  \input{figures/method_overview.tex}
  \methodoverview{0}
  \caption{Overview of \method{}. Panel 1 aligns the spectrum Transformer and
  molecular GINE\@. Panel 2 uses their frozen, cached embeddings to produce the
  full candidate pool. Panel 3 applies pointwise coarse scoring followed by
  spectrum-conditioned relative candidate preferences on the coarse top-40;
  the legacy Transformer is a historical reference. Dashed arrows denote
  training-only operations. Positives are never forced into a truncated list,
  and no molecule is generated.}
  \label{fig:overview}
\end{figure}

\subsection{Cross-Modal Base Retriever}

The spectrum encoder follows SpecEmbedding's peak-sequence architecture
\cite{xiong2025specembedding}, which processes peak $m/z$ values and
intensities with sinusoidal $m/z$ features and a Transformer. In our
implementation, each spectrum has at most 100 tokens: a precursor-$m/z$ token
with intensity 2, followed by up to 99 fragments selected by intensity.
Selected fragments retain their source-array order, and their intensities are
normalized by the largest selected fragment intensity. Only these
$m/z$--intensity tokens enter the spectrum encoder; adduct, instrument, and
collision-energy metadata are not explicit inputs. We initialize this tower
from scratch and train it against molecules rather than spectra. The molecular
encoder is a GINE network over atom and bond features
\cite{hu2020pretraininggnn}. Mean-pooled graph features are augmented with
explicit graph-size features. Two projection heads map the encoders to a common
$d$-dimensional space:

\begin{equation}
  \zs = \frac{p_s(f_s(s))}{\lVert p_s(f_s(s))\rVert_2},
  \qquad
  \zm = \frac{p_m(f_m(m))}{\lVert p_m(f_m(m))\rVert_2}.
\end{equation}

We use a bidirectional cross-modal contrastive loss that permits multiple
positives. Spectra and molecules sharing a molecular label form positives,
which avoids treating replicate spectra as false negatives. For a minibatch of
size $B$, let $P(i)$ be the indices sharing the label of item $i$. With a
learned inverse-temperature scale $a$, the base logits are

\begin{equation}
  \ell_{ij}=a\,\mathbf{z}_{s_i}^{\top}\mathbf{z}_{m_j}.
\end{equation}

The spectrum-to-molecule term is

\begin{equation}
  \mathcal{L}_{s\rightarrow m}
  =-\frac{1}{B}\sum_{i=1}^{B}\frac{1}{|P(i)|}
  \sum_{j\in P(i)}
  \log\frac{\exp(\ell_{ij})}{\sum_{k=1}^{B}\exp(\ell_{ik})},
\end{equation}

and the molecule-to-spectrum term applies the same expression to
$\ell^\top$. The alignment loss is their mean. The implementation parameterizes
$a=\exp(u)$ and caps it at 100. At inference, the normalized dot product
$b_i=\zs^\top\mathbf{z}_{m_i}$ is the base score.

\subsection{Candidate Construction and Training Protocol}

MassSpecGym supplies target-molecule-keyed candidate lists; each query reuses the
list keyed by its target molecule. The base retriever scores the complete pool
of at most 256 molecules. The new coarse-to-fine reranker first scores this
full pool pointwise and then applies a relation module to the coarse top
$K_r=40$ candidates. Both 256 and 40 are fixed design and compute-budget
choices for this revision; the evaluation script can truncate a saved pool
without inserting a positive target.

Training uses only queries whose target naturally occurs in the full
256-candidate pool. Queries without a natural positive remain in coverage
statistics but do not enter the reranker loss. Candidate order is shuffled
during training while each candidate's embedding and base score move together.
No rank embedding is used anywhere in the new model, and the cache-compatible
\texttt{base\_ranks} field is ignored by its forward path. In particular, no positive
is inserted into a truncated list. Missing positives at a smaller evaluation
cutoff are therefore treated as coverage misses rather than repaired training
examples.

\subsection{Spectrum-Conditioned Relative Candidate Reranker}

For candidate $m_i$ we retain an absolute compatibility vector, but omit the
retrieval rank:

\begin{equation}
  \mathbf{x}_i=
  [\zs,\ \mathbf{z}_{m_i},\ \zs\odot\mathbf{z}_{m_i},
  |\zs-\mathbf{z}_{m_i}|,\ b_i].
  \label{eq:relative_features}
\end{equation}

An absolute MLP produces a coarse score $c_i=f_{\rm abs}(\mathbf{x}_i)+\alpha b_i$.
For the coarse top-$K_r$ candidates, let $u_i=W_m\mathbf{z}_{m_i}$ and
$v=W_s\zs$. The relation vector $\rho_{ij}$ contains the spectrum condition,
the signed difference $u_i-u_j$, its absolute value, the base-score difference
$b_i-b_j$, and the cosine similarity between $u_i$ and $u_j$. A shared MLP
defines an ordered preference:

\begin{equation}
  p_{ij}=g(\rho_{ij})-g(\rho_{ji}),
  \qquad p_{ij}=-p_{ji}.
  \label{eq:antisymmetry}
\end{equation}

The relation weights are spectrum-conditioned and normalized over valid
non-self candidates. With $w_{ij}\ge0$, the final score is

\begin{equation}
  q_i=c_i+\beta\sum_{j\ne i}w_{ij}p_{ij}.
  \label{eq:relative_score}
\end{equation}

The relation contribution is scattered back to the full candidate pool after
coarse top-$K_r$ selection. All operations are shared across candidates and
use candidate-attached masks, so jointly permuting a list and its attached
features permutes the output scores in the same way, up to tie handling in
coarse top-$K_r$ selection. Equation~\ref{eq:antisymmetry} gives raw pairwise
antisymmetry independently of the learned parameter values; the weighted
contribution in Equation~\ref{eq:relative_score} is not itself required to be
antisymmetric because $w_{ij}$ and $w_{ji}$ may differ.
The model uses no raw peaks, SMILES tokens, precursor mass, formula, or
acquisition metadata, and no rank embedding is present in its parameter path.

\paragraph{Structural interpretation.}
The reversed-pair difference constrains one ordered candidate pair to have a
single preference direction: swapping $i$ and $j$ changes only the sign of the
raw pair preference $p_{ij}$. This is a useful inductive bias for local statements of the
form ``candidate $i$ is preferred to candidate $j$ for this spectrum,'' and it
is compatible with the pairwise term in the training objective. The shared
candidate operations impose the corresponding equivariance at the list level.
These are function-class properties, not performance guarantees: pairwise
antisymmetry alone does not rule out cycles across three or more candidates,
prove better hard-negative discrimination, or imply an advantage over the
pointwise control. Those are empirical questions for future, separately
authorized experiments.

The implementation exposes an explicit \texttt{pair\_mode} control. The
reported relative model uses \texttt{pair\_mode=antisymmetric}, which realizes
Equation~\ref{eq:antisymmetry}; \texttt{pair\_mode=directed} keeps the same
ordered preference network but omits the reverse-pair subtraction as a
controlled implementation path. No directed training run is reported here, so
this interface does not constitute a new performance result. The legacy
\texttt{use\_antisymmetric} flag remains only for configuration compatibility,
and missing fields in older checkpoints default to the former antisymmetric
semantics.

The near-parameter-matched pointwise control uses the same absolute branch and
removes only the relative aggregation. At hidden width 512, the relative model
has 1,485,189 trainable parameters and the no-rank pointwise control has
1,478,690, a 0.44\% difference. The generic candidate-set Transformer below
is retained as a historical reference, not as the proposed mechanism.

\subsection{Secondary Historical Reference}

For context, we retain the earlier rank-aware candidate-set Transformer as a
historical reference. It uses a candidate-attached rank embedding and cross-
candidate self-attention \cite{vaswani2017attention}, unlike the current rank-free relative model. Its
historical training-time positive forcing and configuration are not part of the
formal protocol of the proposed method; further details are given in
Appendix~B, but no numerical comparison to the current primary table is made.

\subsection{Learning Objective}

For the labeled queries $\mathcal{B}_{L}$ in a minibatch, let $V_n$ be the valid
candidate set and $y_n$ the positive index. The primary objective averages
listwise cross-entropy over labeled queries:

\begin{equation}
  \mathcal{L}_{\mathrm{list}}
  =-\frac{1}{|\mathcal{B}_{L}|}
  \sum_{n\in\mathcal{B}_{L}}
  \log\frac{\exp(q_{ny_n})}{\sum_{i\in V_n}\exp(q_{ni})}.
\end{equation}

We also contrast each positive with every other valid candidate retrieved for
that query. The implementation averages over all valid positive--negative pairs
in the minibatch:

\begin{equation}
  \mathcal{L}_{\mathrm{pair}}
  =\frac{
  \sum_{n\in\mathcal{B}_{L}}\sum_{i\in V_n\setminus\{y_n\}}
  \log\!\left(1+\exp(q_{ni}-q_{ny_n}+\gamma)\right)}
  {\sum_{n\in\mathcal{B}_{L}}\left(|V_n|-1\right)}.
\end{equation}

The complete objective is

\begin{equation}
  \mathcal{L}
  =\mathcal{L}_{\mathrm{list}}
  +\lambda_{\mathrm{pair}}\mathcal{L}_{\mathrm{pair}}
  +\lambda_{\mathrm{spec}}\mathcal{L}_{\mathrm{spec}}.
\end{equation}

For a cyclicly mismatched spectrum $s'$ from the same minibatch, the
spectrum-dependency term contrasts the score of the true candidate under the
correct and mismatched query:

\begin{equation}
  \mathcal{L}_{\mathrm{spec}}
  =\operatorname{softplus}\!\left(
  q(s',m^+)-q(s,m^+)+\delta\right).
  \label{eq:spectrum_dependency}
\end{equation}

It is an in-batch dependency constraint, not a claim of causal identification.

The listwise term is a top-one likelihood: it separates the positive from the
valid list but does not prescribe a total order among negatives. Both terms are
invariant to adding the same query-specific constant to every $q_i$. Thus, the
raw outputs are relative scores, not probabilities; probability calibration is
neither performed nor evaluated.

For embedding width $d$, full-pool coarse scoring costs $O(Kd)$ per query and
the relative branch costs $O(K_r^2d)$ with chunked pair evaluation, rather than
quadratic work over all 256 candidates. This is a structural complexity
statement. The repository also provides a parameterized forward-only benchmark
that varies candidate cutoffs, warm-up and measurement batches, batch size, and
device while recording timing metadata; it excludes data loading and does not
compute retrieval metrics. The repository retains these scripts as
implementation references, but no timing, memory, throughput, or deployment
result is claimed in this paper.

\section{Experiments}

\subsection{Dataset and Candidate Pools}

We use the published MassSpecGym split \cite{bushuiev2024massspecgym}: 194,119
training spectra, 19,429 validation spectra, and 17,556 test spectra. These
counts total 231,104 spectra; the benchmark reports approximately 29,000 unique
molecular structures. Its similarity-aware, structure-disjoint construction
reduces close structural overlap between training and test molecules.

MassSpecGym supplies mass- and formula-conditioned candidate settings. The mass
pool uses a precursor-mass tolerance; the formula pool assumes a known formula
and is not unrestricted annotation. Frozen pools contain at most 256 candidates.
The reranker is closed-set: it improves the ordering of supplied candidates but
cannot recover a target absent from the pool or generate a new molecule.

Our primary evaluation is the overlap-clean, alignment-42 cache-local matrix,
which reuses the saved candidate order, embeddings, and reference
two-dimensional InChIKey identity labels; it is not a fresh official-loader
re-encoding or alignment retraining. A separate official-compatible
candidate-order and identity audit is retained as a protocol check, not as a
new primary result. A local exact-target-SMILES evaluation is retained as a
sensitivity view. Further protocol details are given in Appendix~A.

\subsection{Implementation Details}

The aligned model outputs 512-dimensional embeddings. Its spectrum Transformer
has four layers, 16 heads, and width 512; the molecular GINE has four layers,
hidden width 128, and dropout 0.2. No published SpecEmbedding checkpoint is
loaded, so the stage that would freeze a pretrained spectrum tower is skipped;
both towers are optimized end to end on the MassSpecGym training split.
Alignment uses AdamW with batch size 128, learning rate and weight decay
$10^{-4}$, cosine scheduling, gradient-norm clipping at 1, at most 100 epochs, and
patience 5. Each epoch makes ten draws per molecular label, randomly selecting
an associated spectrum. Spectrum and graph augmentation are independently
enabled with probability 0.5. The former removes a floor-rounded 20\% of
fragment peaks below normalized intensity 0.3 and jitters intensities by
$\pm15\%$; the latter applies 10\% node and 10\% directed edge-entry dropout.
Validation disables augmentation but retains replicate-spectrum sampling under
the fixed random stream. The main reranker matrix uses alignment checkpoints
produced with seeds 42, 43, and 44. Within each checkpoint, reranker seeds
42--44 control initialization, data-loader order, candidate shuffling, and
dropout; they do not turn the alignment checkpoints into independent draws
from a known population.

The proposed reranker has hidden width 512, relation width 32, pair chunks of
16, and dropout 0.1. It scores the full candidate pool at $K=256$ and applies
the relative branch to its coarse top-$K_r=40$. We use AdamW with learning rate
$10^{-4}$, batch size 32, at most 30 epochs, and early stopping on validation
MRR with patience 5. The pairwise and spectrum-dependency weights and margins
are $\lambda_{\mathrm{pair}}=0.2$, $\lambda_{\mathrm{spec}}=0.1$,
$\gamma=0.1$, and $\delta=0.1$. The near-parameter-matched pointwise control uses
the same absolute branch and optimization but no relation aggregation. The
legacy candidate-set Transformer is retained only as a historical reference.

For each reported experiment, alignment checkpoints and reranker training
seeds are listed with the result artifact. A reranker training seed controls
initialization, data-loader order, candidate shuffling, and dropout; it is not
an independent alignment replicate. Training uses only queries whose positive
is naturally present in the full candidate pool. Queries without such a label
are excluded from the reranker loss but retained in coverage statistics. No
positive is inserted into any cache. All models are selected without test-set
access. Each formal reranker run requests at most 20,000 training queries and
uses three reranker seeds. The three alignment checkpoints and nine paired
alignment--reranker combinations provide descriptive robustness evidence; they
are not a confidence interval, significance test, or complete end-to-end seed
analysis.

\subsection{Code, Data, and Artifact Availability}

The anonymous supplement provides source inspection, locked environment files,
automated tests, and a tiny synthetic CPU smoke test. It does not provide the
MassSpecGym spectra, candidate pools, trained checkpoints, or caches. The
repository's reproducibility index lists the local checkpoints, resolved
configurations, logs, hashes, and selected ranking outputs used for the formal
results. The real-data entry point is
\texttt{run\_rerank\_pipeline.py}; additional implementation and
reproducibility notes are given in Appendix~D. The repository source also
contains a query-level case exporter that records candidate identities and
base/reranker ranks for selected cache queries. It provides ranking evidence
for subsequent manual inspection, not fragment attribution or a chemical
causal interpretation.

\subsection{Baselines and Metrics}

The controlled comparisons are the aligned cosine retriever, the
near-parameter-matched no-rank pointwise scorer, and the relative reranker.
The earlier candidate-set Transformer is retained only as a historical
reference. We also run a local saved-embedding
cosine control that matches the scoring form associated with JESTR; it is a
reimplemented control, not an official JESTR encoder/checkpoint reproduction.
Because both this control and the aligned base retriever use cosine scores on
the same saved normalized embeddings, their local numbers coincide; this is a
protocol-matched scoring control rather than an independent JESTR comparison.
GLMR remains reported-only because its conditional generation pipeline is not
reproduced here.

We report Recall@1, Recall@5, Recall@10, Recall@20, and mean reciprocal rank
(MRR) over all 17,556 test queries. The primary table is a cache-local,
overlap-clean no-forcing evaluation at alignment seed 42, using the saved
candidate order and two-dimensional InChIKey identity labels. It uses three
reranker training seeds (42--44) and at most 20,000 training queries. A separate
table reports the same pointwise/relative comparison for alignment seeds
42--44. A tracked official-compatible candidate-order and identity audit is
reported as a protocol check, not as fresh loader re-encoding. Means and sample
SDs within an alignment describe reranker dispersion; the cross-alignment rows
are descriptive checkpoint sensitivity, not independent population
replicates. The current paper does not claim official JESTR reproduction,
statistical significance, or end-to-end deployment performance.

\subsection{Main Results}

\begin{table}[t]
  \centering
  \caption{Primary cache-local no-forcing evaluation at the overlap-clean
  alignment-42 checkpoint. Values are percentages; learned rows are means
  $\pm$ sample SD over reranker training seeds 42--44. All methods rank the
  same saved candidate pool and use the same two-dimensional InChIKey labels;
  this is not a fresh official-loader re-encoding. The expanded alignment
  sensitivity is reported in Table~\ref{tab:alignment_sensitivity}.}
  \label{tab:main_results}
  \tiny
  \setlength{\tabcolsep}{1pt}
  \begin{tabular}{@{}l>{\raggedright\arraybackslash}p{4.2cm}rrrrr@{}}
    \toprule
    Pool & Method & R@1 & R@5 & R@10 & R@20 & MRR \\
    \midrule
    \multirow{3}{*}{Mass}
      & \base{} (256-candidate pool) & 47.46 & 63.51 & 71.02 & 77.76 & 55.03 \\
      & Pointwise control
      & 58.12$\pm$0.54 & 73.61$\pm$0.54 & 78.63$\pm$0.48 & 82.24$\pm$0.10 & 64.91$\pm$0.47 \\
      & Relative candidate model
      & 57.88$\pm$0.59 & 73.55$\pm$0.58 & 78.72$\pm$0.29 & 82.15$\pm$0.13 & 64.78$\pm$0.52 \\
    \midrule
    \multirow{3}{*}{Formula}
      & \base{} (256-candidate pool) & 63.10 & 75.79 & 80.87 & 84.93 & 68.86 \\
      & Pointwise control
      & 70.76$\pm$0.59 & 80.07$\pm$0.35 & 84.33$\pm$0.08 & 86.86$\pm$0.08 & 75.17$\pm$0.41 \\
      & Relative candidate model
      & 70.88$\pm$0.38 & 80.60$\pm$0.37 & 84.68$\pm$0.26 & 86.85$\pm$0.09 & 75.38$\pm$0.27 \\
    \bottomrule
  \end{tabular}
\end{table}

Both learned models improve on their alignment-42 base in both pools. Pointwise
is slightly higher than relative at R@1 and MRR for mass (58.12 vs. 57.88 and
0.6491 vs. 0.6478), whereas relative is slightly higher for formula (70.88 vs.
70.76 and 0.7538 vs. 0.7517). These descriptive differences do not establish
an independent relative-module effect. Evaluation-only candidate-cutoff files
are retained in the repository; they are not separately trained models and are
not used as an additional primary result.

This controlled comparison gives the paper a two-part result. Supervised
second-stage scoring is useful within the supplied candidate pool, but adding
the relative interaction is not required for the observed improvement and is
not uniformly better than the matched pointwise scorer. The latter result is a
boundary of the proposed mechanism, not a failed comparison to be hidden.

The local exact-target-SMILES sensitivity view is summarized in Appendix~A and
does not change the displayed conclusion.

\begin{table}[t]
  \centering
  \caption{Descriptive alignment-checkpoint sensitivity under the expanded
  no-forcing top-40 reranker cache. Each learned value is the mean $\pm$ sample
  SD over reranker seeds 42--44; the cache-local matrix is not a fresh
  official-loader re-encoding.}
  \label{tab:alignment_sensitivity}
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{@{}llrrr@{}}
    \toprule
    Alignment & Pool & Base R@1/MRR & Pointwise R@1/MRR & Relative R@1/MRR \\
    \midrule
    42 & Mass & 47.46/.5503 & 58.12/.6491 & 57.88/.6478 \\
    43 & Mass & 35.92/.4433 & 58.32/.6398 & 57.61/.6350 \\
    44 & Mass & 23.91/.3226 & 46.42/.5238 & 47.05/.5284 \\
    \midrule
    42 & Formula & 63.10/.6886 & 70.76/.7517 & 70.88/.7538 \\
    43 & Formula & 45.08/.5185 & 64.72/.6836 & 64.10/.6795 \\
    44 & Formula & 28.73/.3687 & 54.26/.5877 & 54.35/.5911 \\
    \bottomrule
  \end{tabular}
\end{table}

Across the nine alignment--candidate--seed paired comparisons, relative minus
pointwise has mean R@1 differences of $-0.11$ and $-0.14$ percentage points
for mass and formula, respectively; the corresponding mean MRR differences
are $-0.0005$ and $+0.0005$. Relative wins five of nine R@1 pairs in each
candidate protocol. These are descriptive paired estimates, not significance
tests, and they support the boundary claim that supervised reranking is more
stable than any independent relative-module advantage.

\begin{sloppypar}
\paragraph{Mechanism analysis.}
Table~\ref{tab:relative_ablations} reports the complete 30-run relative-module
ablation matrix: five relative configurations, two candidate protocols, and
three reranker seeds at the alignment-42 checkpoint. The full configuration is
the antisymmetric, spectrum-conditioned model. Directed pairs, removal of the
pair loss, removal of the spectrum loss, and removal of spectrum conditioning
each change the two protocols in different directions; no variant uniformly
dominates the full model. The results therefore identify a behavioral boundary
for the module, rather than a causal ranking of its components.
\end{sloppypar}

\begin{table}[t]
  \centering
  \caption{Relative-module ablations under the cache-local no-forcing top-40
  protocol. Values are R@1/MRR percentages and decimals, respectively; each
  entry is a mean $\pm$ sample SD over reranker seeds 42--44.}
  \label{tab:relative_ablations}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}lrr@{}}
    \toprule
    Variant & Mass R@1/MRR & Formula R@1/MRR \\
    \midrule
    Full antisymmetric & 57.88$\pm$0.59 / .6478$\pm$.0052 & 70.88$\pm$0.38 / .7538$\pm$.0027 \\
    Directed pair & 57.97$\pm$0.49 / .6471$\pm$.0047 & 70.06$\pm$1.29 / .7471$\pm$.0106 \\
    No spectrum conditioning & 58.42$\pm$0.81 / .6522$\pm$.0069 & 71.01$\pm$0.40 / .7546$\pm$.0033 \\
    No $\mathcal{L}_{\mathrm{pair}}$ & 58.01$\pm$0.38 / .6488$\pm$.0037 & 70.91$\pm$0.36 / .7541$\pm$.0027 \\
    No $\mathcal{L}_{\mathrm{spec}}$ & 58.44$\pm$0.54 / .6516$\pm$.0064 & 70.59$\pm$0.79 / .7515$\pm$.0056 \\
    \bottomrule
  \end{tabular}
\end{table}

JESTR and GLMR remain reported-only, not reproduced, and not directly
comparable background methods. We retain their mechanism descriptions but do
not reproduce their numeric table or infer a relative ranking.

\paragraph{Difficulty strata.}
In the seed-42 local query analysis, the selected labeled subsets contain
14,726 mass and 15,670 formula queries. For the relative model, R@1 gains over
the base at base ranks 2--5/6--20/21--40 are +54.05/+38.13/+29.98 percentage
points for mass and +55.41/+34.18/+19.10 for formula. Base-rank-1 queries
instead decline to 87.41\% (mass) and 95.12\% (formula), from 100\% under the
base stratum. The pointwise control shows the same unresolved-query pattern;
these strata are descriptive and do not establish a causal relation-module
effect.

\paragraph{Historical baseline boundary.}
Earlier rank-aware Transformer results used the former top-40 cache and
training-only positive replacement. They are retained only as historical
context, not as measurements of the present rank-free, no-forcing method or
evidence for the relative module introduced here; further details appear in
Appendix~B.

\section{Discussion}

The expanded cache-local matrix separates candidate coverage from list ordering.
The full-pool upper bound is 100\% by construction of the saved cache, whereas
evaluation-time top-40 truncation gives mass and formula upper bounds of 83.88\%
and 89.26\%, respectively. Across three alignment checkpoints and three
reranker seeds per checkpoint, both learned variants improve their
corresponding base rows. Pointwise is slightly stronger in the aggregate
R@1/MRR comparison, while relative is not uniformly better at any cutoff. The
result supports supervised second-stage reranking as a framework component,
but does not isolate an independent relative-module benefit.

The 30-run ablation matrix shows that directed versus antisymmetric pairs,
spectrum conditioning, and the two auxiliary losses affect the candidate pools
and metrics differently. These observations are descriptive: they clarify the
behavior of the proposed module, but do not establish a causal component gain.

The relative branch also has a higher structural cost than pointwise scoring:
the full-pool absolute branch is $O(Kd)$, while pair evaluation on the coarse
subset is $O(K_r^2d)$. The repository retains fixed-hardware forward-only
measurements as implementation references; they are not end-to-end latency,
throughput, or deployment claims in this paper.

The primary matrix reuses a cache-local candidate order and saved alignment
embeddings. A separate official-compatible audit uses the published candidate
JSON and the same saved embeddings, but does not re-encode spectra/molecules
through a fresh official loader or retrain the first stage. External methods
and other candidate constructions are outside the comparison.

\section{Limitations and Responsible Evaluation}

The expanded reranker matrix requests up to 20,000 training queries, evaluates
three alignment checkpoints, and repeats each principal configuration with
three reranker seeds. This provides descriptive checkpoint sensitivity, not a
confidence interval, significance test, or complete end-to-end seed analysis.
The evaluation also reuses saved embeddings and supplied candidate pools; it
does not claim fresh official-loader re-encoding, external-baseline
reproduction, or candidate-aware alignment retraining. These limitations bound
the scope of the two-part result: the framework-level reranking observation is
supported within the evaluated checkpoints, whereas the relative module's
generality remains open.

The method is closed-set and formula-conditioned when the formula candidate
protocol is used. It cannot recover a target absent from the candidate pool,
generate a molecule outside that pool, or generalize automatically to other
candidate constructions and acquisition conditions. The spectrum tower uses
selected peaks and precursor $m/z$ but no explicit adduct, instrument, or
collision-energy metadata. Its pairwise structural constraints likewise do not
provide probability calibration or a guarantee of globally consistent ranking.
Further protocol and reproducibility notes appear in Appendices~A and~D.

\section{Conclusion}

\begin{sloppypar}
We introduced \method{}, a rank-free, no-forcing second-stage reranker that
combines full-pool coarse scoring with spectrum-conditioned relative candidate
preferences. Across three alignment checkpoints with three reranker seeds per
checkpoint, the expanded cache-local matrix showed that both the relative model
and its near-parameter-matched pointwise control improved their corresponding
base in mass- and formula-conditioned retrieval. The primary table uses the
overlap-clean alignment-42 saved-embedding cache, while a separate audit checks
the published candidate order and two-dimensional identity rule. Pointwise was
slightly stronger in the main R@1/MRR summaries, and the 30-run ablation matrix
found no uniformly superior relative configuration. The evidence therefore
supports a carefully scoped supervised reranking framework and makes the
relative interaction's boundary explicit: it is a principled candidate-
relation mechanism, but the current evidence does not establish an independent
or generally robust benefit over pointwise scoring. The method remains limited
to the fixed saved-embedding candidate protocol.
\end{sloppypar}

\appendix

\section{Evaluation Protocol and Candidate Identity}

The primary multi-seed matrix is cache-local: it reuses the saved candidate
order, embeddings, and reference two-dimensional InChIKey identity labels for
the overlap-clean alignment-42 checkpoint. A separate official-compatible audit
uses the MassSpecGym 1.3.1 retrieval candidate order with the same saved
embeddings; it does not perform fresh official-loader re-encoding or alignment
retraining. The two scopes are kept distinct so that the primary table is not
presented as a fresh official evaluation.

The tracked full-pool identity check found one identity-equivalent positive for
each of the 17,556 test queries and no multiple-positive or candidate-identity
collision in either candidate protocol. Exact-target-SMILES labels consequently
agree with the reference identity labels on these saved pools. The local
exact-target-SMILES evaluation is retained as a sensitivity view rather than a
second main protocol. Formula candidates assume a known formula; both settings
remain closed-set and the reranker cannot recover a target that is absent from a
requested candidate cutoff.

The dataset audit also recorded six validation and three test spectrum-record
matches with training records, with different target molecules. The affected
validation records are excluded from the overlap-clean alignment validation
configuration. The three alignment checkpoints used for sensitivity analysis
are internal descriptive estimates, not a claim of population-level stability.
Evaluation-only candidate-cutoff sensitivities use the same saved 256-candidate
cache and are not separately trained cutoff-specific models; the retained files
are supporting implementation artifacts rather than an additional primary table.

\section{Implementation and Historical Baselines}

The alignment model produces 512-dimensional normalized embeddings. The
spectrum tower has four Transformer layers, 16 heads and width 512; the
molecular tower is a four-layer GINE with hidden width 128. The reranker uses
hidden width 512, relation width 32, pair chunks of 16, dropout 0.1,
$K=256$, and $K_r=40$. AdamW is used for both stages; the reranker uses a
learning rate of $10^{-4}$, batch size 32, early stopping on validation MRR,
and the objective weights and margins reported in the method section.

A reranker seed controls initialization, data-loader order, candidate shuffling
and dropout. The reported means and sample standard deviations over seeds 42--44
therefore describe reranker-level variation, not independent alignment
replicates. Training queries without a naturally present positive remain in
coverage statistics but do not enter the reranker loss. No positive is inserted
into a current-method cache.

The current training entry point writes a lightweight \texttt{data\_summary} into
new reranker checkpoints. It records the actual labeled training-query count,
the requested maximum count, \texttt{train\_k}, model type, pair mode, seed, and
available cache-protocol fields such as split, candidate type, pre-retrieval
cutoff, and positive-forcing status. This metadata helps distinguish pilot
artifacts from other training scopes, but it does not change the forward path
or replace full-split, alignment-level, or uncertainty experiments.

The historical Candidate-Set Transformer uses a rank embedding and generic
candidate self-attention. Its earlier training-time positive forcing and
rank-aware configuration are not part of the current relative/pointwise
protocol. It is retained only to connect this paper with the earlier design;
its larger parameter count and different training protocol prevent it from
serving as the near-parameter-matched comparison.

\section{Additional Mechanism Analyses}

The 30-run ablation matrix in Table~\ref{tab:relative_ablations} covers the full
antisymmetric model, directed pairs, no spectrum conditioning, no pair loss,
and no spectrum loss across both candidate protocols and reranker seeds 42--44.
No ablation is uniformly best, so the table is evidence about the module's
behavior and sensitivity rather than a causal component estimate.

Difficulty analysis shows the largest relative-over-base improvements for
queries whose base rank is 2--5 or 6--20, while base-rank-1 and high-similarity
queries can decline. This pattern motivates the candidate-relation hypothesis
but does not establish that the relation branch is universally beneficial.

The difficulty strata above are repeated here as an audit of where the fixed
candidate pool leaves headroom: gains are largest for base ranks 2--5 and
6--20, while rank-1 queries can decline. Evaluation-only cutoff results and
forward-only cost measurements remain implementation references rather than
end-to-end deployment benchmarks.

\section{Reproducibility and Responsible-Evaluation Notes}

The formal matrix requests up to 20,000 of 194,119 available training queries,
uses alignment checkpoints 42--44, and repeats the reranker configurations with
seeds 42--44. The reproducibility index records the source commit, resolved
configuration, cache metadata, checkpoint and log hashes, and selected ranking
outputs. The anonymous supplement contains source inspection, locked
environment files, tests and a synthetic CPU smoke test, but not the real
spectra, candidate pools, caches, or trained checkpoints. The smoke test is
therefore not a reproduction of the scientific metrics; the local artifact
boundary is stated explicitly in Appendix~D.

\begin{sloppypar}
The local saved-embedding cosine control is a reimplemented JESTR-style scoring
control, not an official JESTR encoder or checkpoint reproduction; GLMR remains
reported-only. The paper does not claim SOTA, statistical significance,
complete alignment-level stability, cross-dataset generalization or end-to-end
deployment latency. These boundaries define the scope of the method result
rather than additional experimental claims.
\end{sloppypar}

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
