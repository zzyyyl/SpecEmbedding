\documentclass[runningheads]{llncs}

% ADMA 2026 submission checklist, verified against the official CFP on
% 2026-07-14:
% - Research-paper deadline: July 17, 2026 (AoE).
% - English, unpublished work, Springer LNAI/LNCS format, at most 15 pages.
%   The limit includes references, acknowledgements, and limitation claims.
% - Double-blind PDF and supplementary material. Remove author-identifying
%   text and metadata. The author list entered at submission is final.
% - Do not include acknowledgements or funding information in the review PDF.
% - If submitted, supplementary material is due three days after the paper
%   deadline and must be a single anonymous ZIP/PDF of at most 20 MB, unless an
%   anonymous repository is used.
% - No simultaneous archival submission and no new public preprint during
%   review.
% - Text produced with AI assistance must be disclosed. The disclosure below
%   covers every section of this draft.
%
% Before submission:
% 1. Replace all provisional cross-paper comparisons with results obtained
%    under one canonicalization/candidate protocol, or retain the explicit
%    "reported result" qualification.
% 2. Fill in the remaining top-K ablations and efficiency results.
% 3. Check the final PDF is <=15 pages and scrub identifying PDF metadata.

\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,fit,positioning}
\usepackage{hyperref}

\hypersetup{
  hidelinks,
  pdfauthor={},
  pdfsubject={},
  pdfkeywords={mass spectrometry, molecule retrieval, learning to rank}
}

\newcommand{\method}{SpecEmbedding-Rerank}
\newcommand{\base}{SpecMolAlign}
\newcommand{\zs}{\mathbf{z}_s}
\newcommand{\zm}{\mathbf{z}_m}
\newcommand{\candidates}{\mathcal{C}}

\title{Non-Generative Learning to Rerank for MS/MS-Based Molecule Retrieval}
\titlerunning{Non-Generative Molecular Reranking}

% Keep these fields empty for ADMA's double-blind review.
\author{}
\authorrunning{}
\institute{}

\begin{document}

\maketitle

\begin{abstract}
Retrieving molecular structures from tandem mass spectra is difficult because
many candidates share mass, formula, and substructure information. Joint
embedding methods rank candidates by spectrum--molecule similarity, whereas
generative retrieval can introduce an autoregressive molecular decoder. We
investigate an alternative design point: direct, non-generative learning to
rerank a retrieved molecular list. Our framework, \method{}, first aligns a peak-sequence
spectrum encoder and a molecular graph encoder. A second-stage residual ranker
then combines spectrum and candidate embeddings, explicit interaction features,
the base score, and the base rank under listwise supervision. We compare a
pointwise scorer without learned cross-candidate interaction with a
candidate-set Transformer across three
reranker initializations while keeping the first-stage checkpoint and caches
fixed. On the 17,556-query MassSpecGym test split, their mean $\pm$ sample-SD
Recall@1 values are
$67.44\pm0.74$ and $68.58\pm0.20$ for mass-conditioned retrieval, and
$73.97\pm0.59$ and $74.57\pm0.28$ for formula-conditioned retrieval, compared
with fixed-base values of 47.46 and 63.10. The validation-overlap-clean run gives the
Transformer a modest advantage, but the same paired-win pattern is absent under
an earlier alignment checkpoint. A fixed-alignment audit of five removal
settings gives none a full-model MRR advantage in all six paired comparisons;
removing the rank embedding improves Recall@1 and MRR in all six. Thus, the evidence
supports a supervised, non-generative second-stage reranking framework, while
checkpoint-robust self-attention and independent component benefits remain
unestablished. Neither variant generates molecules or forces the target into
the test top-40 list.
\keywords{Data mining for bioinformatics \and Tandem mass spectrometry \and
Molecule retrieval \and Learning to rank \and Multimodal representation
learning}
\end{abstract}

\section{Introduction}

MS/MS is widely used to identify small molecules in metabolomics,
natural-product discovery, and related biomedical applications.
An observed spectrum is only a partial, condition-dependent view of a molecular
structure: different molecules can yield similar fragments, while the same
molecule can produce different spectra across instruments, adducts, and
collision energies. Consequently, structure annotation is naturally a
large-scale data mining problem in which a query spectrum must retrieve and rank
a correct structure from a chemically constrained candidate pool.

MassSpecGym provides structure-disjoint data splits and standardized
mass- and formula-conditioned candidate pools for this problem
\cite{bushuiev2024massspecgym}. Recent methods mainly address the cross-modal
gap between spectra and molecules. JESTR learns a spectrum--molecule joint space
with contrastive multiview coding and ranks candidates directly by cosine
similarity \cite{kalia2025jestr,tian2020cmc}. GLMR instead retrieves contextual
candidates, generates a refined molecule with a conditional language model, and
reranks candidates by molecule--molecule similarity \cite{zhang2026glmr}.

Both approaches leave an important data mining question open. Fixed pairwise
cosine scores do not directly optimize the final candidate order, while
generative retrieval adds an autoregressive decoder and makes ranking depend on
the quality and validity of a generated structure. We ask whether hard
candidates can instead be separated directly in embedding space by a supervised
second-stage ranker, and whether explicit candidate interaction adds value beyond
a pointwise residual scorer.

Building on the peak-sequence spectrum architecture introduced by SpecEmbedding
\cite{xiong2025specembedding}, we construct \method{}, a two-stage retrieval and
reranking framework. The first stage trains that spectrum tower from scratch and
aligns it with a GINE molecular graph encoder. The second stage operates only on
the first-stage top-$K$ list. It constructs explicit spectrum--candidate
interaction features, injects the original score and rank as retrieval priors,
and predicts a residual correction to the base score. We instantiate this
framework with pointwise and candidate-set Transformer variants.

Our contributions are:

\begin{itemize}
  \item We formulate and evaluate direct, non-generative second-stage
  learning-to-rank for MS/MS-to-molecule retrieval, avoiding both fixed
  cosine-only ranking and autoregressive molecular decoding.
  \item We compare pointwise and self-attentive rankers with matched features,
  score heads, objectives, and optimization. On the same fixed caches, we then
  audit five removal settings of the full configuration.
  \item Using the MassSpecGym split and candidate files under the exact-SMILES
  identity rule implemented in our caches, both implementations improve the
  fixed base retriever by large margins across three reranker seeds.
  The overlap-clean comparison finds a modest candidate-set Transformer
  advantage, but comparison across alignment checkpoints does not establish a
  robust self-attention benefit. Moreover, within the fixed-alignment audit, no
  removal setting gives a uniform paired MRR advantage to the full model under
  both candidate protocols.
\end{itemize}

\paragraph{AI-assistance disclosure.}
OpenAI Codex assisted throughout all sections with drafting and language
revision, and during the project with code editing, experiment orchestration,
and consistency audits. Reported observations and numerical results came from
the stated software pipeline, not model generation. The authors reviewed all
AI-assisted text and code, verified the technical claims, citations, saved
artifacts, and reported results, and assume full responsibility for the
manuscript.

\section{Related Work}

\subsection{Annotation Paradigms}

Computational MS/MS annotation differs in retrieved objects and ranking
evidence. Spectral-library systems return measured spectra and depend on
library coverage and acquisition compatibility. Candidate-score correction
predates our work: MetFusion combines in-silico fragmentation with
spectral-library evidence \cite{gerlich2013metfusion}; MS2Query retains the top
2,000 MS2DeepScore matches and reranks them with a five-feature random forest,
with optional score-threshold filtering \cite{dejonge2023ms2query}; and
LC-MS2Struct combines an MS/MS scorer with chromatographic retention order to
jointly rank candidates from one LC analysis \cite{bach2022lcms2struct}. Our
setting instead ranks structures in query-specific MassSpecGym candidate pools
from frozen spectrum--molecule representations, without spectral-library-neighbor
or retention-time evidence \cite{bushuiev2024massspecgym}.

Within structure-database retrieval, CSI:FingerID and MIST predict molecular
fingerprints from spectra \cite{duhrkop2015csifingerid,goldman2023mist}.
CMSSP, JESTR, and MVP instead learn joint spectrum--molecule spaces by
contrastive alignment \cite{chen2024cmssp,kalia2025jestr,chen2026mvp}; MVP
aligns molecular graphs and fingerprints with individual and consensus spectra.
These methods ultimately order candidates using a predicted-fingerprint score
or cross-modal similarity. SpecEmbedding addresses spectrum--spectrum
representation learning \cite{xiong2025specembedding}. We reuse only its
peak-sequence architecture, train it from scratch against molecular graphs, and
study the complementary second-stage problem of learning the order within an
already retrieved hard-candidate list.

Simulation and generation use different intermediate representations.
MassFormer predicts tandem mass spectra from molecular graphs
\cite{young2024massformer}; MSNovelist generates structures from
MS/MS-derived fingerprints \cite{stravs2022msnovelist}. GLMR aligns spectra and
molecules, conditions a molecular decoder on retrieved candidates, and orders
those candidates by similarity to the generated molecule \cite{zhang2026glmr}.
Our method does not decode a structure: it remains a closed-set reranker and
cannot return a molecule outside the first-stage list, but directly optimizes
the scores of available candidates.

\subsection{Learning to Rank Candidate Sets}

Learning-to-rank objectives define supervision over preferences or complete
lists rather than treating similarity sorting as a fixed post-processing step.
RankNet learns pairwise preferences, whereas ListNet defines a probability
model over a ranked list \cite{burges2005ranknet,cao2007listnet}. Ranking
functions can also exploit relations among competing items. Set Transformer
provides attention-based blocks for set-structured inputs
\cite{lee2019settransformer}, and SetRank jointly models cross-document
interactions while preserving permutation equivariance \cite{pang2020setrank}.
We transfer these principles to spectrum--molecule retrieval using cached
cross-modal embeddings, explicit pair interactions, and first-stage score and
rank priors. Our contribution is not self-attention or listwise learning by
themselves, but a controlled non-generative second-stage formulation and
evaluation for MS/MS retrieval.

\subsection{Positioning of Our Method}

Table~\ref{tab:method_comparison} isolates the ranking mechanisms of the closest
MassSpecGym approaches. In light of the precedents above, our contribution is
not reranking itself, but a query-specific, non-generative second-stage
formulation over frozen cross-modal representations. Residual scoring and
candidate self-attention are evaluated design choices rather than assumed
sources of gain.

\begin{table}[t]
  \centering
  \caption{Mechanistic comparison with the closest MassSpecGym approaches.}
  \label{tab:method_comparison}
  \footnotesize
  \begin{tabular}{>{\raggedright\arraybackslash}p{1.1cm}
                  >{\raggedright\arraybackslash}p{2.1cm}
                  >{\raggedright\arraybackslash}p{2.1cm}
                  >{\raggedright\arraybackslash}p{1.5cm}
                  >{\raggedright\arraybackslash}p{2.5cm}}
    \toprule
    Model & Ranking signal & Candidate interaction & Generates & Training stages \\
    \midrule
    JESTR & Spectrum--candidate cosine & None at inference & No
      & InfoNCE plus formula-candidate regularizer \\
    GLMR & Generated--candidate cosine & Generator cross-attention & Yes
      & Dual-path InfoNCE plus generation \\
    Ours & Residual score from pair features and retrieval priors
      & Optional candidate self-attention & No
      & Cross-modal InfoNCE plus listwise/pairwise reranking \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Problem Formulation}

Let $s$ be an MS/MS spectrum, $m^+$ its correct molecule, and
$\mathcal{D}_s$ a query-specific mass- or formula-conditioned candidate
pool. A base retriever assigns

\begin{equation}
  b(s,m) = \operatorname{sim}(f_s(s), f_m(m))
\end{equation}

to every $m\in\mathcal{D}_s$ and returns the top-$K$ list
$\candidates_s=(m_1,\ldots,m_K)$. The reranker learns scores
$g(s,m_i,\candidates_s)$ such that $m^+$ is ordered as high as possible.
Because it cannot return a molecule outside $\candidates_s$, end-to-end
Recall@$k$ is upper-bounded by

\begin{equation}
  U_K = \frac{1}{N}\sum_{n=1}^{N}
  \mathbb{I}[m_n^+\in\candidates_{s_n}].
  \label{eq:upper_bound}
\end{equation}

Define the within-list top-$k$ accuracy conditional on first-stage coverage as

\begin{equation}
  A_{k\mid K} =
  \frac{\sum_{n=1}^{N}
  \mathbb{I}[m_n^+\in\candidates_{s_n},\,
  \operatorname{rank}_g(m_n^+)\le k]}
  {\sum_{n=1}^{N}\mathbb{I}[m_n^+\in\candidates_{s_n}]},
  \qquad
  \mathrm{Recall@}k=U_K A_{k\mid K}\le U_K .
  \label{eq:coverage_decomposition}
\end{equation}

A reranker applied to a fixed cache can therefore change only
$A_{k\mid K}$, not $U_K$. We report metrics over all queries, assigning zero
credit when the positive is absent rather than conditioning the primary metric
on successful first-stage retrieval.

\section{Method}

\subsection{Overview}

The framework has three steps: cross-modal alignment, top-$K$ retrieval, and
supervised residual reranking. Spectrum and molecule encoders are frozen while
the reranker is trained, so their embeddings can be computed once and reused.

\begin{figure}[t]
  \centering
  \input{figures/method_overview.tex}
  \methodoverview{0}
  \caption{Overview of \method{}. Panel 1 aligns the spectrum Transformer and
  molecular GINE. Panel 2 uses their frozen, cached embeddings to produce the
  top-40 list. Panel 3 applies shared pair features with either pointwise
  scoring or candidate-set self-attention and adds a residual to the base
  score. Dashed arrows denote training-only operations. Positives are never
  forced into the truncated top-40 list at validation/test, and no molecule is
  generated.}
  \label{fig:overview}
\end{figure}

\subsection{Cross-Modal Base Retriever}

The spectrum encoder follows SpecEmbedding's peak-sequence architecture
\cite{xiong2025specembedding}: it processes peak $m/z$ values and intensities
with sinusoidal $m/z$ features and a Transformer. Each spectrum is represented
by at most 100 tokens: a precursor-$m/z$ token with intensity 2, followed by up
to 99 fragment peaks selected by intensity. Selected fragments retain their
source-array order, and their intensities are normalized by the largest selected
fragment intensity. Only these $m/z$--intensity tokens enter the spectrum
encoder; adduct, instrument, and collision-energy metadata are not explicit
inputs. We initialize this tower from scratch and train it against molecules
rather than spectra. The molecular encoder is a GINE network over atom and bond
features \cite{hu2020pretraininggnn}. Mean-pooled graph features are augmented
with explicit graph-size features. Two projection heads map the encoders to a common $d$-dimensional
space:

\begin{equation}
  \zs = \frac{p_s(f_s(s))}{\lVert p_s(f_s(s))\rVert_2},
  \qquad
  \zm = \frac{p_m(f_m(m))}{\lVert p_m(f_m(m))\rVert_2}.
\end{equation}

We use a bidirectional cross-modal contrastive loss that permits multiple
positives. Spectra and molecules sharing a molecular label form positives,
which avoids treating replicate spectra as false negatives. For a minibatch of
size $B$, let $P(i)$ be the indices sharing the label of item $i$. With a
learned inverse-temperature scale $a$, the base logits are

\begin{equation}
  \ell_{ij}=a\,\mathbf{z}_{s_i}^{\top}\mathbf{z}_{m_j}.
\end{equation}

The spectrum-to-molecule term is

\begin{equation}
  \mathcal{L}_{s\rightarrow m}
  =-\frac{1}{B}\sum_{i=1}^{B}\frac{1}{|P(i)|}
  \sum_{j\in P(i)}
  \log\frac{\exp(\ell_{ij})}{\sum_{k=1}^{B}\exp(\ell_{ik})},
\end{equation}

and the molecule-to-spectrum term applies the same expression to
$\ell^\top$. The alignment loss is their mean. The implementation parameterizes
$a=\exp(u)$ and caps it at 100. At inference, the normalized dot product
$b_i=\zs^\top\mathbf{z}_{m_i}$ is the base score.

\subsection{Candidate Construction and Training Protocol}

MassSpecGym supplies one candidate pool per query. The base retriever scores the
pool and keeps its top $K=40$ molecules. If a training target is absent from the
retrieved top-40, it replaces the last molecule while its full-pool base rank is cached.
That rank is one plus the number of strictly higher-scoring candidates; ties
with the target do not count as higher. The rank embedding receives
$\bar r_i=\min\{\max(r_i,0),512\}$, with index 0 reserved for padding. Training
also shuffles candidate order while moving each candidate's embedding, score,
and rank together to guard against cache-order artifacts.

Scored pools are exact-SMILES-deduplicated unions of the labeled target and
supplied candidates. A static audit found the target already present in all
32,010 frozen pools, so no pool was enlarged. Post-retrieval positive forcing is
training-only; validation and test perform no top-40 replacement. A target absent
from the retrieved top-40 remains absent and lowers the upper bound and all
end-to-end metrics. No
test record, target, or label is used for optimization or checkpoint selection.

\subsection{Residual Reranker Variants}

For candidate $m_i$ with base rank $r_i$, we construct

\begin{equation}
  \mathbf{x}_i =
  [\zs,\ \mathbf{z}_{m_i},\ \zs\odot\mathbf{z}_{m_i},\
  |\zs-\mathbf{z}_{m_i}|,\ b_i,\ \mathbf{e}(\bar r_i)],
  \label{eq:features}
\end{equation}

where $\mathbf{e}(\bar r_i)$ is a learned rank embedding. The direct embeddings
retain modality-specific evidence, while product and absolute-difference terms
make symmetric matching cues explicit. An MLP projects each vector:

\begin{equation}
  \mathbf{h}_i=\operatorname{MLP}(\mathbf{x}_i).
\end{equation}

Both variants receive only the frozen spectrum embedding, frozen candidate
embeddings, cosine base scores, clipped base-rank embeddings, and the candidate
padding mask. Raw peaks, SMILES tokens, precursor mass or formula, and
acquisition metadata are not passed directly to the reranker. Mass and formula
constraints affect candidate-pool construction only; no explicit
candidate--candidate structural-similarity feature is provided.

The candidate-set variant uses a Transformer encoder without candidate
positional encodings to exchange information across candidates
\cite{vaswani2017attention}:

\begin{equation}
  [\tilde{\mathbf{h}}_1,\ldots,\tilde{\mathbf{h}}_K]
  =\operatorname{Transformer}
  ([\mathbf{h}_1,\ldots,\mathbf{h}_K]).
\end{equation}

Padding masks support variable-sized lists. At deterministic inference, jointly
permuting the candidates and their attached embeddings, scores, and ranks
permutes the output scores in the same way; the base rank is a
candidate-attached retrieval prior, not a tensor-slot position encoding
\cite{lee2019settransformer,pang2020setrank}.
The pointwise control sets
$\tilde{\mathbf{h}}_i=\mathbf{h}_i$, retaining the same features, score head,
residual path, and training losses while removing learned cross-candidate
interaction after the retrieval priors have been constructed. It is not
parameter- or capacity-matched because it removes the two Transformer layers:
the pointwise and candidate-set variants contain exactly 1,478,690 and
7,783,458 trainable reranker parameters, respectively. A score head predicts a
correction $\Delta_i$, and the final score is residual:

\begin{equation}
  q_i=\alpha b_i+\Delta_i,
  \label{eq:residual}
\end{equation}

where $\alpha$ is initialized to 1.0 and learned without a sign constraint. The
residual path provides a direct shortcut from the base score; it does not
guarantee preservation of the base order. Moreover, because $b_i$ also enters
$\mathbf{x}_i$ and $\Delta_i$ can learn additional score-dependent effects,
$\alpha$ alone is not a mechanistically interpretable measure of reliance on
the base retriever.

\subsection{Learning Objective}

For the labeled queries $\mathcal{B}_{L}$ in a minibatch, let $V_n$ be the valid
candidate set and $y_n$ the positive index. The primary objective averages
listwise cross-entropy over labeled queries:

\begin{equation}
  \mathcal{L}_{\mathrm{list}}
  =-\frac{1}{|\mathcal{B}_{L}|}
  \sum_{n\in\mathcal{B}_{L}}
  \log\frac{\exp(q_{ny_n})}{\sum_{i\in V_n}\exp(q_{ni})}.
\end{equation}

We also contrast each positive with every other valid candidate retrieved for
that query. The implementation averages over all valid positive--negative pairs
in the minibatch:

\begin{equation}
  \mathcal{L}_{\mathrm{pair}}
  =\frac{
  \sum_{n\in\mathcal{B}_{L}}\sum_{i\in V_n\setminus\{y_n\}}
  \log\!\left(1+\exp(q_{ni}-q_{ny_n}+\gamma)\right)}
  {\sum_{n\in\mathcal{B}_{L}}\left(|V_n|-1\right)}.
\end{equation}

The complete objective is

\begin{equation}
  \mathcal{L}
  =\mathcal{L}_{\mathrm{list}}
  +\lambda\mathcal{L}_{\mathrm{pair}}.
\end{equation}

The listwise term is a top-one likelihood: it separates the positive from the
valid list but does not prescribe a total order among negatives. Both terms are
invariant to adding the same query-specific constant to every $q_i$. Thus, the
raw outputs are relative scores, not probabilities; probability calibration is
neither performed nor evaluated.

For $L$ Transformer layers and hidden width $h$, the set-aware variant adds
$O(L(Kh^2+K^2h))$ computation and $O(LK^2)$ attention memory over the pointwise
scorer. Neither variant decodes molecules token by token. An empirical latency
comparison with GLMR is not reported, so we make no measured efficiency claim.

\section{Experiments}

\subsection{Dataset and Candidate Pools}

We use the official MassSpecGym split \cite{bushuiev2024massspecgym}: 194,119
training spectra, 19,429 validation spectra, and 17,556 test spectra. These
counts total 231,104 spectra; the benchmark reports approximately 29,000 unique
molecular structures. Its structure-disjoint split groups similar molecules and
makes test structures substantially different from training structures.

An exact audit of spectrum-record signatures (peak arrays, precursor $m/z$,
adduct, instrument, and collision energy) found no target-molecule duplicates
across splits, but six validation and three test signatures exactly matched
training signatures. In all nine matched pairs, the target molecules differ
and each evaluation query is marked \texttt{simulation\_challenge=true}. We
report these cases as cross-split spectrum-record overlap. In the canonical
pipeline reported below, all validation-dependent model selection excludes the
six affected validation spectra, leaving 19,423 validation queries for the
rerankers; alignment validation keys decrease from 3,386 to 3,384 molecular
entries. We retain and evaluate all 17,556 official test queries.

MassSpecGym supplies mass- and formula-conditioned candidate settings. The mass
pool uses a precursor-mass tolerance; the formula pool assumes a known formula
and is not unrestricted annotation. Frozen pools contain at most 256 candidates.
Before truncation, their test-query means are 253.88 and 165.75, respectively;
1 mass and 3,159 formula pools contain fewer than 40 candidates. After top-40
truncation, the means are 40.00 and 35.73.

Our cache and evaluator assign one positive by exact target-SMILES equality. In
official code commit \texttt{4f501b3},\footnote{MassSpecGym
\href{https://github.com/pluskal-lab/MassSpecGym/blob/4f501b3207c9f466bc745a0353f61dbfaf150d32/massspecgym/data/datasets.py}{reference loader}
and \href{https://github.com/pluskal-lab/MassSpecGym/blob/4f501b3207c9f466bc745a0353f61dbfaf150d32/massspecgym/data/transforms.py}{label transform},
accessed August 1, 2026.} the reference loader instead defaults to
two-dimensional InChIKey equivalence and may label multiple positives. Our
Recall and MRR therefore use a local exact-SMILES protocol on the supplied
candidate files; the effect of the identity-rule difference is unquantified.

\subsection{Implementation Details}

The aligned model outputs 512-dimensional embeddings. Its spectrum Transformer
has four layers, 16 heads, and width 512; the molecular GINE has four layers,
hidden width 128, and dropout 0.2. No published SpecEmbedding checkpoint is
loaded, so the stage that would freeze a pretrained spectrum tower is skipped;
both towers are optimized end to end on the MassSpecGym training split.
Alignment uses AdamW with batch size 128, learning rate and weight decay
$10^{-4}$, cosine scheduling, gradient-norm clipping at 1, at most 100 epochs, and
patience 5. Each epoch makes ten draws per molecular label, randomly selecting
an associated spectrum. Spectrum and graph augmentation are independently
enabled with probability 0.5. The former removes a floor-rounded 20\% of
fragment peaks below normalized intensity 0.3 and jitters intensities by
$\pm15\%$; the latter applies 10\% node and 10\% directed edge-entry dropout.
Validation disables augmentation but retains replicate-spectrum sampling under
the fixed random stream. The canonical seed-42 run applies the validation
exclusion above, selects epoch 16, and stops at epoch 21.

The reranker has hidden width 512, a 32-dimensional rank embedding, and dropout
0.1. The set-aware variant has two Transformer layers with eight attention
heads; the pointwise control removes both layers. We use AdamW with learning
rate $10^{-4}$, batch size 16, at most 30 epochs, and early stopping on
validation MRR with patience 5. The pairwise weight and margin are
$\lambda=0.2$ and $\gamma=0.1$. With one fixed alignment checkpoint and fixed
caches from the overlap-clean run, each reranker variant is trained with seeds
42, 43, and 44. This measures reranker variation, not end-to-end alignment
uncertainty. The component audit uses these same caches and seeds. Its five
removals respectively disable only the residual shortcut while retaining the
base score as MLP input; jointly disable the base-score feature and shortcut;
zero the rank embedding while preserving input width; remove product and
absolute-difference features while retaining self-attention; or set $\lambda=0$
for listwise-only training. All models are
selected without test-set access and evaluated on one NVIDIA RTX 4090.

Before training-only positive forcing, top-40 base recall is 93.78\% for mass
and 90.42\% for formula on the training split. Training lists are then completed
to 100\% labeled coverage as described above. No target is forced into the test
top-40 lists, whose corresponding upper bounds are 83.88\% and 89.26\%.

\subsection{Baselines and Metrics}

The controlled baselines are the same aligned retriever without the second-stage
ranker and a pointwise reranker that removes candidate self-attention while
retaining all other inputs and objectives, but not the same parameter count. We
also show JESTR and GLMR values
from Table 1 of the GLMR paper
\cite{zhang2026glmr}. These external numbers provide context but are not a
controlled ablation: JESTR and GLMR were not independently reproduced in our
codebase, and their identity and canonicalization rules may differ from our
exact-SMILES single-positive protocol.

We report Recall@1, Recall@5, Recall@20, and mean reciprocal rank (MRR) over all
test queries. For every query, including those whose target is absent from the
retrieved top-40, let $\hat m_n^{(1)}$ be the top-ranked candidate. We report

\begin{equation}
  \operatorname{MCES@1}
  =\frac{1}{N}\sum_{n=1}^{N}
  \tilde d_{15}\!\left(\hat m_n^{(1)},m_n^+\right),
  \qquad N=17{,}556.
\end{equation}

Here $\tilde d_{15}$ is the output of \texttt{myopic-MCES} with threshold
15 and the stronger-bound option enabled \cite{kretschmer2025coverage}. It is
exact for sufficiently close pairs; for
distant pairs it may return the threshold or a computed lower bound rather than
the unconstrained exact MCES distance. Thus, the reported quantity is
thresholded myopic MCES, and lower is better. Exact SMILES-string matches are
assigned zero. The canonical runs selected \texttt{PULP\_CBC\_CMD}, and no MCES
worker failures were logged. Recall and MRR uncertainty is computed over three
reranker seeds with a fixed base checkpoint. MCES@1 is computed for the
overlap-clean base and representative seed-42 Transformer checkpoints; these
single-checkpoint values do not include seed-level uncertainty.

\subsection{Main Results}

\begin{table}[t]
  \centering
  \caption{Representative seed-42 MassSpecGym results and cross-paper context.
  Recall and MRR are percentages; MCES@1 is the thresholded myopic-MCES output
  (lower is better). JESTR
  and GLMR values are reported by Zhang et al.\ \cite{zhang2026glmr}; our rows
  use the local exact-SMILES pipeline and MassSpecGym-supplied candidate files.
  The external rows are
  used only as cross-paper context. Local MCES@1 values use the overlap-clean
  base and representative seed-42 Transformer checkpoints; external MCES
  implementations and solver settings may differ.}
  \label{tab:main_results}
  \small
  \begin{tabular}{llrrrrr}
    \toprule
    Pool & Method & R@1 & R@5 & R@20 & MRR & MCES@1 \\
    \midrule
    \multirow{4}{*}{Mass}
      & JESTR (reported) & 17.62 & 40.36 & 64.76 & 29.12 & 15.82 \\
      & GLMR (reported) & 64.17 & 72.96 & 78.78 & 67.82 & 11.14 \\
      & \base{} & 47.46 & 63.51 & 77.76 & 55.03 & 15.37 \\
      & Candidate-set Transformer & 68.74 & 77.77 & 82.18 & 72.87 & 7.71 \\
    \midrule
    \multirow{4}{*}{Formula}
      & JESTR (reported) & 11.77 & 33.26 & 61.01 & 22.83 & 11.73 \\
      & GLMR (reported) & 68.48 & 78.09 & 84.22 & 72.47 & 5.05 \\
      & \base{} & 63.10 & 75.79 & 84.93 & 68.86 & 5.44 \\
      & Candidate-set Transformer & 74.75 & 82.25 & 86.89 & 78.22 & 3.09 \\
    \bottomrule
  \end{tabular}
\end{table}

In the representative seed-42 run, the candidate-set Transformer adds 21.28 percentage
points in mass-conditioned Recall@1 and 11.65 points in formula-conditioned
Recall@1. The corresponding MRR gains are 17.84 and 9.36 points. Improvements
at Recall@20 are smaller because the first-stage top-40 upper bound is already
the dominant constraint. Relative to the corresponding fixed overlap-clean base, the same
seed-42 Transformer reduces MCES@1 from 15.3681 to 7.7057 for mass candidates
($49.86\%$) and from 5.4389 to 3.0913 for formula candidates ($43.16\%$).

Relative to the externally reported GLMR values, the representative Recall@1 is
higher by 4.57 points for mass candidates and 6.27 points for formula
candidates; its MCES@1 values are also numerically lower. These cross-paper
figures are encouraging but do not by themselves prove that listwise reranking
is superior to generation: our base retriever already differs substantially
from the GLMR pre-retriever. A matched causal comparison would require rerunning
the baselines or applying the reranker to identical frozen embeddings and
candidate lists; we therefore treat both the Recall and MCES comparisons as
contextual rather than causal.

\subsection{Controlled Reranker Ablation}

\begin{table}[t]
  \centering
  \caption{Controlled comparison on fixed alignment-seed-42 caches. The base
  row is fixed; learned rows report mean $\pm$ sample standard deviation over
  reranker seeds 42--44. Recall@1 and MRR are percentages. Removal rows are
  separately trained audits of the predefined full configuration, not post-hoc
  model-selection candidates.}
  \label{tab:reranker_ablation}
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{0.95}
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{3.5cm}cccc@{}}
    \toprule
    & \multicolumn{2}{c}{Mass} & \multicolumn{2}{c}{Formula} \\
    \cmidrule(lr){2-3}\cmidrule(lr){4-5}
    Method & R@1 & MRR & R@1 & MRR \\
    \midrule
    Base & 47.46 & 55.03 & 63.10 & 68.86 \\
    Pointwise
      & $67.44\pm0.74$ & $71.79\pm0.66$
      & $73.97\pm0.59$ & $77.64\pm0.49$ \\
    Candidate-set Transformer (full)
      & $68.58\pm0.20$ & $72.60\pm0.25$
      & $74.57\pm0.28$ & $78.02\pm0.26$ \\
    \midrule
    No residual shortcut
      & $68.16\pm0.06$ & $72.33\pm0.06$
      & $75.01\pm0.54$ & $78.38\pm0.42$ \\
    No base-score input/shortcut
      & $68.07\pm0.20$ & $72.28\pm0.18$
      & $74.39\pm0.55$ & $77.93\pm0.38$ \\
    No rank embedding
      & $70.12\pm0.30$ & $73.75\pm0.28$
      & $76.62\pm0.69$ & $79.38\pm0.58$ \\
    Product/absolute-difference removed
      & $68.10\pm0.85$ & $72.29\pm0.66$
      & $75.76\pm0.55$ & $78.90\pm0.38$ \\
    Listwise only ($\lambda=0$)
      & $68.91\pm0.76$ & $72.86\pm0.46$
      & $74.24\pm0.25$ & $77.71\pm0.25$ \\
    \bottomrule
  \end{tabular}
\end{table}

Both learned rankers substantially outperform the fixed base model. On the
overlap-clean checkpoint, the candidate-set Transformer exceeds the pointwise model by
$1.14$ Recall@1 points for mass candidates and $0.60$ points for formula
candidates on average, and it wins all six paired seed--candidate comparisons.
The corresponding MRR differences are $+0.81$ and $+0.38$ points. This is a
modest, consistent architectural difference within the clean run. Because the
Transformer also has 6.30M more trainable parameters, the comparison does not
isolate candidate interaction from increased capacity. However, the earlier
alignment checkpoint gave Recall@1 differences of $-0.13$ and $+0.21$ points
and only three wins out of six. The evidence therefore supports supervised
second-stage reranking and suggests a possible candidate-interaction benefit, but
does not establish a checkpoint-robust independent contribution from
self-attention.

The separately trained removal rows audit the full configuration on the same
fixed alignment. No removal setting gives the full model higher MRR in all six
paired comparisons. Removing the rank embedding improves both reported metrics in all
six; its mean Recall@1/MRR gains are 1.54/1.15 mass points and 2.05/1.36 formula
points. The residual shortcut, product/absolute-difference features, and
pairwise loss have candidate-dependent directions. The full model's mean MRR
lead over the joint base-score-pathway removal is only 0.32/0.08 points, with
mixed seed signs. These are descriptive comparisons, not significance tests;
the predefined full configuration remains the reporting reference.

On the earlier checkpoint, a sensitivity check excluded the three affected test
queries, leaving 17,553. Across both candidate settings and rerankers, mean
Recall@1 decreased by 0.0045--0.0055 points (maximum seed-level change: 0.0056),
so the aggregate effect is negligible; canonical tables retain all test queries.

The clean and earlier pipelines select the same alignment epochs (best 16,
stop 21), but reranker best/stop epochs differ in 9 of 12 configurations.
Because alignment was retrained and its test metrics changed, this measures
pipeline sensitivity rather than the causal effect of six validation queries.

We do not report candidate-order or full top-$K$ ablations. We also do not
report peak memory, cache construction time, reranker latency, or end-to-end
latency.

\section{Discussion}

Equations~\ref{eq:upper_bound}--\ref{eq:coverage_decomposition} separate
first-stage coverage from within-list ordering. For $k=1$ and $K=40$, the
defined conditional quantity $A_{1\mid40}$ equals $\mathrm{R@1}/U_{40}$.
Using the displayed three-seed means, it increases from 56.58\% for the base
retriever to 80.40\% for the pointwise model and 81.76\% for the candidate-set
Transformer under mass conditioning. The corresponding formula-conditioned
rates are 70.69\%, 82.87\%, and 83.54\%.

Another view is the fraction of attainable top-1 headroom recovered,

\begin{equation}
  H=\frac{\mathrm{R@1}_{\mathrm{rerank}}-\mathrm{R@1}_{\mathrm{base}}}
  {U_{40}-\mathrm{R@1}_{\mathrm{base}}}.
\end{equation}

The pointwise and candidate-set models recover 54.86\% and 57.99\% of this
headroom for mass candidates, and 41.55\% and 43.85\% for formula candidates.
The pointwise gain is also 94.60\% and 94.77\% of the total candidate-set gain
over the base model in the two settings. Thus, most of the observed improvement
is already achieved by the supervised pointwise second-stage ranker as
configured; the candidate-set variant adds only a modest increment.
At Recall@20, the candidate-set three-seed means of 82.20\% and 86.77\% are
only 1.68 and 2.49 points below $U_{40}$; first-stage coverage is therefore the
dominant remaining limitation for that metric under this top-40 protocol. The removals likewise support only
a framework-level interpretation: none gives the full model uniform paired MRR
support, whereas rank removal improves both metrics in every pair. The
predefined full configuration remains the reporting reference; these algebraic
and removal comparisons are not causal estimates of individual components.

\section{Limitations and Responsible Evaluation}

Three-seed sample standard deviations and paired-win counts are descriptive,
not confidence intervals or significance tests. The main table fixes one
alignment checkpoint; an earlier checkpoint only provides a pipeline-sensitivity comparison. Thus,
the component audit establishes neither cross-alignment stability nor causal
component effects: no-base-score jointly removes its MLP input and shortcut,
product/absolute-difference removal retains self-attention, and the rank-free
variant was neither tested across alignments nor selected post hoc. The
candidate-set model also has 5.26 times the pointwise model's trainable
parameters (6,304,768 more; $+426\%$), confounding interaction
with capacity. External JESTR and GLMR rows were not reproduced under identical
software and candidate handling; we claim neither definitive state of the art
nor an independent component benefit.

Training-only positive replacement changes the list distribution without
addressing $1-U_K$. Because frozen full pools contain the exact target,
pool-level evaluation is closed-set even when top-40 retrieval misses it. The
impact of exact-SMILES rather than reference two-dimensional InChIKey identity
is unquantified, and the exact-record audit cannot exclude near duplicates or
correlated provenance. Model selection excludes the six audited
training--validation overlaps; all test queries remain. The manifest pins source
and artifact checksums, but not exact PyTorch, RDKit, PyG, and
\texttt{myopic-MCES} versions. A 2026 audit preprint reports further MassSpecGym
leakage, shortcut, implementation, and metric risks and releases v1.5
\cite{liu2026massspecgymwild}; our scores are artifact- and protocol-specific,
not proven v1.5-equivalent.

Alignment sampling approximately balances molecular labels, whereas metrics
count spectra, giving replicate-rich molecules more weight. Candidate-pool
Recall measures prioritization, not confirmed identification; confidence
frameworks and work on spectral-library absences distinguish these notions
\cite{schymanski2014confidence,hoffmann2022cosmic}. Conformal retrieval can
produce spectrum-specific sets with marginal coverage under exchangeability
and target-in-pool assumptions \cite{rakhshaninejad2026conformal}, but such
guarantees do not automatically cover a target missing from the retrieved
top-40. We perform neither probability calibration nor abstention; orthogonal
experimental evidence remains necessary.

Finally, formula conditioning presumes a known formula. The spectrum tower uses
selected peaks and precursor $m/z$, but no explicit adduct, instrument, or
collision-energy inputs. Robustness to acquisition shift, other retrievers and
embeddings, identity rules, pool construction, larger lists, other benchmarks,
and external libraries is untested. The closed-set method cannot propose
structures outside the supplied pool despite avoiding autoregressive
generation-validity failures.

\section{Conclusion}

We presented \method{} for supervised, non-generative second-stage reranking of
MS/MS candidates. It improves the fixed seed-42 retriever in all
three reranker initializations under both candidate protocols. The candidate-set
Transformer has a modest overlap-clean advantage over the pointwise scorer, but
not the same pattern under the earlier alignment checkpoint. None of five
audited removal settings gives the full model uniform paired MRR support, while rank
removal improves both reported metrics in all six fixed-alignment pairs. We keep
the predefined full configuration as the reference: the strongest supported
conclusion concerns the reranking framework, not any independent component
benefit. Matched external baselines and measured efficiency remain outside the
evidence.

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
