# A06：固定 Morgan 指纹与轻量分子塔

- 状态：原队列因发现结束检查错误，于2026-09-10 12:24:34在首次GPU阶段之前主动中断；
  旧工件完整保留。最小修复版r2全量预检通过、运行配置与原A06逐项相同，12:36:24单次
  派发独立tmux，12:39:36完成CPU导入并等待基线验证GPU，原PID3563758及实际输入/环境
  已独立核验；尚无本轮训练或检索结果。
- 顺序：A05已完成24轮训练及独立审计，全部候选未通过改善门槛；A06确认继承A04的训练设置。
  A04四种完整验证输入对照均已完成，原队列退出0，独立完整排名复核通过，见优化索引D17；
  因而原依赖门槛已满足。对照不改变A06的模型假设或parent选择规则。
- 唯一主要模型假设：把GINE分子塔替换为固定Morgan位向量上的小型MLP，使分子结构特征
  更直接参与跨模态检索，同时减少每个自然负例重复构图与消息传递的计算开销。
  这是待验证的效果与成本假设，没有正式检索成绩或实测整轮提速结论。

## 模型与固定条件

Morgan radius2、2048位、无手性；输入保持D11完整源分子索引及二维标签，不因指纹相等合并
候选。分子塔为2048→512→128，隐藏层使用LayerNorm、ReLU、dropout0.2；其后沿用原
分子投影头。质谱塔、两侧投影维度及余弦打分保持原方案，SpecEmbedding预训练结构不改。

从随机初始化训练；seed42、完整194,119条训练谱图、既定验证排除和19,423条有效验证
query保持不变。batch、学习率、温度、优化器、早停、质量分组及候选损失继承最终确认
parent，不在本轮同时增大负例数、改学习率或增加指纹BCE辅助项。A05完成审计后未晋升，
唯一parent仍为A04：batch128、学习率1e-4、原温度/质量分组及候选监督；选择记录见阶段计划。

输入改为固定指纹后，分子图的节点/边随机扰动不再适用，配置必须显式设为0；不能静默忽略
非零图增强配置。谱图增强仍走原公共实现；随机调用数量变化可能改变后续增强实现，不能
宣称与GINE方案每次随机增强完全一致。固定指纹与分子表示替换作为同一模型改动整体评价。

已完成的A04/A05均包含候选监督，沿用最多16个不同二维自然负例和原损失权重1；
负例经当前分子MLP参与反向传播，同时保留原双向in-batch监督。只运行最后选中的一个方案。
后续更大负例预算的完整CPU输入诊断见[优化索引D16](analysis/massspecgym_optimization.md)；
它不改变本次16负例设置，GPU成本与检索收益仍待后续实验判断。

## 组件与证据

- `SpecEmbedding/models_fingerprint.py`：沿用原质谱编码器和投影/余弦路径，新增显式配置的分子MLP。
- `SpecEmbedding/data/datasets_fingerprint.py`：完整谱图及自然候选读取；复用原query映射和
  不同二维负例采样，读取已审计指纹，每次按需解包，不在worker中构图。
- `SpecEmbedding/utils/fingerprint_validation.py`：复用原完整检索与排名路径，每轮重新编码
  全部分子；快照绑定指纹回执，旧审计默认拒绝未声明的指纹输入。
- 参数集中于`params.yaml`的`fingerprint_encoder`，本轮正式配置已显式启用该模型类型。

完整输入的构建、独立位值审计与公共读取器复核见优化索引D11；D05记录指纹碰撞的限制。
这些证明输入可复核，不证明新模型更好。组件检查和后续真实输入预检集中于D12。
全仓464通过、1跳过、1个相同既有路径审计失败；静态检查通过。测试包含真实小模型两轮
训练/验证与权重重载、两塔及负例梯度、真实多进程读取、权重改变后重新编码、碰撞标签、
无正例query分母及计数相同但重复query的拒绝；这些均为合成CPU检查，不是正式实验。

## 正式接入、启动与验收

1. 已将模型类型、完整构造配置、训练/验证指纹来源绑定到正式入口、checkpoint选优记录和
   完成审计；正式模式显式启用fingerprint及三个固定输入路径，拒绝非零图增强和位宽不符。
2. 已分离parent与候选的构造配置：按parent自己的selection构建并严格重载权重，独立绑定
   模型配置与SHA，同时核对共同数据、tokenizer、排除项和完整分母；GINE默认路径保持原初始化顺序。
   完整审计逐轮检查固定输入回执、全query排名和自然负例采样，并严格构造选中模型重载权重。
   合成CPU检查包含两类parent的真实完整验证、不同投影维度、正式训练两轮（候选监督开/关）、
   全负例重放，以及混用/篡改模型和输入回执的拒绝；不能作为正式GPU结果。
3. 真实全量CPU正例映射、自然负例、实际query顺序和计数预检已于00:33完成，见D12；仍需
   正式候选模型完整编码及GPU资源记录。CPU输入测量不称为GPU训练速度。
4. `d6226ef`干净源码的完整A04条件CPU dry-run已通过，继承batch128、block32、学习率1e-4
   及候选CE16/权重1；复核A04完成审计的126项工件和预检30项输入指纹，完整覆盖194,119条
   训练query、19,423条有效验证query，以及5,711,753/827,600个训练/验证指纹输入。
   GINE比较基线仍使用自己的构造配置及固定图缓存，候选模型训练/验证显式使用指纹缓存；
   质谱塔与谱图增强不变，只有分子图扰动显式置零。上述条件预检时未创建模型运行目录；
   此前A02条件准备保留历史追溯。现已确认A04为唯一parent，并完成整组输入对照和最终
   完整预检，按原GPU门槛单次派发至新运行目录。GPU OOM或完整性错误保留工件并排查，
   不缩减正式训练数据；实际GPU阶段仍须核验设备、输入和逐轮完整计数。
5. 按既定逐项Top-k/MRR规则审计完整结果并保留或舍弃；失败时在attempts.md加删除线。

## 最终配置绑定入口准备

2026-09-10 08:24已归档独立`finalize.py`及检查，路径与SHA见阶段计划第10节；32项CPU
合成测试通过，覆盖四个原进程的结束/PID复用、失败状态、完整Pareto复核、基线资格、
参数完整继承和只执行dry-run。脚本不选择parent，也不启动模型：它要求A05全轮审计及
严格权重读回完成、A04输入对照完成且相应原进程退出，再读取显式的验证证据选择记录。
记录须绑定审计/对照SHA、完整Pareto轮次及选择理由；存在未处理的晋升候选时拒绝继续。

真实A04配置经新构造函数仍与既有条件配置完全一致；A05仅用当前runtime检查继承batch256
的条件分支，未将其视为已选中checkpoint或提前晋升。自然负例16/权重1、学习率和其它
训练参数随最终parent整体继承，仅按既定方案改分子表示并显式关闭图扰动。
真实`--check-only`返回等待；指定尚不存在的决定文件也先以退出码2拒绝，两个调用均未
创建A06最终准备目录或模型运行目录。7份归档文件及5项输入SHA独立读回通过。
上述为入口准备时的状态；A05完成审计后已确认保留A04。随后真实`--check-only`再次返回
`dependencies_pending`：run/audit均complete、controls仍running，A05原入口退出0、训练及
审计进程已结束；未创建最终准备或模型目录。完整最终预检和GPU派发仍待输入对照完成。

## 最终绑定与实际派发

以上等待记录为历史状态。现已完成四种输入对照、原进程退出码及独立保存排名复核，
显式选择A04后，固定`d6226ef`的最终预检于11:12:42通过；7项最终准备文件SHA读回一致。
实际配置为batch128、质量block32、学习率1e-4、候选CE16/权重1；训练/有效验证计数为
194,119/19,423，训练/验证指纹索引覆盖5,711,753/827,600个分子。没有改预训练结构或学习率。

11:16:29启动独立tmux中的唯一入口，11:17:39进入CPU数据导入；派发回执只证明tmux接受，
随后已另核对原PID的真实命令和新`status.json`。运行根、socket、来源/配置/决策指纹集中在
阶段计划第10节。全流程完成、GPU速度及模型是否晋升仍待实际训练、验证和完成审计。

11:19:42完成数据与验证索引导入，原始split计数仍为194,119/19,429/17,556，有效验证19,423；
实际输入清单与最终preflight逐字节相同，runtime为fingerprint，五项导入文件SHA一致。
11:25:58独立读取原PID/starttime、配置环境和外部存储路径通过；入口作为CPU编排进程保持
CUDA不可见，GPU子进程将在各阶段通过原门槛后映射显式cuda:0，届时另核验实际设备。
当前仅`baseline_validation/waiting_gpu`，不把导入或派发写成已经开始训练。

## 结束检查修复与r2恢复

以上首次派发记录为历史状态。检查完成审计的衔接时发现，原入口在指纹训练结束后错误地
把其验证图缓存与GINE比较基线的图缓存做相等检查；指纹训练合法地使用指纹缓存，因此
会被误判为失败。合成阶段执行测试复现了正常指纹输入被拒绝、错误图缓存反而被接受；
修复后GINE/指纹的正常和错误输入四种情况均符合预期，相关43项检查通过。

只在原A06源码上增加该结束检查修复及测试，新固定版本为`65eecfc`，开发分支修复提交为
`2e9ea2a`；没有带入新谱图差值分支。该固定版本全仓505通过、1跳过、1个相同的既有路径
审计失败，静态检查通过。新preflight与原版仅源码commit、源码/配置/输出路径不同，
`runtime_config`整体完全一致。运行根、socket与回执SHA集中在阶段计划第10节。

旧入口在首次GPU阶段前已停止，既无baseline推理输出也无训练checkpoint，不计为模型失败
或新attempt；原完成审计准备未派发且不再使用。r2于12:36:24单次派发，原PID与实际命令
已核验，12:39:36完成CPU导入并等待原GPU门槛；新输入清单与预检逐字节一致，五份导入
文件SHA、实际外部存储环境和完整计数复核通过。模型收益及完整审计均未完成。

12:46:01已启动r2专用CPU完成审计队列，13项生命周期检查和真实未完成入口拒绝检查通过；
12:48:06原审计PID3565443存活、CUDA不可见，状态为等待原入口结束，尚未执行完成审计。
该队列仅在原入口退出且全部阶段完成后审计一次，并严格重载权重；不自动重试、晋升或派发
下一轮训练。详细运行位置和指纹见阶段计划，旧版未派发的审计准备不再使用。
