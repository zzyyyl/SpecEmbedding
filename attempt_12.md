# A12：提高自然候选对比损失的相对权重

- 状态：**第一优先，后台等待GPU0**。2026-09-11按用户授权改为共同A04对照的独立
  尝试，不再等待A11；新全量输入及路径隔离预检通过，11:37已完成CPU数据准备并进入
  物理GPU0等待。尚未开始GPU基线评价或模型训练，无本轮模型结果。
- 唯一主要假设：将候选CE相对双向in-batch对比损失的权重由1提高至2，可能使训练更
  重视同一自然候选池中的结构区分，改善完整验证Top-k/MRR。

## 依据与固定改动

本地A04增加候选CE后已通过完整审计并晋升；这支持研究候选监督，但不证明进一步提高
权重必然有效。[MSAlign预印本v1，§5.3](https://arxiv.org/html/2605.19752v1#S5.SS3)
比较候选与in-batch对比目标，为目标选择提供依据；其冻结外部编码器、数据划分和配置
不同，不能作为本轮权重或预期收益的实验证据。

优先级理由：A04完整审计相对其基线的Top-1提高9.5711个百分点、MRR提高0.10505，
各项Top-k均改善（原始来源：`audits/optimization_a04_20260910_r2/receipt.json`）；
这是现有待试方向中最直接的本地收益证据，但尚不能证明权重2优于1。MSAlign v1
§5.3 Table 4在另设Formula-based split上报告in-batch与candidate目标R@1分别
29.5%/53.83%，只支持方向，不能套用增幅或改成本地划分。模型参数和编码预算不增加，
因此先于无本地瓶颈证据的结构改造。反证包括更强梯度可能加剧裁剪、过拟合或牺牲Top-k；
完整审计不晋升则记录失败，不自动继续加权扫描。

[MS-MOLE预印本v1](https://arxiv.org/html/2602.16507v1)讨论指纹预测与正确分子检索之间
的取舍，提醒不能用指纹准确度替代检索验收。指纹辅助预测继续保留为待研究方向；本轮
只调整已存在的检索监督比重。完整研究边界见[研究索引R10](analysis/massspecgym_optimization.md#r10候选监督权重与辅助指纹目标的取舍)。

固定目标为 `L = L_inbatch + 2.0 * L_candidate`，只把
`train.align.candidate_supervision.loss_weight`从`1.0`改为`2.0`。
`L_inbatch`保留原双向、多正例定义，`L_candidate`保留原每query候选CE及归约方式。
余弦嵌入、温度/可学习缩放、二维正负身份、无可用负例query的处理均继承parent。
本轮不同时改变损失形式、负例数、采样分布、学习率或梯度裁剪，也不扫描多个权重值。

## Parent与训练范围

Parent固定为已完成独立审计的A04 epoch20，不依赖A10/A11中途或未来成绩。生成配置
时核验A04候选CE权重为1.0、均匀CE16采样和原始两塔构造；只改变权重，不叠加A10
困难负例等未晋升设置。与并行尝试独立比较，未来组合须另行登记。

从随机初始化完整训练，parent checkpoint只用于fresh完整验证比较。继承A04的GINE、
原谱图均值汇聚、原Q/K配置、均匀采样及模型的其余构造参数。
保留seed42、batch128、质量block32、学习率1e-4、CE16、100轮上限/patience5、原增强、
优化器、调度及裁剪。SpecEmbedding质谱预训练共享结构保持原样。

MassSpecGym v1.5全部194,119条train、既定六条排除后的19,423条val、自然候选及二维
身份协议不变；不提供真值分子式、不引入外部样本、不用test选择参数或提前扩seed矩阵。
每轮完整重编码验证候选，沿用Top-1/MRR选优和Top-k Pareto保留规则。

## 缓存与资源

损失权重不改变tokenizer、规范图、Morgan或候选组的构造；校验来源后复用有效的固定
输入缓存，不删除活跃任务引用的文件。新run须重新生成包含权重2.0的候选监督输入回执、
运行manifest和checkpoint配置；旧权重的配置回执不能当作本轮已验证输入。
学习embedding随新权重重新编码。数据、临时文件和工件均在外部项目存储根。

本改动不新增模型参数、输入张量或候选编码次数；实际显存和整轮时间仍按每轮资源记录
核验。权重改变会影响梯度及裁剪，不能按总训练loss的绝对数值与parent直接判断优劣。
本次仅等待物理GPU0，以UUID映射进程内cuda:0；A10继续使用物理GPU1。每阶段仍需
10,000 MiB、连续120秒及派发前复查；独立运行缓存、输出和冻结源码，不能抢占A10槽位。

## 准备与验收

先核验现有图塔及图+指纹训练路径都使用显式权重，再检查权重2.0下的独立损失/梯度、
实际优化器更新、完整query计数、配置传递与保存重载。关闭改动时保持原权重1.0行为。
独立完成审计须确认实际训练和选中checkpoint的权重均为2.0，并保留parent模型的原配置
用于baseline重编码。最终绑定后执行真实全量输入预检，使用新的固定源码和输出目录。

完整训练与独立审计后按既定整体晋升规则判断Top-1/5/10/20与MRR，逐轮核验全量query、
负例来源、选优及资源成本。未晋升则在attempts.md加删除线并保留原因；单次结果不
推广为候选权重的一般最优值，也不代替同协议SOTA与稳定性验收。

当前结果：**无**。

## 实现与检查记录

`params.yaml`增加默认不启用的权重变更预设，独立继承函数要求parent已启用候选监督且
原权重为显式预登记值；只替换权重并绑定外部存储，保留模型、采样与其余科学参数。
两种分子输入共用现有`CandidateTrainerAlign`，无需另建损失或修改训练器。

已用逐query标量log-partition独立核对权重1/2下的总损失、反向梯度及裁剪后的优化器更新，
包含重复二维正例及无负例query；正式入口的CPU合成训练核验GINE和GINE+指纹的
实际全query计数、权重2.0保存/重载和负例重放，旧权重或旧输入回执不匹配时拒绝。
完整检查数量、首次失败与修正记录集中于阶段计划；这些是实现验证，不是模型收益。

实现提交`0234610`已推送并冻结新worktree。2026-09-11 03:49完成真实全量输入预检，
03:51独立复核19项输入与6个准备文件；训练/验证覆盖分别为194,119/19,423，验证分子
827,600个。条件模型与A04的参数量、随机初始化权重及RNG状态完全相同；新候选输入
回执只改变权重，原A04回执仍为1.0。固定数据缓存继续复用，未生成学习embedding。
源码、配置和回执哈希集中于阶段计划第10节。此预检仅证明A04条件分支可用；最终
parent确定后必须重新核验并绑定其实际模型、采样和指纹输入，不能直接据此派发。

## 最终绑定与衔接准备

以下是优先级调整前的历史A11后继组件验证，不代表现行启动依赖；新共同A04分支须
生成新回执和全量预检，不部署这些旧条件队列。

2026-09-11在A09仍训练时完成最终绑定组件128项CPU检查，覆盖GINE/GINE+指纹、
既有Q/K或注意力汇聚、均匀/结构负例的继承；模型和其余科学参数不变，只把候选监督
权重1改为2。父模型按原权重与原构造重编码baseline，新候选输入回执明确记录权重2；
原回执不改写，旧权重、来源或实际CLI不匹配时停止。真实A04条件完整preflight清单
与独立构造的预期清单一致，合成CLI检查覆盖全部继承组合。

单次衔接组件26项检查通过，含真实隔离tmux的字面参数/环境传递与重复派发拒绝。
实际缺绑定探测确认不会创建队列、最终配置或模型目录。部署前必须取得真实A11入口、
训练和完成审计进程的PID/starttime，并固定A11原队列manifest/launch/claim/runner_exec、
最终parent决定和审计启动清单；不能预填未来PID或仅凭状态文件判断结束。

有实际绑定后才可部署CPU等待队列；原依赖结束、完整独立审计及唯一incumbent确定后，
再绑定最终配置、执行真实全量预检并单次进入既定GPU等待。失败保留工件并停止。
05:03独立读回24项准备/来源文件SHA一致，A09与A12固定源码干净；A12仍未部署，
这些是衔接准备检查，不是训练结果。工件位置和指纹集中于阶段计划第10节。

## 共同A04分支的实际隔离与派发

2026-09-11重新绑定A04完整审计与epoch20权重，seed42、batch128、质量block32、原两塔、
均匀CE16、学习率1e-4、100轮上限/patience5保持，只改CE权重1→2；实际构造仍为
9,046,985参数，初始权重和RNG与A04配置相同。存储路径调整独立记录，不改变科学参数。

固定源码为`/data1/${USER}/repos/SpecEmbedding-opt-a12-parallel-20260911`，commit
`e4b6ca5672d5741285cd62bde4d8693cea42101f`；原0234610条件worktree保留且未修改。
数据根内使用`experiments/massspecgym_v15_opt_a12_20260911_topk256/`存放独立工件，
`runtime/a12_parallel_20260911/`存放可写依赖缓存、legacy缓存路径与临时文件。
30个实际本地模块导入路径均属于新源码，派发后复核197个源码/YAML文件及实际进程环境；
与A10输出、运行缓存及源码目录无解析路径重叠。共享验证图以只读mmap读取并复制tensor，
正式tokenize在本进程内执行；不会写入其他批次的TokenSet或学习embedding。

完整CPU预检重新核验19项输入，覆盖train194,119、有效val19,423、验证分子827,600。
独立tmux socket为`/tmp/specembedding-opt-a12-parallel-20260911-1010/tmux.sock`，
`opt/%0`入口PID3885413/start1458588106；派发回执不自动重试，11:37实际进程核验通过。
两项CPU导入完成，当前`baseline_validation=waiting_gpu`；只等待物理GPU0，对应UUID
绑定后进程内设备为cuda:0。A10继续占用物理GPU1，不改其源码/配置/环境。

准备、单次派发及独立核验回执位于数据根的
`audits/optimization_a12_parallel_a04_20260911/`和
`audits/optimization_a12_parallel_launch_20260911/`，指纹集中于阶段计划。
尚无A12模型trainer身份或完成审计队列；模型启动后需绑定实际PID/starttime并部署独立
完成审计。GPU资源与CPU/磁盘带宽仍可能竞争，隔离不构成速度或OOM保证。
