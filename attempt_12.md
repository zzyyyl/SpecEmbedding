# A12：提高自然候选对比损失的相对权重

- 状态：**准备中，未启动**。在A09训练、A10等待及A11准备期间预登记；最终parent须等
  A11完整审计后决定；配置继承、训练/保存/审计检查及A04条件全量CPU预检已完成，
  当前没有最终配置绑定、训练队列、运行目录或模型结果。
- 唯一主要假设：将候选CE相对双向in-batch对比损失的权重由1提高至2，可能使训练更
  重视同一自然候选池中的结构区分，改善完整验证Top-k/MRR。

## 依据与固定改动

本地A04增加候选CE后已通过完整审计并晋升；这支持研究候选监督，但不证明进一步提高
权重必然有效。[MSAlign预印本v1，§5.3](https://arxiv.org/html/2605.19752v1#S5.SS3)
比较候选与in-batch对比目标，为目标选择提供依据；其冻结外部编码器、数据划分和配置
不同，不能作为本轮权重或预期收益的实验证据。

[MS-MOLE预印本v1](https://arxiv.org/html/2602.16507v1)讨论指纹预测与正确分子检索之间
的取舍，提醒不能用指纹准确度替代检索验收。指纹辅助预测继续保留为待研究方向；本轮
只调整已存在的检索监督比重。完整研究边界见[研究索引R10](analysis/massspecgym_optimization.md#r10候选监督权重与辅助指纹目标的取舍)。

固定目标为 `L = L_inbatch + 2.0 * L_candidate`，只把
`train.align.candidate_supervision.loss_weight`从`1.0`改为`2.0`。
`L_inbatch`保留原双向、多正例定义，`L_candidate`保留原每query候选CE及归约方式。
余弦嵌入、温度/可学习缩放、二维正负身份、无可用负例query的处理均继承parent。
本轮不同时改变损失形式、负例数、采样分布、学习率或梯度裁剪，也不扫描多个权重值。

## Parent与训练范围

Parent是A11完整独立审计后唯一保留的incumbent。A11未晋升则继承其已审计parent；
只有未选中Pareto候选达标时，先解决权重及选优关联，不能自动择取。生成配置时核验
parent候选CE权重仍为1.0，否则先排查来源，不静默把另一配置当成本轮对照。

从随机初始化完整训练，parent checkpoint只用于fresh完整验证比较。继承已晋升的
GINE或GINE+指纹、谱图汇聚/QK配置、均匀或结构相似采样，以及模型的其余构造参数。
保留seed42、batch128、质量block32、学习率1e-4、CE16、100轮上限/patience5、原增强、
优化器、调度及裁剪。SpecEmbedding质谱预训练共享结构保持原样。

MassSpecGym v1.5全部194,119条train、既定六条排除后的19,423条val、自然候选及二维
身份协议不变；不提供真值分子式、不引入外部样本、不用test选择参数或提前扩seed矩阵。
每轮完整重编码验证候选，沿用Top-1/MRR选优和Top-k Pareto保留规则。

## 缓存与资源

损失权重不改变tokenizer、规范图、Morgan或候选组的构造；校验来源后复用有效的固定
输入缓存，不删除活跃任务引用的文件。新run须重新生成包含权重2.0的候选监督输入回执、
运行manifest和checkpoint配置；旧权重的配置回执不能当作本轮已验证输入。
学习embedding随新权重重新编码。数据、临时文件和工件均在外部项目存储根。

本改动不新增模型参数、输入张量或候选编码次数；实际显存和整轮时间仍按每轮资源记录
核验。权重改变会影响梯度及裁剪，不能按总训练loss的绝对数值与parent直接判断优劣。
GPU仍按既定0/1择一、显式cuda:N和逐阶段显存条件等待，不创建并行抢卡任务。

## 准备与验收

先核验现有图塔及图+指纹训练路径都使用显式权重，再检查权重2.0下的独立损失/梯度、
实际优化器更新、完整query计数、配置传递与保存重载。关闭改动时保持原权重1.0行为。
独立完成审计须确认实际训练和选中checkpoint的权重均为2.0，并保留parent模型的原配置
用于baseline重编码。最终绑定后执行真实全量输入预检，使用新的固定源码和输出目录。

完整训练与独立审计后按既定整体晋升规则判断Top-1/5/10/20与MRR，逐轮核验全量query、
负例来源、选优及资源成本。未晋升则在attempts.md加删除线并保留原因；单次结果不
推广为候选权重的一般最优值，也不代替同协议SOTA与稳定性验收。

当前结果：**无**。

## 实现与检查记录

`params.yaml`增加默认不启用的权重变更预设，独立继承函数要求parent已启用候选监督且
原权重为显式预登记值；只替换权重并绑定外部存储，保留模型、采样与其余科学参数。
两种分子输入共用现有`CandidateTrainerAlign`，无需另建损失或修改训练器。

已用逐query标量log-partition独立核对权重1/2下的总损失、反向梯度及裁剪后的优化器更新，
包含重复二维正例及无负例query；正式入口的CPU合成训练核验GINE和GINE+指纹的
实际全query计数、权重2.0保存/重载和负例重放，旧权重或旧输入回执不匹配时拒绝。
完整检查数量、首次失败与修正记录集中于阶段计划；这些是实现验证，不是模型收益。

实现提交`0234610`已推送并冻结新worktree。2026-09-11 03:49完成真实全量输入预检，
03:51独立复核19项输入与6个准备文件；训练/验证覆盖分别为194,119/19,423，验证分子
827,600个。条件模型与A04的参数量、随机初始化权重及RNG状态完全相同；新候选输入
回执只改变权重，原A04回执仍为1.0。固定数据缓存继续复用，未生成学习embedding。
源码、配置和回执哈希集中于阶段计划第10节。此预检仅证明A04条件分支可用；最终
parent确定后必须重新核验并绑定其实际模型、采样和指纹输入，不能直接据此派发。
