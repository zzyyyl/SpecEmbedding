# A07：谱图侧有符号母离子差值残差

- 状态：**训练中**。A06训练期间已预登记；其完整审计后全部Pareto候选未晋升，已按下列
  规则绑定A04。2026-09-10 14:20:38单次派发，14:49:22完成基线验证，14:51:48在GPU0
  启动训练。15:48核验前4轮完整训练与验证计数正常，尚未完成或作晋升决定；准备、决定与运行指纹
  集中记录于阶段计划第10节。
- 唯一主要改动：给原峰表示增加轻量的`precursor_mz - fragment_mz`残差分支，显式提供
  母离子与碎片的相对m/z信息；Transformer层数、池化和双塔相似度检索路径保持原方案。
- 假设：原峰表示主要独立编码绝对m/z和强度；显式差值可能帮助对比学习识别与分子结构
  相关的峰组合。该假设尚未通过正式训练验证，不预设Top-k/MRR提升。

## Parent与衔接规则

A06完整训练、每轮完整检索验证、独立完成审计和严格权重重载通过后，才绑定parent。
2026-09-10 14:08:07该依赖已满足，全部审计候选未达改善标准，实际采用下述第2条。
按既定容差检查全部Pareto候选；不能凭中途最佳轮、较低loss或较快训练速度自动晋升。

1. A06选中checkpoint通过整体晋升规则时，以它作为新的比较baseline，继承其指纹分子塔
   和训练配置；A07仍从随机初始化训练，不把baseline权重默认为训练初始化。
2. A06所有已审计Pareto候选均未通过时，保留A04为parent，继承其GINE分子塔和训练配置。
3. 若选中checkpoint未通过、但其它Pareto checkpoint符合晋升规则，先解决候选权重与
   正式selection的来源绑定，再继续；失败或未完成的训练/审计不得触发下一轮。

衔接准备提前完成代码、配置继承规则、输入缓存核验和CPU预检；模型实际派发须等前序
训练及审计原进程结束、终态完整、来源指纹一致。只派发一次，新实验使用新目录和固定
源码；发现冲突保留日志工件并排查，不覆盖、重复发命令或静默重试。

## 模型与固定参数

新增分支为64维sin/cos Fourier特征加1维母离子角色标记，经`65 → 128 → 512`的GELU
MLP后残差相加。波长对数均匀覆盖0.01至10,000 m/z单位，末层零初始化；默认512维峰
表示下增加74,496参数，不增加Transformer层或两两峰注意力矩阵。该数值是构造结果，
不是实测GPU开销。零残差只保证同权重谱图塔初始输出一致，不保证整个双塔的初始化RNG
逐位一致，也不保证收敛更好。实现见[组件说明](docs/precursor_delta_encoder_zh.md)。

保留负差值，不取绝对值、不裁零、不删除峰；它是m/z差值，不能未经电荷/加合物换算
称为中性质量损失。token0母离子与同m/z碎片用角色标记区分，padding不进入注意力或池化。
SpecEmbedding质谱预训练共享结构不改，下游组件使用独立构造配置。

| 条件 | A07设置 |
| --- | --- |
| 核心方法 | 独立双塔映射到共享嵌入空间，对比学习，按归一化嵌入相似度排序 |
| 数据与seed | MassSpecGym v1.5，seed42，完整194,119条训练；既定排除后的19,423条验证 |
| 分子塔 | 整体继承最终parent；本轮不同时改分子结构 |
| 训练 | 继承parent的batch128、质量block32、学习率1e-4、weight decay1e-4、温度及调度 |
| 监督 | 保留双向in-batch对比及最多16个不同二维自然负例、候选CE权重1 |
| 谱图增强 | 继承parent；不同时改峰删除、强度扰动或tokenizer |
| 训练长度 | 原100轮上限、patience5、Top-1优先及MRR次序选优，保留Top-k Pareto候选 |
| 候选/评价 | 完整自然候选、二维身份、无forcing；每轮完整重编码；优化只用验证集 |
| 资源 | 显式cuda:0，由物理GPU0/1择一映射；现授权门槛10,000 MiB、不限利用率、连续120秒 |

本轮不同时加入QK归一化、门控注意力或更大负例预算；这些保留在[研究索引R06](analysis/massspecgym_optimization.md#r06双塔框架下借鉴glacier与近期语言模型组件)，用于后续独立尝试。

## 缓存与存储

原始token、增强、候选索引、规范分子图和固定Morgan位值没有因新增谱图模型分支改变，
因此对应的已审计固定输入缓存可以复用。若最终parent为指纹塔，训练/验证继续使用固定
指纹；若为GINE，继续使用相应图输入。各缓存来源与构造指纹须在最终预检重新核对。
学习得到的谱图/分子embedding随当前权重重编码，不能跨轮复用旧表示。

所有新数据、缓存、临时文件和训练工件位于外部项目存储根，经`run_with_storage.py`
启动；不在`/home`新增数据，不移动或清除A06仍在使用的文件。若后续真实构造参数改变，
对应缓存失效并按既有规则清理和重建，保留来源、审计回执及仍活跃的引用。

## 验证与结果记录

准备阶段检查：默认模型不受影响；负值/母离子角色、padding、碎片排列；分支与两塔梯度；
完整构造和严格重载；GINE/指纹两类parent的配置继承；不完整、失败或仍存活的前序进程
不能触发派发。正式启动前另做真实输入全量CPU预检，不能以合成检查替代输入审计。

启动后记录每轮实际训练/验证数量、自然负例指纹、Top-1/5/10/20、MRR、显存峰值和整轮
耗时；完成后独立重算全部保存排名、核对选优和权重。只有通过既定整体改善规则才晋升；
未通过则在`attempts.md`加删除线并保留原因。当前结果：**训练中，尚无完整审计结论**。

15:48中途记录：原始日志与保存的完整验证快照一致，每轮train194,119、val19,423；
以下为优化验证集的早期轨迹，不能替代最终选优/完整审计或据此判断最终模型收益。

| 已完成epoch | Top-1 | Top-5 | Top-10 | Top-20 | MRR | 整轮秒数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 4.32% | 19.57% | 32.38% | 45.63% | 0.128490 | 631.92 |
| 2 | 5.00% | 22.23% | 35.71% | 49.59% | 0.143072 | 639.91 |
| 3 | 6.65% | 25.71% | 40.45% | 54.46% | 0.169216 | 657.26 |
| 4 | 7.84% | 26.81% | 43.22% | 57.30% | 0.182356 | 771.37 |

前四轮PyTorch分配峰值最大3,712,166,912 bytes、预留峰值最大7,092,568,064 bytes；
这不是完整轨迹峰值。整轮耗时约10.5–12.9分钟，含完整训练/验证/保存，不含初始
准备、GPU等待或训练后审计；实际训练子进程的UUID、严格cuda:0和运行配置已核验。
第3/4轮的完整query顺序、样本计数与保存排名重算通过；15:48原入口、训练和独立完成
审计进程的PID/starttime均匹配且存活，审计仍为waiting_parent，尚无最终审计结果。

2026-09-10准备检查：衔接、差值组件、完成审计与外部存储共70项合成检查通过；旧A05
完整已审计结果经新决策检查仍保留A04，四个Pareto候选均被复核。按真实A04配置构造
后续GINE版本为9,121,481参数；按A06当前配置构造指纹版本为9,932,673参数，后者仅为
条件构造检查，不是把运行中的A06选为parent。A04旧默认缓存/输出路径已在新配置构造
中显式改绑当前外部存储模板，科学参数保持继承；模板路径越界时拒绝。

固定源码`36e1b0c`的全仓检查为561通过、1跳过、1个相同既有路径审计失败；A04条件的
真实输入全量预检于14:00:02通过。A06完成审计后另建最终准备目录，重核209项前序/条件
工件，确认原训练与审计进程均结束；最终预检与条件预检仅配置文件位置不同，科学配置、
输入指纹及阶段命令一致。随后已单次启动新入口，实际原进程和导入文件字节一致性核验通过；
独立完成审计于14:29:51单次启动，14:31核验纯CPU环境、原入口绑定和存活状态通过；
13项生命周期检查及实际未完成拒绝探测通过。当前等待本轮原入口结束，完成审计后才做
晋升决定，不把准备或排队写成模型训练完成。
