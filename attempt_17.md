# A17：固定 query batch，增加自然候选负例预算

- 状态：**预登记、全量输入诊断、独立复核与配置组件验证完成，未启动模型或队列**。
- 唯一主要假设：每条谱图在同一步中比较更多不同二维自然负例，可能缩小训练候选目标与
  完整候选检索的差距，提高完整验证Top-1，并兼顾Top-k/MRR。
- 拟固定共同对照：已完成独立审计的A04；从随机初始化训练，不继承A12/A13/A16中途权重。

## 原因与证据

本地最明确的收益来自[A04](attempt_4.md)：加入自然候选CE监督后，完整验证Top-1相对
A02增加9.57个百分点，并通过全部Top-k/MRR晋升规则。这支持继续研究候选监督，不能
证明增加预算必然有效。[A10](attempt_10.md)更换负例分布虽提高Top-1，却损害Top-10/20；
因此本轮保留原均匀二维采样，只增加预算。[A12](attempt_12.md)研究权重，仍待完整结果；
本轮不采用其未晋升的权重2，也不把两个改动组合。

完整CPU诊断`audits/negative_budget_gap_20260911/`重新核验A04已审计的全部19,423条
验证分数，按训练规则“均匀选择不同二维身份，再在该身份内均匀选择源条目”计算反事实
子集判别概率。仅比较16/32/64个负例时，正例严格高于所选负例的query平均概率分别约
55.63%/44.04%/33.31%，A04完整候选Top-1为17.14%。这说明小子集仍可能过于容易；
它不是训练集准确率、新检索成绩、性能上界或增加预算后的收益预测。
诊断保留全部query、候选身份重复及同分边界；来源和独立复核集中于优化索引D27。

外部依据：[MSAlign预印本v1，§5.3/Figure 3](https://arxiv.org/html/2605.19752v1#S5.SS3)
在固定query batch128时增加候选负例，观察到其候选对比方法随规模提高；这比从通用
注意力结构猜测收益更直接支持本轮假设。该消融使用作者另设Formula split及冻结
DreaMS/ChemBERTa，只训练投影；本项目是官方structure-disjoint、可训练两塔，不能
继承其成绩、增幅或约20分钟训练成本。它也没有证明本地16→32的收益。
原PDF已保存于`/data1/${USER}/papers/2605.19752v1_MSAlign.pdf`。

## 固定改动与协议

唯一科学参数改动为`train.align.candidate_supervision.negative_count: 16 → 32`。
保留原谱图塔、GINE分子塔、512维共享嵌入与相似度排名，以及均匀采样、CE权重1、
双向in-batch对比目标、seed42、query batch128、质量block32、lr1e-4、原增强、
AdamW/调度/裁剪、100轮上限和patience5。SpecEmbedding预训练共享结构不修改。
本轮不叠加A13条件层、A16辅助头、结构相似采样或其它未晋升组件。

MassSpecGym v1.5全部194,119条train、19,423条有效val、六条验证排除与自然Mass候选/
二维身份保持；不分析测试分数或据测试调参。小候选池使用`min(32, available)`，不删query、
不补造负例，也不把正例身份当作负例。查询batch没有增大，不能称为扩大in-batch负例池。
负例预算变化会改变采样及后续增强随机数消费，不能声称训练轨迹与A04只差确定性loss值。

## 输入、缓存与计算成本

全量train原始候选池包含每query 11–255个不同二维负例。预算16/32对应每轮
3,105,854/6,211,188次负例出现；实际seed42/epoch1的全部32负例采样已逐项核对来源、
身份和完整query覆盖，没有模型forward/backward。仅79条query在预算32下覆盖全部负例。
预算64约1,241.5万次，首轮选择32以控制代价；不是通过减少训练query来省资源。

参数量仍应为9,046,985，但每batch负图上限由2,048增至4,096，当前实现保留完整负图
计算图用于反向，不能用分块前向的表象声称解决了峰值显存。负图出现数接近翻倍，不代表
整轮时间或显存恰好翻倍；谱图、正图、完整验证和训练流水的成本也需计入。
旧指纹输入诊断D16不适用于GINE的GPU资源结论，GPU准入门槛须在实际准备时重新评估。
首轮记录全量训练、验证、重新编码耗时和本进程CUDA峰值，不预设提速或性能提升。

固定分子图、原始候选元数据及验证索引在构造指纹不变时可继续只读使用；32负例的
`candidate_training_input`和运行配置必须重新生成，不能复用16负例运行回执。
学习embedding每轮重新编码；当前训练缓存构造不改，不扩大未经测量的LRU容量。
若后续引入训练候选磁盘图缓存，先独立验证完整输入/RNG等价和成本，再登记工程变化。
新工件、缓存及临时文件均在外部项目根；固定独立worktree与运行目录在派发前确认。

## 顺序、启动条件与验收

本轮优先级高于缺少本地性能证据的A11/A14/A15；A16已登记的GPU1衔接队列保持。
资源允许时拟接A12释放后的GPU0，但现在没有创建A17队列。A12仅作为资源依赖，
其部分结果不决定本轮参数。最终确认共同A04对照、固定源码、独立路径和完整预检后，
再绑定真实PID/starttime并单次派发，不占用正在运行的项目训练槽位。

已实现默认不激活的中央预算预设与严格配置继承函数，拒绝更改parent、混入结构相似采样
或额外权重；15:58完成170项相关CPU回归，Ruff、绝对路径compileall及diff通过，未重跑
全仓或将既有路径失败改为通过。来源与SHA集中于阶段计划。启动前仍需完成：真实32负例
训练/审计路径验证、完整实际Dataset
输入和采样计数、资源评估、固定版本与运行预检。若本机资源不足或32负例不能在合理成本
内训练，则延后并记录依据，不静默缩减query/batch或转CPU。

完成后按既定完整验证Top-1选优及Top-k/MRR整体晋升规则决定，不用小候选子集概率或
训练CE下降代替结果。若A12/A13已晋升，还须对当前incumbent报告同协议完整指标差异，
不能只优于旧A04便自动晋升。未达标准的已完成尝试加删除线；输入/调度失败另行记录。

当前模型结果：**无**；是否达到或接近同协议SOTA仍未完成。
