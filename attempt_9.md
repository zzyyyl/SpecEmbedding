# A09：GINE分子表示加入固定指纹残差

- 状态：**训练中**。2026-09-11 A08完整审计未晋升后已绑定A04，01:54最终全量预检
  通过；fresh基线验证02:08完成，02:11在物理GPU1启动正式训练，首轮完整计数已记录。
  原入口及独立完成审计队列正常，尚无完成审计或晋升决定；A10已绑定实际进程并等待。
  04:42核验第10轮已结束且原进程仍存活；固定前九轮的完整分数诊断和独立复核已完成，
  同分处理不足以解释相对A04的主要差距，详见[诊断D25](analysis/massspecgym_optimization.md#d25a09前九轮完整验证分数与同分敏感性)。
- 唯一主要假设：保留GINE的图结构表示，增加固定Morgan指纹的小型可学习残差，使两类
  分子信息共同参与对比检索；不同时改变谱图塔、损失、batch或负例预算。

## 最终绑定及完成审计队列（2026-09-11）

A08的27轮完整审计、142项工件及严格权重重载通过，全部Pareto候选未晋升，本轮选择
已审计A04为parent；不继承A08的Q/K归一化。固定源码ef2526d保持干净，最终预检、
运行manifest、配置和真实原进程已核对，正式入口仍为原PID3678937及原starttime。
源码配置通过CLI展开已登记的`gine_fingerprint`和指纹残差参数，实际runtime与冻结
preflight逐字段相同；其字节SHA与展开前源params不同，二者分别固定，不能混用。

数据与完整验证索引导入后，基线验证及正式训练依次进入GPU0/1择一等待，保持10,000 MiB、
利用率仅记录、持续120秒及逐阶段复查。固定tokenizer、图、Morgan和自然候选缓存来源
均已核对，输入构造不变，学习表示仍重新编码。

独立CPU完成审计已绑定实际run：13项生命周期检查及真实“未完成运行拒绝审计”探测
通过，29个不同路径的固定输入重新核对。02:04:04在新独立socket单次启动等待进程，
02:09:58核验实际PID/starttime、pane、原入口及完整外部存储/空CUDA环境通过。
它等待正式入口的所有阶段完成后才执行一次独立审计、完整工件哈希和严格CPU权重
重载；错误保留并停止，不自动重试，也不自行作晋升决定。

首次只读核验辅助脚本误用源params的SHA检查runtime，已确认实际runtime自始与运行
状态、冻结preflight及审计manifest相符；修正辅助脚本后核验通过，失败源码和诊断保留。
没有修改运行配置或重启队列。这不是失败模型attempt。原始路径、来源与回执SHA见计划。

02:08:41 fresh A04基线验证完成，02:11:34正式训练选中物理GPU1并按UUID显露为cuda:0；
02:13:10进入stage2。真实训练PID3752707、原入口和CPU审计进程及其完整环境已核验，
原源码保持干净，runtime与冻结清单一致。日志及首轮资源记录均确认train194,119、
val19,423，batch128、block32和CE16；02:27:07记录首轮完成，后续仍在训练。这是运行
中的记录，全部保存排名、最终权重、早停和晋升仍须完整审计后判断。

02:32 A10已绑定本轮真实入口/训练/审计进程及43项固定文件，单次部署CPU衔接队列并
核验实际进程、独立socket和环境；只等待本轮全部完成及审计，不提前决定parent。

## 依据与边界

[完整验证排名诊断D21](analysis/massspecgym_optimization.md#d21a04与a06完整验证排名的交集诊断)
显示已完成A04与A06的命中集合有差异，但A06整体较弱；交集不能直接证明融合有效。
[FP-GNN原文](https://academic.oup.com/bib/article/23/6/bbac408/6702671)结合图与混合指纹，
[Chemprop原文](https://pubs.acs.org/doi/10.1021/acs.jcim.9b00237)分析加入固定分子特征的收益
和退步。这些研究针对分子性质预测，不是本地MassSpecGym检索的统一复现。

本轮只用已有Morgan radius2、2048位、无手性输入和原GINE，不复制外部完整模型、混合
指纹、性质预测头或预训练权重。核心仍是独立谱图塔与分子塔映射到共享空间，对比学习并
按归一化嵌入相似度检索；分子塔不依赖当前谱图或候选列表上下文。

## 模型与继承设置

在GINE池化与规模特征融合后的128维分子表示上加
`Linear(2048,256) → LayerNorm → GELU → Linear(256,128)`，再进入原分子投影头。
最后线性层权重和bias为0，分支构造隔离随机数状态，检查原两塔初始权重和训练随机序列
保持不变；没有额外dropout或门控。CPU构造已确认增加557,952参数，梯度检查已通过，
实际GPU显存与耗时仍待测量。零残差起点不保证最终优化收益。

Parent是A08完整审计后仍被保留的唯一incumbent：A08选中模型通过既定整体改善规则则
继承A08；全部Pareto候选未通过则继承其已审计比较parent，可能为A07或A04。只有非选中
候选通过时先解决selection/checkpoint绑定；任何未完成、失败或歧义状态均不能触发本轮。
从随机初始化训练，parent权重只作比较；其谱图差值/QK开关及其余参数完整继承。

固定seed42、完整194,119条train、既定6项排除后的19,423条val、v1.5 Mass自然候选和
二维正例标签、无forcing；继续batch128、质量block32、学习率1e-4、原温度/调度、CE16
及原监督权重、100轮上限和patience5。只用完整验证集选优，保留全部Top-k Pareto候选，
不使用test误差或额外数据选择结构。

## 输入、缓存与存储

指纹对应原始候选分子，已有图增强仍只作用于图视图，参数不变；不因图扰动而生成新
指纹，也不因指纹相同而合并二维身份或候选。训练目标与自然负例均必须附带各自指纹，
验证候选按原完整顺序绑定，不能从正例标签、当前谱图或另一个分子的行号代取特征。

已有固定图和Morgan缓存构造参数不变，核验来源与全量行映射后复用；不复用已学习的
embedding。新增组合输入须分别绑定图/指纹来源、完整query及候选行号，真实构造变化
才使对应缓存失效，保留活跃任务引用。全部新工件、缓存与临时数据仍在外部项目存储根。

实现需保留训练负例分块，新增指纹只按当前batch取出，不把全部候选float32指纹搬上GPU。
batch128与最多16负例时，原始FP输入张量最多约17 MiB；这是张量预算，不包括激活、
优化器、图和其它模型开销，不作为实测显存或提速结论。最终预检须重新评估完整模型成本，
显式cuda:N、GPU0/1等待、不回退CPU及不缩减训练数据的规则保持。

## 启动前检查与验收

先验证独立组件：继承权重/RNG、零残差初始输出、两步后分支与GINE/两塔梯度、图和
指纹行绑定、排列与分块一致、严格重载和缺失输入拒绝。组件准备期间正式入口须拒绝
未接通的新模型类型，不能以仅构造可用冒充整条训练路径完成。

之后完成训练/验证组合输入、完整来源预检、checkpoint配置和独立完成审计接入，并用
真实小型合成训练检验全样本计数、自然负例、权重重载与重新编码。A08审计结束后才
绑定最终配置、固定源码和单次派发；运行记录全部Top-k/MRR、完整样本、耗时及显存。
按既定整体改善规则晋升，未通过则在attempts.md加删除线并保留结果。当前结果：**无**。

## 组件准备进展

2026-09-10已实现独立组件与未激活配置，详见[组件说明](docs/graph_fingerprint_encoder_zh.md)。
22项合成检查通过，覆盖有/无QK及差值分支的原两塔初始权重/RNG与输出一致、原优化器
分组覆盖全部参数、两步梯度、独立残差公式、分块与分子排列、缺失/错误输入形状拒绝、
严格重载和缺失新权重拒绝。后续接入核验发现，该阶段对照采用checkpoint重构器的初始化
顺序，与真实fresh训练入口不同；现已修正组件为先构造分子塔、再构造谱图塔，直接捕获
真实`train_align`入口的模型作比较，四种谱图条件下原权重/RNG及零残差输出均通过。
此问题发生在未启动的准备组件，未改变已运行实验的源码、权重或结论。

| 继承的谱图条件（仅构造检查） | A09总参数 |
| --- | ---: |
| A04原谱图塔 | 9,604,937 |
| 有符号差值分支 | 9,679,433 |
| Q/K归一化 | 9,605,193 |
| 差值与Q/K归一化 | 9,679,689 |

这些条件不是parent选择，也没有执行正式训练。初次组件准备的主机全仓为612通过、1跳过、1个
相同既有路径审计失败；Ruff、compileall及diff检查通过。首次沙箱全仓检查因多进程
共享socket被拒而出现DataLoader超时，核验后仅中断该pytest并在主机权限下重跑，未改
测试来跳过多进程验证。SpecEmbedding预训练源码保持不变，A07/A08固定源码未修改。
两篇原论文已下载并核验版本与PDF标题，位置和SHA集中于阶段计划第10节。

## 组合输入准备进展

2026-09-10新增训练/验证组合读取器：复用既有谱图、图增强、自然负例采样和PyG批处理，
将目标及负例各自的固定指纹按原始分子行号附到图上；两类缓存的构造模块及格式保持不变。
负例保留原图缓存容量和增强来源描述，固定指纹不受图增强影响；验证同时绑定图/指纹
来源、完整候选顺序及数据指纹，每次重新编码学习表示，并在排名快照记录两类缓存。

新增22项合成检查通过：精确目标SMILES及验证置换、训练原始query映射、同RNG下图/谱
增强和采样一致、缓存隔离、真实spawn worker跨epoch读取、错配来源拒绝、指纹碰撞不合并
二维标签、直接余弦重算及权重变化后重编码、两轮完整合成训练与重复query拒绝。与模型
和既有纯指纹检查合计65通过；全仓634通过、1跳过、1个相同既有路径审计失败，Ruff、
compileall及diff检查通过。另试的减少验证图拷贝微调因复测自动审批两次超时未验证，
已撤回，当前保留通过全仓检查的实现。

在上述组合输入准备节点，仅组件、组合数据路径及排名快照审计可用；`train_align.py`、正式编排和完整完成
审计仍未接通新类型，正式构造器继续拒绝。尚须完成真实入口合成训练、独立baseline
输入分离、全量真实预检、最终parent绑定与固定源码，不能把上述synthetic检查记为
MassSpecGym训练或本轮收益。A07/A08固定源码与原队列保持，不创建A09运行或派发。

## 正式接入准备进展

2026-09-10已接通`gine_fingerprint`的严格构造、`train_align`正式路径、独立baseline
评价、队列预检/阶段检查和完整完成审计。新增CLI选项为`--molecule-input gine_fingerprint`，
同时要求完整train/validation指纹路径；图增强、图缓存容量和自然负例监督保留原设置。
基线按自身checkpoint决定是否需要图/指纹，新模型按自己的配置决定，不能共用错误输入。
正式checkpoint保存完整残差配置及两类输入来源，严格重载不接受缺失分支权重。

合成检查覆盖真实训练入口的两轮全量小型训练、质量batch、候选样本完整重放、Q/K与差值
分支组合、旧GINE/纯指纹/组合模型三种基线的输入分离、实际基线入口重编码、缺失/错配
工件拒绝和完整完成审计。全仓668通过、1跳过、1个相同既有路径审计失败，Ruff、compileall
和diff检查通过。两处既有夹具补齐真实selection必存的模型配置，错配测试改为确实不同的
缓存容量；不放宽生产检查。本轮尚须冻结源码、真实输入条件预检及A08审计后的最终绑定，
没有正式训练、test评价或融合收益结论；原固定缓存构造模块、预训练源码及A07/A08不变。

## 真实条件预检

源码冻结为`ef2526d9c4cdd7561b7d40d605ced5c4b5c28dda`后，18:29:06通过A04条件的
真实全量CPU预检。重新核验A04完成审计及其绑定工件、30项实际输入文件、图/指纹共同
索引和独立baseline命令；覆盖194,119条train、19,423条val，指纹行数分别5,711,753和
827,600，图增强仍0.1/0.1、batch128、学习率1e-4。18:33另行读回全部准备文件和输入SHA、
源码与阶段命令，确认无模型运行目录；路径和回执指纹集中于阶段计划第10节。

这是可运行性条件检查，不是A09最终parent选择。A08完成审计后仍须确定唯一incumbent，
保存最终继承配置并重新核验对应命令/来源、评估资源后单次派发；该预检节点没有A09 GPU队列、
模型训练或检索成绩，不以条件预检替代最终绑定或完成审计。

## 最终绑定入口准备

19:22已归档A09最终parent绑定与CPU预检入口，64项检查通过，Ruff通过，10个准备文件
另行读回SHA及固定训练源码核验通过。覆盖A04/A07/A08及谱图分支的继承、真实预检清单
中的CPU导入阶段、未完成或仍活跃进程阻断、审计/权重/来源错配拒绝、非选中候选单独
达标时暂停，以及预检失败保留工件；所有检查均为准备验证，没有模型训练或检索收益。

入口先要求实际A08完成审计队列的不可变绑定及SHA，不能使用预计PID或仅凭complete
状态推断完成；全量审计、原进程退出、权重与工件来源通过后，才按既定规则选择唯一
parent。继承其全部科学参数和谱图设置，配置只添加预登记指纹分支，并重新执行完整
CPU预检；已存在最终绑定/模型目录时拒绝，不派发GPU或自动重试。

在该准备节点，只读探测确认修复后的A08原等待进程存活，尚未创建A09最终配置目录、
衔接队列或模型目录。后续单次派发入口仍须核验最终ready
回执和命令再进入GPU等待，不能把本准备回执作为已启动或已完成证据；来源集中于计划第10节。

## 实际依赖绑定与后台衔接

A08已启动正式训练后，重新核验其原入口、训练和独立完成审计等待进程的实际PID/
starttime、配置、CUDA环境与23项输入；不可变绑定指向真实A08运行及审计清单，没有
预填未来PID或审计结论。该绑定不等于完成，最终parent仍须等A08审计后按既定规则确定。

单次衔接队列20项检查通过，包括真实隔离tmux传参和绑定参数传递；真实探测确认
三个A08原进程仍活跃。20:00:38队列单次启动，20:02独立核验原PID/starttime、专用
socket/pane、空CUDA和完整外部存储环境通过，没有最终绑定目录、runner exec或模型
工件。随后仍需原依赖全部结束、完整审计、最终配置与全量预检通过才进入GPU等待。

首次准备命令因自动审批服务连接中断被拒，核验目录不存在且命令未执行后，重试相同
CPU准备命令成功；未绕过审批或复用失败模型工件。固定训练源码ef2526d和已有缓存
构造保持，后续每轮仍按同一实验卡更新结果，不能把队列启动记为模型训练。
