# A05：扩大质量分组的训练 batch，并复用固定验证图

- 状态：2026-09-10 04:18:19单次派发，完整数据/验证索引导入及物理GPU0上的baseline
  完整验证已完成，04:25:36重新通过GPU门槛开始训练；06:40:34完成前11轮，继续训练，入口见阶段计划第10节。
  干净源码固定为`a5aeb3b`，A04独立审计、唯一parent与最终完整dry-run均已通过。
- 唯一模型优化假设：batch128→256，每个质量block仍32条，由每batch约4个block增至8个，
  增加批内对比的结构范围，检验Top-1及其余Top-k/MRR是否改善；不是梯度累积。
- Parent：已完成独立审计并晋升的A04 epoch20，最终绑定记录见阶段计划；其余条件草案保留追溯。
  训练从随机初始化开始，parent checkpoint只用于完整验证比较，不用于初始化。

## 预先固定的条件

seed42，全部194,119条训练谱图每轮恰好一次，保留最后71条；每轮759个batch，旧batch128
为1517个。学习率仍1e-4，不做线性放大；weight decay1e-4，100轮上限、patience5和既定检索
选优规则不变。两塔、图/谱图增强、温度和自然候选监督配置继承A04，只改变batch大小；
最多16个不同二维自然负例、候选CE权重1。仅派发这一个方案，不运行另一条件分支或多seed矩阵。

batch加倍会改变梯度、每轮更新次数和对比目标，不能称为数学等价提速；不保证收敛更好。
每轮学习率调度保持原规则，完整样本覆盖不变。首轮记录实际吞吐、各阶段耗时及显存峰值。
现有入口的配对对比验证也共用此batch参数，因此随之变为256；其loss不直接与batch128的
数值比较。用于checkpoint选优的完整候选检索验证仍保持分子1024/谱图128和完整分母。
若OOM/NaN或完整性失败，保存工件并排查；不静默减batch、减候选、减训练数据或重试原目录。

## 单独记录的缓存工程改动

复用完整CPU验证索引，并用无损磁盘图缓存替代逐轮RDKit构图。只保存固定分子输入，节点、
边及图大小特征恢复后与现算张量完全相同；不缓存随训练变化的embedding。完整候选顺序、
图资格排除、二维正例、19,423条有效验证query及缺正例分母规则不变。

采用验证分子batch1024、谱图batch128、workers4；缓存准备使用独立CPU进程池，完整重建
所有图并逐张比较四种张量，来源/版本/字节指纹均通过后才启用。训练入口与baseline验证
使用同一缓存，逐轮snapshot、selection、预检与完成审计均绑定缓存回执。详见优化索引D09。

实际完整图缓存已审计通过，约806MiB；完整CPU图加载三组测量显示约208秒→31秒，全部
批量输入张量指纹相同。该CPU测量不包含GPU模型计算，不能作为A05整轮训练的提速结论。

缓存和batch的影响分别说明：CPU图读取测量只能说明数据准备开销；整轮训练加速必须来自
A05实测，不能把A04耗时变化全部归因于CPU，也不能把两个改动的收益混为单一因果结论。

完整训练流的LRU容量模拟见优化索引D10：负图容量增大十倍只减少约4%的未命中构图，
正图容量增大没有收益。因此保留草案中的训练图缓存容量，不附加未经测量的大内存设置；
完整训练候选磁盘图缓存属于后续待验证工程方案，未加入本次已冻结输入。

## 快速衔接顺序

最终绑定和单次派发记录位于阶段计划中的`optimization_a05_finalization_20260910/`，其
`ready.json`及`dispatch.json`取代条件草案用于本次启动；原准备工件保留。新命令通过外部存储入口
启动，原始数据副本、配置路径、下载/临时缓存和实验输出均绑定到外部项目根；所有非路径
训练设置不变。此次只迁移后续引用，没有改动A04。具体校验见优化索引D13。
真实队列子进程已核验保留外部Numba和Matplotlib缓存路径；固定源码相关29项检查通过。

实际baseline验证已从固定图输入重新编码全部827,600个候选，完整19,423条query的保存分数
已与A04同一checkpoint的epoch20独立比较：最大分数差4.18e-7，各指标差异均低于预设容差。
该次检索验证计时约34.36秒，A04选中轮约229.21秒；GPU/时段不同，不能把整个倍数归因于
缓存，也不能代替A05整轮训练提速结论。原始回执与范围见阶段计划。

1. A04训练期间完成缓存实现、测试、独立源码冻结、完整图构建及审计，准备两个可能parent
   对应的配置草案（已完成）；仅最终选中的一份成为运行配置。真实CPU预检已核验100轮
   全量batch128/256的query顺序相同、均无丢样或重复，随机数流不变；这是采样预检，非训练。
2. A04结束后确认原训练进程退出、完成全轮排名/样本/自然负例重放审计，并记录唯一parent（已完成）。
3. 固定最终配置、checkpoint及源码指纹，执行完整dry-run（已完成）；新运行目录仍拒绝已有工件。
4. 通过物理GPU0/1择一的原空闲门槛，先完整baseline验证，再进行A05全量训练（已单次派发）。
5. 结果在attempts.md用一句话记录；未晋升者加删除线，保留具体结果与原因。

GPU等待及严格cuda:0的UUID映射规则不变。准备完成不等于正式实验完成；test、额外seed、
rerank矩阵和论文更新仍待模型优化与独立审计成熟后执行。

## 首轮观察

第1轮完整验证Top-1/5/10/20为3.24%/16.61%/28.57%/42.65%，MRR0.1101；这是随机初始化
后的早期结果，最终效果与保留决策等待完整训练及审计。实际train194,119、val19,423，759个
batch且最后71条；观察到的完整query排列与文件SHA独立读回通过，无重复或丢样。记录的自然
负例总数3,105,854，最少11、最多16，完整逐query负例重放仍留待最终审计。

首轮总计682.62秒，训练631.47秒、完整检索验证45.99秒；A04首轮分别为808.23、580.95、
220.88秒。本次整轮耗时短约15.5%，训练阶段本身未加快，主要省在验证。GPU/时段不同且
batch影响优化目标，不能把这个比例视为固定加速保证或模型收益；继续观察后续资源与验证轨迹。
PyTorch峰值allocated为6,504,201,728字节、reserved为13,899,923,456字节，参数量不变；
不将这些分配器数值当作整机或worker内存峰值。首轮资源回执及全量计数见阶段计划。

第2/3轮资源记录仍为train194,119、val19,423，整轮分别708.08/715.22秒，完整检索验证
45.04/46.35秒。第3轮保存的完整验证快照为Top-1/5/10/20：7.46%/25.38%/40.23%/52.45%，
MRR0.1709；早期轨迹继续改善，最终选优和与A04的比较仍待完整训练及独立审计。

05:38:34完成第6轮，保存的完整验证Top-1/5/10/20为11.06%/32.37%/47.37%/58.91%，
MRR0.2218，按既定Top-1后MRR规则成为当前选优轮。第5轮曾回落，继续遵守原patience5；
第6轮总计752.75秒，资源记录仍为train194,119/val19,423。完整训练及与A04的最终比较尚未结束。

06:15:30完成第9轮，保存的完整验证Top-1/5/10/20为12.82%/34.87%/48.68%/61.02%，
MRR0.2413，刷新当前选优并重置patience计数；此前第7/8轮未超过第6轮Top-1。
第9轮总计724.08秒，其中训练672.60秒、完整检索验证46.66秒；前9轮资源记录全部为
train194,119/val19,423，原训练PID继续存活。尚未完成训练，不据此作A05最终晋升判断。

06:40:34完成第11轮，完整验证Top-1/5/10/20为12.80%/33.63%/49.72%/62.74%，MRR0.2425，
资源记录train194,119/val19,423、整轮749.08秒。MRR虽高于第9轮，Top-1仍少3条命中，
既定Top-1优先规则保持当前选优epoch9，patience计数2/5；未改训练或选优设置。
