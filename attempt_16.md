# A16：训练期用加合物条件峰分布辅助监督分子嵌入

- 状态：**辅助头验证、完整train目标缓存及独立读回完成，正式训练未启动**。
  输入源码已冻结，缓存完整覆盖194,119条query；正式训练/审计接入仍待，不具备派发条件。
- 唯一主要假设：在现有对比目标之外，让正例分子的共享嵌入结合观测加合物预测峰分布，
  可能使分子表示包含更多与碎裂相关的信息，从而提高完整验证Top-1，并兼顾Top-k/MRR。

## 为什么考虑这项实验

按用户提供的GLACIER方法与结果摘要，其谱图预测模型在对比微调前已具备较强检索能力，
化学碎片监督、精确质量和测量条件值得借鉴。参考位置为
[GLACIER v1方法](https://arxiv.org/html/2606.29161v1#S3)、
[训练消融](https://arxiv.org/html/2606.29161v1#S4.SS2.SSS5)与
[分辨率实验](https://arxiv.org/html/2606.29161v1#A6)。遵照用户要求，本次仅以其结果
启发实验设计，不重新核验论文/本地复现的具体数值，也不启动GLACIER复现。

这里提出的是本地适配假设：**谱图预测作为双塔训练的辅助任务**。原方法的碎片检测、
指导标签和按预测谱排名，与我们的轻量辅助头不同；原结果没有证明这一适配有效。
其不同查询子集、测量条件下的成绩不能直接变成本项目的收益预测或SOTA达标线。

本地A04的完整审计说明加强候选区分监督曾有效，支持继续研究训练信号；它不证明
谱图辅助目标有效。A07增加母离子—碎片差值后未晋升，提醒“有化学含义的输入”本身
不足以保证提升。本地来源分别见[A04](attempt_4.md)和[A07](attempt_7.md)及其审计工件。
相较继续增加通用注意力组件，本方案更直接研究分子结构与观测谱之间的联系，值得先
评估可行性；证据仍弱于A12的本地监督方向和A13的可用字段/外部消融支持。

## 完整训练输入证据与设计修订

2026-09-11完整读取194,119条训练query，包含7,301,230个原始峰及22,742个按当前训练
定义计算的二维分子身份；峰m/z最大2954.380615，每query峰数中位数20、最大299。
分箱范围和成本仅根据train决定，未分析val/test谱值或据其选择范围。

0.1 Da分箱、同bin线性强度求和并L2归一化后，同二维身份的谱图平均余弦为0.480512；
相同身份/加合物配对为0.569742（8,603,910个有序非自身配对），跨加合物配对为
0.062613（1,837,120对）。组间组成不同，这些是描述性统计，不是加合物的因果收益、
预测准确率上限或本地检索成绩。它们支持在预测目标中区分不同离子形式；train的两类
加合物均完整可用。碰撞能量字段缺失92,510条，且原数值单位尚未统一，本轮先不使用。

据此，在A16尚未实现/派发前，将原无条件草案显式修订为**只在训练辅助头输入加合物**。
这是一个完整条件辅助目标的假设，不能把将来的差异单独归因于谱预测或加合物；A04的
谱图塔和独立分子编码保持原样，不启用或继承A13未晋升的谱图FiLM分支。

第一版诊断误把TSV原始InChIKey字段当作训练身份，独立核验拒绝：260条身份字段不同，
全部query标识、峰数、峰m/z/强度字节一致。现已按固定源码从SMILES重新计算二维身份，
另存r2结果并与完整train.pkl逐条复核通过，未修改数据或训练协议。原诊断、拒绝原因
及脚本保留。复算脚本还修复了每query重复读NPZ数组的问题，只停止并重跑该CPU诊断，
没有中断训练。诊断/独立回执来源集中于阶段计划，旧分组数字不作为有效结论。

## 登记的单一组件设计

保持原谱图塔、GINE分子塔、共享嵌入和对比目标，增加只在训练使用的小型预测头：

```text
正例分子的归一化共享嵌入 + 观测加合物 → 小型低秩MLP → 非负分箱峰强度
配对的原始观测MS/MS峰 → 固定分箱与强度归一化 → 辅助监督目标
总损失 = 原对比与自然候选损失 + λ × 谱图辅助损失
```

目标取自同一条训练query的未增强观测峰；不把Tokenizer人为添加的母离子token当作
碎片峰，不把随机峰删除/强度扰动后的输入当作干净监督。不伪造碎片结构或MAGMa标签。
候选负例不额外经过辅助头；检索时移除辅助头，候选仍可独立预编码并以嵌入相似度排序。
辅助头梯度必须回传分子塔；不能仅证明预测头拟合而宣称共享嵌入改善。

固定0.1 Da、范围[0,2955)，bin索引按float64的`floor(mz*10)`构造；另加溢出bin共
29,551维，不删除越界query。保留全部原始观测峰，同bin线性强度求和后L2归一化，
零强度条目不占稀疏载荷；负值/非有限/零范数目标应带query身份拒绝，不静默跳过。

辅助头为`Linear(514,64) → SiLU → Linear(64,29551) → Softplus`，预测归一化后计算
与目标的余弦距离，固定λ=1.0；原对比损失与候选CE权重保持。514维由512维分子嵌入
和两个加合物指示位组成，H/Na顺序沿用既定词表，未知ID2使用全零条件并保留query。
两层线性层实际构造新增1,953,775参数，低于登记的约200万预算；完整输出维度的合成前向
与反向已通过，这不是GPU显存或训练耗时测量。
辅助头使用独立CPU初始化随机流，前向不新增dropout，须验证原参数/RNG和λ=0退化等价。

λ=1.0是首轮固定工程选择，不是已验证的最佳权重；余弦loss有界不代表梯度一定小，
后续须记录主/辅助梯度和裁剪影响。条件内仍有碰撞能量、仪器及重复谱差异，辅助目标
仍可能损害检索。原始谱中38,596条query的最强峰在母离子20 ppm内，预测容易的母离子
信号也可能主导辅助loss；完整检索指标才决定是否晋升，不用谱预测loss代替。

## Parent、数据及启动条件

拟以已完成审计的A04为共同对照，从随机初始化训练；比较checkpoint仅用于fresh基线
评价，不继承A12/A13中途权重或未晋升设置。正式绑定前重新确认parent；若采用后续已
晋升模型，须先回填本卡并冻结其完整配置，不能在启动脚本中隐式切换。

继承seed42、batch128、质量block32、均匀CE16/权重1、lr1e-4、100轮上限/patience5
以及原增强/优化器/调度/裁剪。MassSpecGym v1.5全部194,119条train和19,423条有效val、
既定六条验证排除、自然Mass候选及二维身份保持，不用test调参。
SpecEmbedding质谱预训练共享结构不改，本卡不增加外部配对数据或预训练步骤。

启动前需完成：完整train目标覆盖与来源绑定；目标构造独立复算；原参数/RNG保持及
λ=0退化等价；梯度和真实合成优化更新；逐轮全量计数；严格checkpoint重载/关闭辅助头
后的检索一致性；正式入口、审计和独立worktree/配置/可写目录准备。均通过后才冻结
唯一配置、更新本卡并按既定GPU规则单次派发。

## 缓存与成本

新目标缓存放在外部项目根`/data1/${USER}/SpecEmbedding/`内独立版本目录，优先保存稀疏
bin/强度，避免按所有query持久化稠密目标。来源至少绑定完整train文件、原始query索引、
分箱边界/溢出策略、强度变换与构造代码；目标不可从验证/测试或预测embedding生成。
构造参数变化即停用失效版本，确认无活跃引用后按授权清理载荷并保留来源记录。
原固定图/候选缓存在指纹和只读路径不变时复用，学习embedding每轮重新编码。

0.1 Da全train稀疏bin/强度及指针载荷理论为50,495,264 bytes，稠密float32则约22.95 GB；
该估算未计加合物ID、query来源与文件头；下文记录了实际缓存容量。batch128的单份稠密
预测张量约15.13 MB，不是峰值显存。0.01 Da同结构头约1,924万参数，超出本轮预算；
0.1 Da是覆盖、分辨率与成本的折中，不称为GLACIER精确碎片模型的等价实现。

成本主要是正例辅助头、目标读取/搬运与反向传播；须分别记录准备时间、磁盘容量、
完整训练/验证时间和峰值显存。推理不调用辅助头是设计属性；端到端速度收益仍需实测，
不能把GLACIER逐候选预测谱的成本直接当成我们已测得的优势。

## 验收及延后条件

按现有完整验证Top-1优先、Top-k/MRR整体晋升规则判断，辅助loss下降不作为晋升依据。
完整训练和独立审计后更新同一条记录，未晋升则加删除线并保留结果；没有数字收益承诺。
若完整目标不能可靠绑定、异质条件使监督意义不足、预测头超出本机预算，或准备成本
显著高于现有更有证据的方案，则延后，记录具体原因后再决定是否恢复。

## 组件实现进度

2026-09-11已实现独立辅助头、稀疏余弦损失、只读目标缓存与CPU准备入口，中央配置为
未激活预设。初次90项组件/相关回归检查通过；补充完整维度反向与实际CPU入口检查后，
两个新增测试文件39项全部通过。检查包含独立稠密公式/梯度、原参数和RNG保持、λ=0
两步优化退化等价、辅助梯度回传、真实spawn两轮读取、完整query/token绑定和旧缓存拒绝。
正式模型构造仍拒绝未接通的辅助字段；尚未验证真实Trainer中的损失权重、逐轮计数、
checkpoint与检索一致性，不能据组件测试宣布正式训练可用。回执与源码指纹见阶段计划。

Ruff与绝对路径compileall通过；首次compileall相对路径因外部存储包装器切换工作目录
未找到源文件，已改成绝对路径复核，未把该次exit0视为成功。未重跑全仓pytest。

## 完整目标缓存

实现提交`0665618`已推送，输入准备固定于独立detached源码
`/data1/zyl/repos/SpecEmbedding-opt-a16-inputs-20260911/`。13:50:24完成全量构建、独立标量
目标重放及正式缓存加载，共189.12秒，仅CPU；13:52:38另外完成全部文件、稀疏行及
既有独立加合物缓存的逐query/token绑定读回复核，346项固定来源SHA一致。

目标位于`/data1/zyl/SpecEmbedding/spectrum_target_cache/massspecgym_v15_a16_20260911_topk256/`，
覆盖全部194,119条train及22,742个二维身份，共6,117,788个非零bin，二进制载荷
50,495,264 bytes、含来源记录总计122,487,663 bytes。7,301,230个原始峰中有10个零强度
峰，不占稀疏条目；没有删除query，没有溢出峰或float32下溢bin。全部目标平方范数与1的
最大误差为8.952e-8；该数值只验证目标归一化，不是谱预测或检索成绩。

准备、运行及独立回执集中于阶段计划。后续正式接入还须验证真实Trainer的目标搬运/
query对应、主/辅助损失和梯度、完整样本计数、checkpoint严格重载及关闭辅助头后的
检索一致性，再冻结独立正式训练源码并评估GPU成本。当前A12/A13运行源码未修改。

当前检索结果：**无**。没有新增模型队列、学习缓存或论文结论。
