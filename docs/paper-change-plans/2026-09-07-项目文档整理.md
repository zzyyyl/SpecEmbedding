# 论文修改计划：项目文档去重与事实校正

- 状态：`执行中`
- 创建日期：2026-09-07
- 最后更新：2026-09-07
- 负责人：Codex / 作者核验
- 关联论文：`paper/main.tex` / `paper/main_cn.tex` 及项目说明、投稿待办
- 计划约束：只整理文档并核对既有工件，不启动训练、推理或外部复现；保持全量重训计划暂停。

## 1. 修改目标与动机

删除重复或已被后续工作替代的文档，压缩长篇说明，修正当前实现、历史实验和发布快照混写的问题。
保留可运行入口、有效约束、未完成工作及证据来源，避免把精简变成结论升级。

## 2. 当前证据与问题定位

- README 的旧谱图检索示例、模型结构和多个中文入口重复；模型文档对温度、对齐损失、学习率的描述与代码不符。
- 8 月修改意见和已执行计划重复，ADMA 待办与转投计划包含过期日程、旧表格和失效状态。
- 9 月 7 日全量重训计划已记录：新版默认 top-256/no-forcing 与旧 20,000-query/top-40 运行不同；需以原始 cache、checkpoint 和实验索引核对，不能照抄论文协议描述。
- 发布 PDF 与匿名包各有固定 manifest，不能用旧 HEAD、页数或包体积描述当前源码。

## 3. 修改范围

### 3.1 涉及文件与章节

- [ ] 根目录 README、模型说明、AGENTS 和 `docs/` 当前文档：明确各自职责，合并重复命令与状态。
- [ ] 计划目录：删除已归档的 8 月长篇计划和原始意见，保留 Git 追溯索引；压缩在途计划但保留协议、用户决定和暂停状态。
- [ ] `paper/` 投稿文档：合并为一份当前待办，删除旧 ADMA 日程和重复实验表。
- [ ] 双语稿：核对已知训练/评价协议混淆；按可核验工件修正描述，不变更结果数字、模型定义或已用引用，清理未使用的 BibTeX 条目。
- [ ] `analysis/` 与 `reproducibility/` 说明：增加证据导航，删除被后续报告替代的待办，区分历史快照与当前状态。

### 3.2 明确不做的事项

不改模型/训练/评价代码、配置、原始日志、实验 JSON、冻结分析报告或已有匿名 ZIP；不删除外部参考论文、数据处理 notebook 或有效实验记录。

冻结报告若已在此前被手工追加说明而偏离 manifest，只移走重复说明并恢复可从 Git 校验的原冻结字节，不改 manifest、实验数字或结果。

## 4. 具体内容设计

运行流程集中于中英文入口；架构文件解释编码器，reranker 文件解释第二阶段；项目记忆只保留当前状态。
投稿待办引用在途计划和 manifest，不再复制结果表和哈希。论文区分实际训练缓存 forcing 与测试 no-forcing、top-40 结果与 top-256 方法配置；按已有表格纠正模型优劣、directed 已运行和均值/SD 描述，并压缩重复附录，不改变数字或扩大主张。匿名包 README 模板同步协议边界，现有 ZIP 作为历史验收工件保留。

## 5. 引用文献与真实性核验

不新增文献或改写 BibTeX 元数据；检查引用闭包及双语一致性，删除两稿均未使用的条目。删除计划中尚未核实的暂拟书目信息，保留后续核验要求。外部链接只作为已有资源入口，不新增外部事实。

## 6. 实验与计算边界

- 新增训练、评价：否；只读核验现有缓存、checkpoint、日志、索引和发布工件。
- 静态统计：文档规模、链接、引用和文件哈希。
- 文档/论文编辑：是；科学结果数值与研究范围保持不变。

## 7. 分步执行清单

- [x] 盘点文档、检查干净工作树，使用 CodeGraph 定位实现，建立本计划。
- [ ] 完成去重、事实校正、索引与双语同步。
- [ ] 检查链接、删除文件引用、计划状态、引用闭包和结果数字；按涉及范围编译双语稿。
- [ ] 检查冻结工件未被改写、`git diff --check` 与最终 diff。
- [ ] 先提交实质修改，再回填 commit 与验证结果，以独立文档提交归档本计划。

## 8. 风险、证据边界与待确认事项

旧报告中的“当前”只代表生成时点；冻结报告保留原始字节并在导航中说明适用范围。
如核验发现超出既知协议问题的新结论或需要新实验，暂停对应论文修改并按仓库规则处理；文档整理本身不恢复其他实验任务。

## 9. 验证方案

检查所有维护文档的本地链接、代码示例参数、引用闭包、数字及协议限定；核对 release 哈希。
双语稿有修改时执行正式构建并更新 release manifest。文档不改变运行逻辑，不新增单元测试。

## 10. 执行记录

- 2026-09-07：开始整理；基线为 `a2780dd`，工作树干净。确认历史实验索引中的 36 个主运行和 30 个消融均显式请求 20,000 条、train-k=40，继续核验 cache 元数据。

## 11. 最终结果

- 完成日期：尚未完成
- 验证结果：尚未完成
- 实质修改 commit：尚未提交
- 计划归档 commit：无需自我引用
- 相对原计划的偏差：无
