# SpecEmbedding 项目记忆

最后更新：2026-09-10。本文是内部状态摘要，不放入匿名补充包；事实以代码、配置和可校验工件为准。
运行入口见 [文档地图](README_zh.md)，模型细节见 [reranker 说明](reranker_solution_zh.md)。

## 当前实现与证据

当前默认实现为无 rank 的 `relative`：最多 256 个自然候选先做 pointwise coarse scoring，
再对 coarse top-40 计算谱图条件的反对称偏好；`pointwise` 为容量匹配对照。
统一 pipeline 默认所有 split 不 forcing。该默认值不代表历史实验配置。

[实验索引](../reproducibility/mentor2026_experiment_index.json) 中的 36 个主运行和 30 个
relative 消融均显式请求最多 20,000 条训练 query、train-k=40。它们复用旧 top-40 缓存，
训练含正例 forcing，验证/测试不 forcing；不能称为新版全量、全池 no-forcing 实验。
另有使用 pre-overlap-clean alignment 的 5,000-query/top-256 pilot 和独立官方候选顺序/二维
身份审计。各系列的来源与边界集中在 [分析索引](../analysis/README.md)，不混用其结果。

已有结果支持固定候选池内监督式 residual learning-to-rank 的收益，但 relative 相对
pointwise 的方向随候选协议、指标与 checkpoint 改变，独立普遍优势尚未建立。
保存 embedding 的候选/身份审计不是 fresh official-loader 重编码；所有评价名称须与工件匹配。
JESTR-style cosine 只是本地保存嵌入上的重实现打分控制，JESTR/GLMR 官方模型未统一复现。
其余论文主张约束见 [AGENTS.md](../AGENTS.md)。

## 在途任务

- [MassSpecGym 全量重训](paper-change-plans/2026-09-07-MassSpecGym全量重训.md)：**执行中，已获准转为单 baseline 优化；旧矩阵停止，论文更新暂停**。
  当前 processed 数据已核验为 v1。用户已授权采用 v1.5，并在审计证明必要时重训 alignment。
  旧监测器于 19:48 停止，未派发训练，原锁、日志和固定源码保留。
  旧构图的芳香性/键类型依赖 SMILES 表示，迁移需规范构图并重训一个 alignment-42；
  正式 alignment 已增加每轮全谱遍历与数量校验。首个 v1.5 队列于 20:17 在候选审计阶段停止：
  官方源含 RDKit 无法构图的候选，未启动 GPU，失败工件及 `6843e12` 固定源码保留。
  修正为显式记录现有候选编码器的无效图排除（保留源候选顺序/内容，正例和 query 不得丢失）；
  修复 `d15510b` 已推送并冻结新 worktree；r2 队列于 20:29 启动，但于 20:33 再次失败退出。
  本次候选已通过 SMILES 解析，随后生成 InChIKey 时触发 `KekulizeException`，不能直接归为
  既有无效图排除。21:54 核验进程已退出、未进入 GPU 阶段，日志与失败工件保留，未重启。
  用户随后要求停止任务、排查并修复。已定位 RDKit 解析与 InChI 转换的 Kekulé 搜索顺序差异；
  修复 `aecdfee` 仅转换分子副本并保留身份转换记录，不改候选或编码器图。
  完整 CPU 数据审计与独立文件比对已通过，验证进程已退出；详情见
  [修复记录](../analysis/massspecgym_v15_inchi_fix.md)。未启动新训练。
  2026-09-08 用户明确要求继续推进，r3 正式队列已于 00:05 在新独立 tmux 中启动，
  固定源码为 `15b1b5d`，与已验证修复的 Python 代码及配置一致；CPU 准备与审计已于
  00:38:30 完成，实测进入 `alignment42/waiting_gpu`，预处理子进程已退出。
  随后按用户授权实现多 GPU 择一等待（`ad81b61`）：各卡独立计时、固定 UUID、最终复查，
  子进程将选中物理卡映射为显式 `cuda:0`；每个 GPU 阶段重新选择，支持两个以上 GPU。
  23:55:48 再次确认尚未训练后主动中断 r3 等待入口，保留旧源码、日志、锁与工件。
  新固定 r4 队列于 23:55:52 启动，已核验并复制导入完整 CPU 数据，实测同时等待 GPU 0/1。
  23:58:03 经持续空闲窗口与最终复查选中物理 GPU 0，alignment-42 子进程已启动；日志确认
  严格 `cuda:0`、正式全量模式和全部 194,119 条训练谱图，复查已进入第 1 轮训练，
  GPU 进程记录与 UUID 绑定一致。此单个 alignment-42 完成后再运行 12 个全量 reranker 组合；
  不能称为三 alignment 矩阵。
  2026-09-09 复查：alignment、六缓存已完成；Mass relative 三 seed、pointwise seed42 的
  全量训练和完整测试完成，pointwise seed43 正在训练。结果仍为 cache-local exact-target-SMILES。
  用户要求排查准确率落差，已核对新旧实际权重哈希、六 cache 指纹、四 test cache 全 query
  标签/来源/点积及分散样本 GPU 重编码，未发现旧权重或磁盘 TokenSet 缓存复用。
  旧候选真值与干扰项的芳香性表示存在强格式捷径，已用完整候选上的无谱图解析规则量化；
  不能将旧高分直接作为新版应恢复的可靠基准，旧模型依赖程度仍需控制实验。
  详见[准确率排查](../analysis/massspecgym_v15_regression_audit.md)。该证据影响旧结果解释，
  此前计划设为待确认；随后用户授权先固定单 baseline 优化，并把目标更新为分子检索
  **Top-k 整体提高、MRR 为辅，各指标达到或接近同协议现有 SOTA**。已核验后中断 r4 的
  pointwise seed43 及后续矩阵，4 组完整结果与全部
  工件保留，seed43 的部分 checkpoint 不作为完成结果。外层状态为中断，内层仍残留 running；
  已确认进程组退出、专用 pane dead，不能据残留状态重启。下一轮固定 Mass/relative/seed42，
  保持 v1.5、全量、无 forcing 和双 GPU 择一等待；入口已收窄范围。
  A01 已实现完整验证候选重编码与检索选优，Top-1/MRR 选 checkpoint 并保存 Top-k 的 Pareto
  候选；优化分支不运行 test 或 reranker。固定源码 `cba38a4` 的 A01 于 2026-09-09 15:58:57
  在第 19 轮早停，四阶段完成、进程退出，独立 CPU 完成审计通过。选中 epoch14，Top-1 略升，
  其余 Top-k/MRR 相对 r4 下降；两个非支配候选均未通过整体改善标准，保留 r4 为 incumbent。
  同一 A01 轨迹内检索选优优于最低 loss 轮，但不能据此对跨运行差异作因果归因。
  独立完成审计入口已实现，可逐轮重算保存分数与排名、核对权重/选优和完整覆盖，
  按预设容差列出各 Top-k/MRR 改善及退步；完成回执、基线决策与运行位置见同一阶段计划第 10 节。
  主排名匹配 torchmetrics1.8.2 逐 query CPU argsort，稳定排序另列敏感性视图；
  实验卡与最新测试见[优化索引](../analysis/massspecgym_optimization.md)。调参仅看验证集，矩阵后置。
  用户要求每轮尝试的修改和结果用一句话写入根目录[attempts.md](../attempts.md)，已补齐A01–A03；
  进行中的条目标明核验时间与未完成状态，必要的单次详细说明使用`attempt_<n>.md`并链接。
  用户随后要求失败尝试加删除线；未达晋升标准的A01/A03已标记，保留修改、结果和原因。
  等待期间已实现 A02 质量邻近 batch：保持 batch128、模型/loss 不变，全谱每轮一次，
  记录顺序/质量指纹与唯一覆盖；完整 CPU 核验表明邻近质量负例显著增加，但身份多样性减少。
  实现 `fc42b27` 的干净固定源码与真实输入 dry-run 已核验；A01 完成审计和决策后，A02 于
  16:01:20 单次派发，16:05:28 完成 CPU 准备，验证索引哈希与 A01 相同；16:12:44 在物理
  GPU 0 上启动 alignment；18:09:37在第27轮正常早停，完整逐轮样本/分数/权重审计通过。
  A02 epoch22相对r4的各项Top-k/MRR均超过预设改善容差，已成为下一轮比较baseline。
  A02 随机初始化，r4 checkpoint仅用于比较；这是单seed完整验证改善，不是test/SOTA结论。
  两次固定基线验证的微小数值差异已独立核验，远小于决策容差，见优化索引 D02。
  计划要求的完整验证谱图置换/常量检查已实现并通过合成测试与全索引 CPU 预检，见 D03；
  尚无正式控制推理，输入构造检查不称为模型结果。
  完整 train CPU 诊断已区分多正例损失的熵下界与图扰动影响，见优化索引 D04；不凭 A02
  较高的训练 loss 判失败。A03 仅关闭分子图增强的开关已实现、测试并完成真数据 dry-run，
  谱图增强保持不变；最终固定A02 epoch22作比较、mass_blocks/block32，随机初始化。
  A03于18:32:27单次派发，18:36:36完成CPU准备；18:38:41通过持续空闲检查，在物理GPU0
  的严格cuda:0上开始baseline完整重编码验证，实际子进程UUID映射已确认。
  验证索引与A01/A02哈希相同，18:41:36验证完成，训练阶段重新等待；18:43:40启动alignment。
  19:08核验已完成5轮完整检索验证，入口/训练PID存活，逐轮样本计数和资源记录生成正常。
  首轮耗时/吞吐/参数/内存实测与中途指标只维护于优化索引A03，尚无最终收益结论。
  用户要求避免高风险提速；已按原始运行记录拆分单次尝试耗时，并在优化索引D08固定
  验证分子batch512→1024的完整数值/性能核验方案，尚未派发，等待A03结束后执行。
  当前训练batch、学习率和正在运行的源码不变；显存余量或条件耗时计算不作为实测提速。
  后续CPU并行化问题已核对实际4个验证worker、逐轮重复构图和embedding往返传输，D08记录
  增加worker、固定图缓存及本轮embedding留GPU的独立核验方向，尚未实施或宣称收益。
  正式 alignment 的逐轮资源记录与完成审计已实现，保存耗时、实际吞吐、参数量、CUDA
  分配峰值和主进程峰值 RSS；不含 worker 内存或其它 GPU 进程，不回填 A01/A02 未记录的峰值。
  实现与验证位置见优化索引A03；A02源码未修改，A03已冻结包含该实现的新源码。
  完整验证候选的 CPU Morgan 指纹可区分性诊断及独立复核已完成，见优化索引 D05；
  2048/4096 位的完全碰撞相同，不能据此推断训练效果相同。候选对比目标的原文核验见 R01，
  该次碰撞诊断没有派发指纹塔或新损失实验，也不生成可复用的训练指纹特征缓存。
  全量训练自然候选的紧凑元数据与独立源文件复核已完成，见优化索引 D06；所有query均有
  自然正例，重复二维负例和无标签候选结构与held-out目标的交集已记录，尚未用于正式训练。
  全量候选读取/不同二维负例采样工具`01024f5`通过新增23项测试及真实全量CPU采样预检；
  每条query保留，同身份正例排除、源位置、无重复负例和RNG均核验通过，详细统计见D06。
  诊断预算不是正式超参数，未改变A03。后续数据/损失/训练组件`bee5b63`已完成，详见D07；
  它逐query补充自然候选监督，正负图策略一致，不改变预训练架构或检索打分。
  新增23项组件测试及真实全量原始谱图/token/候选映射核验通过；随后`67787bc`接入正式
  配置/CLI/输入指纹与完整采样重放审计，新增18项集成检查通过，详见D07。
  已冻结A04独立源码、完成真实全量dry-run及与D07实际query映射的交叉核验，
  [A04实验卡](../attempt_4.md)已准备，仍等待A03完整结果后确定最终parent/配置，未派发。
  用户要求当次训练期间提前准备下一次；据此把实现、测试、CPU输入核验和源码冻结前移，
  完成后只做结果审计、唯一基线决策及最终指纹确认，模型训练保持单方案串行。
  `82c20dd`进一步接入完整CPU验证索引的显式复用，来源/配置与两端文件哈希严格核验，
  每轮模型embedding仍重新编码；真实全量复制约5秒，详细边界见D08。未使用的A04源码已
  更新并重新完成全量预检，全仓382通过、1跳过、1个相同既有路径审计失败；A04仍未派发。
  A03于20:40:48完成27轮并正常早停，原进程退出、pane exit0，独立CPU完成审计通过；
  选中epoch22及全部Pareto候选均存在整体指标退步，唯一决策为继续保留A02，详情见A03索引。
  衔接期间的验证batch512→1024→512测量已于20:45单次派发，20:47:11在物理GPU0上
  开始首组完整编码；测量通过预设门槛后才改变A04验证batch，否则保持512。A04模型未启动。
  三组完整测量及数值审计于21:03:50完成、入口正常退出，按预设规则采用验证batch1024，
  完整数值、耗时漂移和外推边界见D08。A04最终全量预检完成，于21:08:20单次派发，
  21:09:37完成CPU数据及验证索引导入；21:11:58通过持续空闲门槛，在物理GPU0的严格
  cuda:0上开始baseline完整验证，尚无新模型训练结果。A04固定源码`82c20dd`不得修改，
  运行位置、专用socket及指纹见阶段计划。
  A04随后于21:17:15在物理GPU1的严格cuda:0上开始训练，21:47:50完成前两轮完整训练/验证，
  原进程保持运行；中途成绩及资源见A04实验卡，尚未完成或晋升。用户进一步要求缓存从下一轮
  应用；固定验证图的无损存储、CPU全量构建/独立逐图审计及正式入口已实现，详见D09。
  新增29项检查通过，全仓411通过、1跳过、1个相同既有路径审计失败；实现`881cdfa`已推送并
  冻结为新A05源码，真实完整缓存构建及逐图审计于22:01完成，CPU图读取测量于22:14完成；
  测量结果与范围见D09，不是GPU训练速度结果。实际batch256的100轮全量采样预检、两个条件配置草案及A02条件parent
  的完整dry-run均已完成；尚未创建A05运行目录。
  [A05实验卡](../attempt_5.md)已准备：训练batch128→256、学习率不变，最终parent等A04完成审计；
  缓存作为单独工程改动记录，既不复用模型embedding，也不修改当前A04。
  22:35时A04已完成前五轮，第六轮继续；最新原始验证成绩及固定至第四轮的CPU中途审计
  分别见A04实验卡，不能把日志进展或中途审计写成最终晋升，A02仍为incumbent。
  新增原文及固定源码研究见优化索引R02：指纹相等标签与二维身份不同，Mass候选池也可能
  配有已知分子式输入，外部结果需逐项对齐；新论文已存档，没有启动外部模型或改变A05设计。
  完整训练流缓存容量分析及短时CPU/GPU观察见D10；扩大训练LRU收益有限，A05草案保持原容量，
  继续采用已审计的验证图磁盘缓存。A04在22:50完成第六轮，当前选优仍为第五轮；第七轮期间
  存在共享GPU计算，不能把整卡利用率或所有耗时归因于本任务与CPU。训练正常继续。
  后续轻量分子表示的固定Morgan输入准备机制见D11及[使用说明](fingerprint_cache_zh.md)，
  尚未接入正式模型；32项新增检查通过，全仓443通过、1跳过、1个相同既有路径审计失败。
  `93abdd8`固定源码的完整训练/验证指纹构建及逐位独立审计于23:41完成，原CPU pane退出0，
  完成后公共读取器对完整来源与文件指纹复核通过；规模/耗时见D11，原始回执位置见阶段计划。
  这批指纹用于后续模型输入准备，尚未接入A04或A05；A05下一轮采用的是D09固定验证图缓存。
  A04于23:37完成第九轮，原日志成绩继续改善，23:46核验原任务仍在训练；详见A04实验卡，
  最终审计仍待训练结束，A02仍为已确认基线，A05配置保持冻结。
  2026-09-10新增轻量分子塔及其完整训练/验证输入组件，见优化索引D12与[A06实验卡](../attempt_6.md)。
  21项新增检查通过，全仓464通过、1跳过、1个相同既有路径审计失败；尚未接入正式入口。
  下一步分别核验parent/候选架构配置并完成输入/审计绑定；A05仍为下一次正式实验，不改当前任务。
  A03独立源码为`c50ddf0`，正式预检与派发指纹已保存。
  固定 baseline 的完整验证错误诊断已保存，分组不能证明元数据特征的因果收益；额外的质量
  硬过滤会丢失真值，未实施。诊断结论与后续假设集中于优化索引 D01，未改变 A01/A02 配置。
  旧预训练权重的现存缓存与新划分 token 可逐条匹配，
  未见 test 二维身份交集，但缺历史指纹并使用过未排除六条记录的 validation，暂不直接接入正式训练。
  用户授权互联网研究及预训练权重使用、下游质谱塔/分子塔/rerank 结构与参数的逐项迭代，
  要求模型不过度复杂、能在本机训练；SpecEmbedding 质谱侧预训练模型结构不得改动。
  最新顺序为基础检索先达到或接近同协议SOTA，随后才用rerank强化；采用阶段计划既定的
  逐项验收标准，不因只改善某个指标提前切换。常规进展不再重复汇报是否使用rerank。
  用户要求构造参数/来源等变化后清理失效缓存：新run拒绝旧指纹，重建并审计新版本；确认
  没有活跃训练/准备/审计引用后清除旧载荷，保留来源与审计记录。有效固定输入可以跨轮复用。
  用户明确当前轮次不改，A05及之后启动改用外部项目根，数据和缓存不再写入home；
  [外部存储入口](storage_zh.md)已实现，下一轮基于原A05源码仅应用存储及子进程继承修复并冻结为`a5aeb3b`。
  新路径草案和完整条件dry-run于01:27完成，旧准备回执保留但不再用于后续派发；原始文件
  字节副本已核验，当前A04引用的原件未删除。实际位置、指纹及剩余启动门槛见阶段计划。
  A06真实全量CPU输入预检于00:33完成，见D12；这不是模型训练或性能结果。
  A04现已完整结束25轮并通过独立样本/负例/排名/权重审计，epoch20的全部Top-k/MRR均超过
  A02比较基线的预设改善容差，已晋升为当前incumbent；完整结果见优化索引A04和实验卡。
  `d6226ef`已将指纹塔接入正式入口、独立parent构建、完整输入绑定和完成审计，新增27项
  合成检查通过；正式训练和GPU性能待验证，不改变A05顺序。最新全仓501通过、1跳过、
  1个相同既有路径审计失败，固定下一轮源码的存储/正式队列相关29项通过。
  A06固定源码的完整A02条件CPU dry-run也已通过，所有训练/验证输入及parent独立配置均绑定，
  没有创建模型运行目录或GPU任务；A05仍为下一次实验，最终A06配置等待A05审计，回执见阶段计划。
  D14已完成全部train/val元数据及固定A04 epoch20分数CPU审计：母离子token原已包含，
  加合物完整、碰撞能量大量缺失且数值范围需另核验，未更改模型/缓存或提前确定新实验。
  逐指标强参考已记录到版本化JSON（见R04），附录Top-10明确属于其变体；同协议S_j仍待
  完整来源、分母和缺失MRR核验，不把已报告最大值或本地validation当作最终达标证据。
  D15补充核验MRR语义：当前MRR与Top-k共用首正例排名，保留非正余弦分数；不能混称为
  TorchMetrics独立MRR函数的topk/正值过滤结果。21项评价回归检查通过，100份完整已保存
  分数的敏感性审计及独立读回完成，冻结范围内的选优/Pareto/容差判断均未改变；范围见D15。
  该诊断没有改动生产评价或训练配置。
  A05已完成唯一A04 parent绑定和最终完整预检，于04:18:19单次派发，04:25:36在物理GPU0
  的严格cuda:0上开始训练，04:38:01完成首轮并继续训练。训练batch256、学习率不变，继承A04
  自然候选监督并启用固定验证图缓存；实际进程中的配置/UUID/外部缓存和临时目录均已核对。
  同权重完整baseline重编码的缓存前后分数差异已独立核验；首轮全量样本/顺序检查通过，
  资源表明本次省时主要来自验证，训练阶段本身未变快，细节与早期指标见A05实验卡。
  05:38:34已保存前6轮资源及完整验证快照，资源均记录train194,119/val19,423，原进程存活，
  当前选优第6轮；具体早期指标见A05实验卡，最终模型收益仍待完整训练及审计。
  A06已补完继承A04自然候选监督的完整条件CPU预检，复核126项parent工件及30项预检输入，
  尚未派发；最终parent/batch仍等A05结果审计。当前运行位置和准备回执集中在阶段计划。
  D16另完成指纹16/64/255负例预算的全量CPU输入诊断及独立读回，为后续扩大自然候选监督
  提供输入成本依据；没有改A05/A06配置，不能把输入张量尺寸或核验耗时当作GPU训练指标。
  06:15:30完成A05第9轮并刷新当前选优，前9轮资源记录均为完整train194,119/val19,423，
  原训练进程继续运行；完整验证指标集中于A05实验卡，尚未作最终晋升判断。
  D17补充保留各query母离子、移除碎片的输入对照，70项相关测试与全部19,423条验证输入
  的重新分词/母离子核验及独立读回通过；尚未运行正式模型控制推理，不能称为检索结果。
  已冻结独立对照源码，A05/A06源码和有效分子输入缓存均未改变；具体工件见阶段计划。
  06:40:34完成A05第11轮，继续训练，当前选优仍为epoch9。D17进一步完成A04 epoch20
  四种验证视图的checkpoint/完整图输入预检，重验126项A04工件；06:43:22在独立tmux中
  单次启动对照队列，真实PID及`normal/waiting_gpu`已确认，尚未开始推理。各阶段重新等待
  GPU0/1并完整审计保存分数，A06派发前先确认该对照队列终态；路径、锁和socket见阶段计划。
  06:53:25完成A05第12轮并刷新当前选优，完整样本计数及保存排名重算通过，具体指标见
  A05实验卡；原训练及独立对照等待进程均存活，尚未进入A05最终完成审计。
  07:10的只读现场采样确认A05所在GPU0另有不同进程组占用约8.2GiB；资源回执及解释范围
  见A05实验卡与阶段计划。不能将整卡利用率归给A05或将共享时段耗时视为独占卡性能，
  没有据此量化减速或重写此前首轮结果；现有训练和对照等待均保持原样。
  07:18:48完成A05第14轮并再次刷新当前选优，完整样本计数、query顺序及保存排名重算
  已核验；指标见A05实验卡。原训练继续，对照仍等待GPU，尚未进入最终完成审计。
  07:55:48已完成A05前17轮，当前选优为第16轮；两轮新增保存排名重算及完整计数通过，
  各项变化见实验卡，仍未作最终晋升。07:59:49单次启动独立CPU完成审计等待队列，
  原训练/入口及审计等待PID均已核验存活；队列绑定原PID/starttime，要求原进程退出且
  所有阶段完成后才执行一次审计，随后重验工件SHA和严格重载权重。23项固定输入SHA、
  13项生命周期合成检查及真实未完成运行的拒绝检查通过；当前仅waiting_parent，
  没有完成审计结果或A06派发。路径与指纹见同一阶段计划，A04对照仍等待GPU。
  08:20:49完成A05第19轮并刷新当前选优，前19轮全量计数及新增保存排名重算通过，指标
  见实验卡；原训练继续。08:24已归档A06最终配置绑定入口，32项CPU合成检查、真实两种
  batch配置继承及等待条件检查通过；它要求完整审计/对照、原进程退出和显式基线选择，
  仅执行最终CPU dry-run，不自动决定parent或启动模型。当前未创建最终准备/模型目录，
  运行位置与指纹见阶段计划；A04仍为incumbent，阶段目标未完成。
  08:46:20完成A04正常输入完整GPU复现及逐query分数审计，子进程正常退出，各项指标
  均在既定容差内；工件SHA、完整保存排名和query顺序独立读回通过，见D17。其余三种
  干预视图尚无结果，队列当前等待permuted阶段的GPU门槛；A05已完成21轮并继续训练，
  当前选优仍为第19轮，完成审计与A06最终配置绑定条件尚未满足。
  09:18核验A05原训练进程继续第24轮，前23轮完整训练/验证已完成；两个审计队列仍存活。
  R02补充已归档MS-MOLE的逐指标checkpoint选优边界和分箱谱图MLP参数核对，仅为后续
  设计依据，A06顺序与配置不变；没有把参考研究或等待状态记作新模型结果。
  A05于09:21:43在第24轮正常早停，选中epoch19；原训练进程已退出，24份完整计数和
  selection/checkpoint指纹读回通过。原入口继续全量负例重放，独立完成审计尚待入口结束，
  不能写作全流程完成或已作最终晋升；具体进展见A05实验卡。输入对照仍按原GPU门槛等待。
  A05原入口于09:32:15完成并退出0，既有独立审计队列自动接上CPU审计，09:43:55通过全部
  24轮负例重放、完整验证分数/选优检查及严格权重读回。131项工件及审计源码重新核验后，
  选中epoch19和全部Pareto轮次均未通过改善门槛，唯一决定为保留A04，A05已加删除线；
  完整比较见优化索引A05，指纹见阶段计划。A06确认继承A04的batch128及候选监督，
  实测最终绑定入口仍等待输入对照；原对照PID存活，其余视图仍等待GPU，A06未派发。
  D18已完成全部train/val有符号母离子—碎片差值输入审计及独立复核，原CPU进程退出0，
  token与D14/固定验证索引一致；大量query含负差值，不能默认裁零或据此删谱。R05已归档
  DreaMS原文和源码子集，区分编码差与差值编码；这两项仅提供后续设计依据，没有新模型
  成绩或预训练架构/缓存改动。统计、来源和范围见优化索引，回执见阶段计划；10:24原对照
  队列仍存活并等待GPU，A06未派发。
  A04四种输入对照于10:59:02全部完成且原队列退出0，完整保存排名及元数据独立复核通过；
  正常输入优于干预视图，但常量/母离子仍保留命中，不能把全部成绩归因于碎片，结果见D17。
  A06显式继承A04的最终全量预检通过，11:16:29单次派发；11:19:42完成数据和验证索引导入，
  11:25:58原PID、配置/输入指纹及外部存储环境核验通过，现等待基线验证GPU，尚无本轮模型结果。
  等待期间新增独立下游有符号m/z差值组件，默认关闭，可供A06之后的单方案选择；SpecEmbedding
  预训练共享类、A06固定源码、原始token与固定分子输入均未修改。设计和验证边界见优化索引D20
  及`docs/precursor_delta_encoder_zh.md`；尚未绑定A07或宣称模型收益。
  随后在检查A06完成审计衔接时，发现原入口把指纹训练的验证图缓存误与GINE比较基线混用；
  12:24:34在首次GPU阶段前停止原入口，无baseline/训练工件。修复`2e9ea2a`已推送，并只将
  该修复及测试接到原运行源码，r2固定版本为`65eecfc`，没有带入差值分支；其全仓505通过、
  1跳过、1个相同既有路径审计失败，相关43项检查通过。完整预检证明运行配置整体不变，
  12:36:24单次派发r2，12:39:36完成CPU导入并等待GPU，原PID3563758及实际输入/环境
  独立核验通过。旧队列/锁/工件和未派发的旧完成审计准备保留，不计为模型失败或新attempt；
  上述为r2历史现场，当前运行以A06实验卡和阶段计划中的最新切换记录为准。
  r2专用CPU完成审计队列已于12:46:01单次启动，原PID3565443于12:48:06核验存活且CUDA
  不可见，等待训练入口退出及全部阶段完成后审计一次；尚无完成审计结果，不自动晋升或启动
  下一轮。原训练PID3563758继续等待GPU，具体socket和来源指纹集中在阶段计划。
  运行根、唯一socket、PID/starttime和回执集中于阶段计划，模型及剩余验收见A06实验卡。
  用户随后授权忽略利用率并缩小显存余量；13:12:06在首次GPU阶段前停止r2及其审计等待，
  保留所有旧工件。r3于13:16:08单次派发，仍使用65eecfc固定源码及同一模型/输入/训练设置，
  完整预检只允许准入参数变为10,000 MiB/100%，轮询30秒、持续120秒和最终复查保留。
  33项相关测试及100%利用率、显存阈值边界的合成检查通过；当前实际阶段和完成审计绑定
  以计划最新记录为准。r3于13:23:24完成基线验证，13:26:24在GPU1启动训练子进程，真实
  显式cuda:0/UUID/配置绑定已核验；专用完成审计队列已确认纯CPU并绑定新入口。暂无本轮
  模型结果。本次是资源等待策略变更，不计为失败模型或新增attempt。
  用户进一步固定后续核心为双塔共享嵌入与对比学习，按嵌入相似度检索；允许借鉴GLACIER
  及近期语言模型的轻量注意力、归一化和残差。已核对原始论文并记录可迁移范围与优先次序，
  见优化索引R06；它们是后续研究假设，尚未绑定下一轮或改变正在运行的A06。
  A06训练期间已将谱图有符号母离子差值残差登记为[A07](../attempt_7.md)，状态为准备中、
  未启动，parent待A06完整审计后确定；衔接/差值/审计/存储70项合成检查通过，真实A04/A06
  条件构造和A05已审计决策复核通过，未把运行中的A06选为parent；旧parent默认路径显式
  改绑当前外部存储模板，完整自动衔接入口尚在准备。用户要求此后每轮都提前写入`attempts.md`和`attempt_<n>.md`，
  已纳入AGENTS；启动/完成后更新，未晋升条目加删除线，不把待选方案写成已运行成绩。
  14:08:07 A06 r3完成全部9轮的独立审计与严格权重重载，唯一Pareto候选epoch4未晋升，
  保留A04；完整检索与资源结果维护于A06实验卡，attempts已加删除线。A07固定源码36e1b0c
  的条件及最终全量预检通过，前序原进程与209项依赖工件复核后绑定A04；14:20:38单次派发，
  14:22:33完成数据/验证索引导入并进入双GPU等待，实际配置、完整计数和外部存储环境核验
  通过。14:29:51独立CPU完成审计队列单次启动，14:31实际进程/纯CPU环境/原入口绑定核验
  通过，13项生命周期检查及未完成拒绝探测通过；训练入口仍等待GPU。准备、决定和启动
  回执集中于阶段计划；新模型成绩、完成审计和最终SOTA目标仍未完成。
  A07等待期间已预登记[A08](../attempt_8.md)并实现独立下游Q/K RMS归一化，训练、推理
  和inference_mode使用同一路径，保留两塔已有初始权重/RNG；默认及预训练源码不变。
  125项相关检查通过，全仓590通过、1跳过、1个相同既有路径审计失败；真实A04及含A07
  差值分支的条件构造通过，固定源码/全量预检和最终parent绑定仍待，没有第二个模型队列。
  Qwen3版本化原论文已下载并核验标题，具体来源与SHA见阶段计划，不涉及外部模型执行。
  A08随后冻结为9c2839e并于15:06:51通过A04条件的真实全量预检，未创建模型运行目录；
  最终parent仍待A07审计。A07已于14:49:22完成baseline并在14:51:48于GPU0启动训练，
  15:14前两轮完整训练/验证计数正常，真实子进程UUID/严格cuda:0及配置绑定已核验；中途
  成绩和资源只维护于A07实验卡，尚无最终模型收益或晋升结论，独立完成审计队列继续等待。
  15:48已核验A07前4轮全量计数及新增排名重算，三个原进程PID/starttime匹配且存活，
  独立完成审计仍等待。A08仅CPU的最终配置绑定入口已归档，21项合成检查及真实等待
  探测通过；没有创建最终绑定或模型目录，也尚未部署自动GPU衔接队列。最终parent和
  正式预检仍依赖A07完成审计，细节见实验卡和阶段计划。
  D21完成已审计A04/A06完整验证排名交集诊断及独立读回，命中集合差异仅为后续特征
  融合提供研究线索，不证明新模型有效，不改变A06未晋升决定或当前实验顺序。
  16:10已核验A07前6轮完整计数及新增排名重算，原训练/入口/审计进程继续存活，尚未
  完成或晋升。A08专用CPU衔接队列于16:08:34单次启动，真实PID/starttime、专用pane和
  完整外部存储/空CUDA环境均独立核验通过，等待A07完成审计后绑定唯一parent并最终
  预检、单次exec正式入口；没有A08模型目录或成绩。19项合成检查含独立tmux真实传参
  通过，首次脚本命名遮蔽标准库的CPU预检失败日志保留，修复后未改变模型或数据协议。
  运行位置与回执集中于阶段计划；A08实际启动后仍须核验设备并绑定自己的独立完成审计。
  A08之后已预登记[A09](../attempt_9.md)，保留GINE并加入固定Morgan的小型零初始化分子
  残差。独立组件22项检查及真实参数构造通过，原两塔初始权重/RNG和预训练源码保持；
  组合输入、正式入口及独立审计接入待做，当前新模型类型被正式构造器拒绝，没有A09
  训练或队列。主机全仓612通过、1跳过、1个相同既有路径审计失败；首次沙箱多进程
  socket限制导致的超时经核验后改在主机重跑，未改测试或正式训练。依据与原论文归档
  见R07及阶段计划；图/指纹互补仍是假设，A07/A08原队列与实验顺序保持。
  16:45核验A07已完成前9轮，新增完整query顺序及排名重算通过，原入口/训练/完成审计
  与A08等待进程仍存活；最新日志选优第9轮，中途成绩只维护于A07实验卡。仍未完成本轮
  审计或晋升，A04保持incumbent，A08最终绑定/派发和A09正式接入均未完成。
  A09随后完成组合输入读取器及排名快照绑定，新增22项合成检查通过，保留原图/谱增强、
  负例采样RNG及两类固定缓存格式；准备组件的初始化改为与真实fresh入口一致，直接
  捕获原入口模型核对权重/RNG。相关65通过、全仓634通过、1跳过、1个相同既有路径
  审计失败，静态检查通过。正式入口/全量预检/完成审计仍待，尚无A09运行或结果。
  17:35已核验A07前13轮完整计数及新增排名重算，中途成绩只维护于A07实验卡；原训练
  与两个等待队列继续运行，A07/A08固定源码保持干净，没有新派发或晋升。
  A09正式训练、独立baseline和完整完成审计接入随后通过合成检查，完整三类模型输入
  分离、候选重放、真实小型入口训练及严格重载已验证；全仓668通过、1跳过、1个相同
  既有路径审计失败，静态检查通过。真实全量预检、最终parent绑定与正式训练仍待；
  这不代表MassSpecGym新成绩或最终SOTA达标，细节继续维护于原A09实验卡和阶段计划。
  D19另完成已下载外部Formula参考的完整query覆盖审计：两文件缺失集合均精确对应官方
  test的仪器类型空值记录；这是比较条件证据，不确定上游原因或Mass主表分母，不改变
  本地训练/评价或SOTA门槛。独立复核通过，具体范围和工件见优化索引及阶段计划。
  参考论文放入用户指定的论文目录；对标来源、可比性、近似达标阈值、下载位置与执行路线
  统一见上述计划。SOTA 目标未完成，不因当前单 seed 或局部指标改善而宣布达标。
  运行路径、独立 socket、状态入口及验证记录集中于计划。
- [NPLIB1 增强](paper-change-plans/2026-09-05-NPLIB1跨数据集增强.md)：计划状态为执行中，
  数据处理、重叠审计与实现 pilot 已记录，正式 GPU 矩阵尚未完成。该数据与 MassSpecGym
  有明显训练身份重叠，定位为第二套 in-domain 派生协议；去重 zero-shot 仅作小样本敏感性分析。
  中断的 CPU alignment 工件已隔离，不用于论文。
- [GLACIER 交接](glacier_reproduction_handoff_zh.md)：代码、权重与开放输入已下载；尚未
  安装独立环境或完成推理。来源与缺口见 [manifest](../analysis/glacier_reproduction_manifest.json)。

这些任务的状态不因文档整理而自动恢复或完成。正式训练必须全量且 GPU-only，详见仓库规则。

## 存储与发布

用户约定的外部工件路径：代码位于 `/data1/zyl/repos/<repo_name>/`，模型数据、权重及推理
工件位于 `/data1/zyl/<model_name>/`；例如 GLACIER 分别为 `ms-pred/` 与 `GLACIER/`。
本项目外部数据卷为 `/data1/zyl/SpecEmbedding/`。下载前记录来源、版本/commit、校验值和日期，
不把数据或外部仓库复制进当前工作树。

双语稿、参考文献和 PDF 在 `paper/`；页数、源版本和 SHA-256 只维护于
[release manifest](../paper/release/build-manifest.yaml)。现有匿名 ZIP 是已跟踪的历史交接
工件，其验收仅适用于 [验证记录](../reproducibility/anonymous-supplement-validation.yaml)
指定的源码版本，不代表最新代码。后续发布须重建并独立验收，细节见
[复现说明](../reproducibility/README.md)。目标 venue、作者/COI 和最终联合匿名检查仍见
[投稿待办](../paper/TRANSFER_2026_PLAN.md)。
