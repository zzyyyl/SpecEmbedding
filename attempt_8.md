# A08：谱图注意力Q/K归一化

- 状态：**训练中**。A07完整审计未晋升后已绑定A04，19:39核验原队列同一PID已exec正式
  入口；19:47:20在GPU1启动训练，严格cuda:0及完整输入已核验，来源见计划第10节。
- 唯一主要改动：在谱图Transformer每层的Q/K线性投影后，分别沿每头特征维做RMSNorm，
  再计算原缩放点积注意力；V、输出投影、残差、前馈网络、池化和双塔对比检索保持继承。
- 假设：约束各头Q/K幅值可能改善峰间注意力分配及训练稳定性。当前没有证据证明既有
  checkpoint存在注意力饱和，也不预设此组件能提高检索成绩。

## 依据与实现范围

[Qwen3 Technical Report](https://arxiv.org/html/2505.09388v1)第2节描述QK-Norm用于训练稳定性；
[Transformers的Qwen3实现](https://github.com/huggingface/transformers/blob/main/src/transformers/models/qwen3/modeling_qwen3.py)
对Q/K投影结果按head dimension做RMSNorm。本轮只借鉴这一局部构造，不整体复制其语言
模型结构，也不将外部语言任务收益写成质谱证据。
组件实现和兼容边界见[说明](docs/qk_norm_encoder_zh.md)；版本化原论文已下载至用户指定
论文目录，来源与SHA集中于阶段计划第10节。

本地设置拟为`qk_norm: {eps: 0.000001}`，每层Q和K各有一个head_dim大小的可学习缩放
向量，同层各头共享，初值为1；保留原`1/sqrt(head_dim)`缩放。当前512维、16头、4层
增加256个参数，属于构造预算，实际显存与耗时仍待测量。不同时加入门控、RoPE、GQA、
SwiGLU、不同分子塔或新的损失。SpecEmbedding预训练源码和默认构造路径不改，仅为新
下游实例显式启用，完整模型配置进入selection和checkpoint来源校验。

实现须检查训练、eval和`inference_mode`都执行同一Q/K归一化，避免框架推理快路径跳过
自定义层；padding仍排除，保留双向注意力和碎片排列语义。新模块可与A07差值分支组合，
是否继承该分支由A07完整审计决定，不在同一尝试里同时加入两个未经晋升的改动。

## Parent与固定条件

A07全量训练及独立完成审计、严格权重重载、全部Pareto复核和原进程结束后才绑定parent：
选中模型符合既定整体改善规则则继承A07；全部候选未通过则保留A04；若只有非选中候选
通过，先解决checkpoint与selection来源绑定，不能直接自动晋升或混用配置。

从随机初始化训练，parent checkpoint仅作比较。继承最终parent的GINE、batch128、质量
block32、学习率1e-4、weight decay1e-4、温度、谱图/图增强、候选CE16/权重1、100轮上限
和patience5。保持v1.5、Mass自然候选、seed42、194,119条全量train、既定排除后的
19,423条有效val，二维身份、无forcing、Top-1优先及MRR次序选优，保留全部Top-k Pareto
候选；优化不使用test。GPU0/1择一映射显式cuda:0，延续10,000 MiB、不限利用率、30秒
轮询和持续120秒；最终预检结合实际模型成本复查准入，不宣称显存可用等于算力独占。

## 缓存、启动与验收

Q/K归一化不改变tokenizer、原始谱图、规范分子图、候选身份或固定指纹，核验来源后继续
复用固定输入缓存；学习embedding随权重每轮重新编码。所有新工件和临时数据仍经外部
存储入口放在项目数据根，参数真正改变时按缓存构造指纹失效处理，保留活跃任务的引用。

准备检查包括独立注意力公式比对、训练/推理一致性、掩码和排列、两塔与归一化参数梯度、
默认权重/RNG兼容、带/不带差值分支的严格重载、独立baseline构造与完整输入预检。正式
运行记录完整计数、Top-1/5/10/20、MRR、每轮时间和显存，完成后独立审计全部保存排名及
选优。按现有整体改善规则晋升；失败项加删除线，保留结果和原因。当前中途结果见末节，**尚无完整结果**。

2026-09-10准备进展：新增组件及独立baseline保护已实现，125项相关合成检查通过；全仓
590通过、1跳过、1个相同既有路径审计失败，Ruff、compileall及diff检查通过。检查覆盖
两塔/归一化梯度、完整合成训练入口、负例重放、严格权重重载、默认权重/RNG和推理快路径。
按真实A04配置构造为9,047,241参数；保留A07差值分支的条件构造为9,121,737参数，后者
只检查结构，不代表A07已被选为parent。随后固定源码`9c2839e`的A04条件全量CPU预检于
15:06:51通过，确认194,119条train、19,423条val、独立A04 baseline构造和有效固定图缓存，
未创建训练目录或GPU任务。最终parent与完整派发配置仍须等待A07审计；若A07晋升，
需用其正式selection/checkpoint完成相应最终预检，不能把条件构造当作已绑定。

15:48已归档仅负责最终parent绑定和CPU全量预检的入口，来源及回执见阶段计划第10节。
它绑定A07原入口、训练和独立审计的PID/starttime；三者结束且完整审计、严格重载和全部
工件指纹通过后，才应用既定晋升规则并继承唯一parent。只允许已审查的模型增量、parent
来源及外部配置位置变化；数据、候选协议或其它阶段命令出现差异时拒绝并保留现场。
21项合成检查和Ruff通过，覆盖两种parent、等待状态、预检失败及意外协议/命令变化。
实际只读检查确认A07三原进程仍存活，没有创建最终绑定或模型目录。这是衔接入口准备，
当时尚未部署自动GPU派发队列；最终真实预检、单次派发及运行核验仍待A07完成审计。

后续衔接方案：使用A08专用detached tmux中的单个CPU等待进程，等待A07原入口、训练
和独立审计全部结束，完整核验后执行一次最终配置绑定及全量CPU预检，再用同一PID
`exec`已核验的正式入口。正式入口仍按原GPU0/1显存规则逐阶段等待。永久单次启动记录
及已有目录保护保持；任何失败、歧义或来源变化均保留现场并停止，不重试或继续派发。
CPU等待、请求exec与真正开始训练分开记录，实际进度以正式运行状态、阶段日志及进程
为准；此队列只衔接A08，不自行选择更多模型。该实现19项合成检查及独立tmux真实传参
检查通过，真实CPU探测确认A07仍存活且A08未派发；部署状态与指纹见阶段计划第10节。

16:08:34该衔接队列已单次启动，16:10独立核验其原PID/starttime、专用pane、空CUDA
可见列表及完整外部存储环境通过；三个A07原进程仍存活，状态为waiting_parent，没有
最终绑定或A08模型目录。此前首次准备因`queue.py`遮蔽标准库而在真实CPU预检失败，
已改名为`successor.py`并使用新的r2准备目录；旧日志及指纹保留，没有启动过失败模型，
不另计attempt。A08实际启动后还需核验设备/输入并绑定本轮独立完成审计，不能继承A07
的完成审计结论。当时没有A08检索成绩或最终parent选择。

## 派发前的衔接修复

19:04:52已单次部署r3等待队列，19:06独立核验通过。准备A09时用真实完整预检清单
检查继承逻辑，发现原A08最终检查对所有阶段读取`command`，但`import_v15`和
`import_validation`是由入口处理的CPU导入阶段，只有source/output；原脚本会在最终
预检后触发`KeyError('command')`。原合成夹具仅有命令阶段，未覆盖该结构。

修复只允许上述两种明确的CPU导入阶段保留原清单，其余无命令或意外字段仍拒绝。
25项最终检查通过，包括真实条件清单逐字段一致、错误阶段拒绝；新队列19项检查通过，
含隔离tmux真实传参。旧队列停止前后均确认尚无最终绑定、runner exec或模型目录，
通过原独立socket的指定pane中断，锁、日志和源码保留；替代队列使用新目录与socket。
第一次替换辅助程序因本机缺少pidfd API在发送信号前退出，复查旧进程仍活跃后采用
显式pane的Ctrl-C，相关准备记录均保留，不删除单次启动锁或重试模型工件。

新的等待进程继续绑定原A07训练/入口/审计身份；模型、输入、GPU门槛、最终parent
规则及固定训练源码9c2839e均未改变。这是运行基础设施修复，不计新attempt或失败模型；
A08仍未训练，后续仍须核验正式设备/输入并绑定其独立完成审计。

## 最终绑定与正式入口

A07完成21轮训练及独立审计后全部保留候选未晋升，本轮已绑定A04。原r3衔接队列在
19:37开始最终绑定并通过真实全量预检；19:39主机核验同一原PID/starttime已执行正式
入口，运行manifest与最终预检逐字节一致。实际配置只比A04增加Q/K RMSNorm，未继承
A07差值分支；训练参数、增强、tokenizer和候选协议均重新核对一致。

完整数据与验证索引导入后，已进入baseline_validation的GPU0/1等待；继续使用既定
10,000 MiB、利用率仅记录、30秒轮询、持续120秒及逐阶段复查。此时训练尚未开始，
不能把基线评价、CPU导入或进入等待记为A08模型成绩。实际完成审计已按本run清单准备，
重新核验23项输入、13项生命周期检查及“运行未完成则拒绝审计”的真实探测通过，
独立队列启动后等待原正式入口完成；具体绑定与部署证据见阶段计划第10节。

19:44:50完成A04基线重编码，19:47:20经训练阶段再次等待后选择GPU1；实际训练PID、
UUID、严格CUDA检查及运行配置已核验。日志确认train194,119、val19,423、batch128、
block32和CE16，19:48:29进入stage2。独立完成审计队列也已单次启动，20:00前核验
为CPU等待，绑定原入口与23项不可变输入；尚未执行本轮完整完成审计或作晋升决定。

## 中途验证记录

20:04独立重算首轮及本轮A04基线的完整验证排名通过，query顺序、候选来源和全量
训练/验证计数一致；本轮A04基线与原选中分数的各项差异均在既定容差内。

| 已完成epoch | Top-1 | Top-5 | Top-10 | Top-20 | MRR | 整轮秒数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 4.10% | 16.47% | 28.93% | 43.06% | 0.116437 | 760.51 |
| 2 | 5.36% | 20.32% | 35.68% | 48.31% | 0.140779 | 790.66 |
| 3 | 6.90% | 25.53% | 40.86% | 54.06% | 0.170049 | 778.67 |
| 4 | 8.74% | 28.18% | 42.66% | 55.27% | 0.190170 | 788.43 |
| 5 | 10.74% | 30.78% | 45.81% | 56.96% | 0.214352 | 769.85 |
| 6 | 8.87% | 27.76% | 43.75% | 56.31% | 0.191561 | 779.69 |
| 7 | 9.99% | 30.24% | 46.34% | 60.34% | 0.209719 | 787.16 |
| 8 | 11.14% | 31.67% | 47.59% | 59.35% | 0.222041 | 801.77 |
| 9 | 12.03% | 34.33% | 48.78% | 60.98% | 0.235678 | 727.93 |
| 10 | 10.15% | 31.76% | 46.97% | 62.86% | 0.214083 | 627.64 |
| 11 | 13.24% | 34.30% | 50.21% | 62.08% | 0.243354 | 657.02 |
| 12 | 12.49% | 35.83% | 51.91% | 63.85% | 0.243305 | 735.92 |
| 13 | 12.66% | 36.93% | 51.64% | 64.89% | 0.245338 | 787.20 |
| 14 | 11.84% | 34.51% | 50.87% | 63.95% | 0.234727 | 788.03 |
| 15 | 12.98% | 37.39% | 52.71% | 64.64% | 0.253071 | 772.10 |
| 16 | 13.58% | 39.30% | 53.26% | 65.60% | 0.259790 | 740.43 |

首轮20:01:10完成，参数9,047,241，PyTorch分配峰值3,933,501,440 bytes、预留峰值
7,277,117,440 bytes。仅为首轮实测，GPU同时有其它任务，不作为完整训练峰值、组件
提速或最终质量结论；本轮仍在训练，最终选择与晋升须等完整审计。

后续第2–4轮的全部19,423条排名已独立重算，并核验每轮194,119条训练计数、原训练PID
和资源记录；第4轮20:40:28完成。20:53主机复查原训练/入口、独立审计及A09等待进程仍
活跃，两个固定源码worktree保持干净；没有本轮完成审计或晋升决定。

21:54继续使用本轮固定源码独立重算第5–9轮全部验证排名，确认每轮194,119/19,423计数、
原训练PID、GPU UUID与资源记录一致；第9轮21:44:54完成。21:46宿主机复查原训练、
完成审计及A09唯一衔接进程均活跃，21:53两份队列状态仍为等待前序；本轮尚未完成，
中途最优未超过A04，不据此提前改变选优、早停或晋升规则。

22:24继续独立重算第10–12轮全部验证排名，核验每轮训练/验证计数、原PID、设备和
资源记录一致；第12轮22:18:35完成，目前按Top-1/MRR选择的中途最优为第11轮。
仍未完成本轮训练或晋升审计，具体后续进程状态见阶段计划。

22:57新增第13–14轮全部保存分数的独立排名重算通过，完整计数、原训练PID、GPU UUID
与资源记录一致；第14轮22:44:50完成。按预设Top-1/MRR顺序的中途最优仍为第11轮，
训练、最终选择与完成审计仍待，不改早停规则或据中途成绩晋升。
23:01:10主机复查原训练和等待队列仍存活，三个固定源码保持干净；日志已记录第15轮
完成及早停计数4/5。第15轮尚未在此独立重算，不作为已完成本轮或最终结果。

A10衔接组件归档后，新增第15–16轮全部验证排名的独立重算通过，输入指纹、原训练PID/UUID和完整
计数均匹配；第16轮23:10:02刷新本轮Top-1最佳，原训练继续。中途最优仍未通过相对
A04的整体改善标准，不提前改早停、晋升或派发顺序；正式结论继续等待整轮完成审计。
