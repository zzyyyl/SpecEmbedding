# A13：用加合物类型条件化谱图峰表示

- 状态：**第二优先，未启动**。2026-09-11调整为固定A04共同对照，待本项目GPU槽位
  空出并完成批次隔离预检；不自动继承A12权重。原条件组件保留，尚无模型结果。
- 唯一主要假设：显式提供观测加合物类型，并在谱图峰表示上做小型条件仿射变换，可能
  帮助同一个谱图塔处理不同离子形式，改善完整验证Top-k/MRR。

## 依据与固定改动

代码中的Tokenizer已把母离子m/z作为首token，但输出和谱图塔入口均没有独立adduct字段。
完整train/val输入审计D14记录两种已知类型且字段完整，支持先研究这个可用输入；
组间成绩差异本身不能证明加入元数据有效。

[MSAlign v1附录B](https://arxiv.org/html/2605.19752v1#A2)研究加合物与碰撞能量编码，
Table 8采用另设formula split，不把其增益转移到本地官方structure-disjoint划分。
[FiLM（AAAI 2018）](https://ojs.aaai.org/index.php/AAAI/article/view/11671)研究条件仿射
变换；本轮借鉴其结构，不是视觉推理模型复现，也没有已知的本地质谱收益。

优先级理由：D14完整输入审计确认194,119条train和19,423条有效val的adduct均可用，
无未知类型；现有谱图塔未显式接收该字段。MSAlign v1 Table 8的仅adduct与无元数据
R@1为52.38%/51.67%，增幅仅0.71个百分点，不把两种元数据的联合增益归给adduct。
这提供有限外部消融支持，低于A12的直接本地证据，但高于未定位瓶颈的通用结构改造。
输入审计来源为`audits/query_metadata_a04_e020_20260910/receipt.json`；分组成绩受组成
混杂，不能证明FiLM收益。仅新增2,048参数和字段查表，仍需实测整轮时间与显存；若
完整结果不晋升则停止这一配置，不擅自增加碰撞能量或更复杂专家网络。

只在现有峰embedding之后、第一层Transformer之前，对每个有效token使用
`h' = h * (1 + gamma[a]) + beta[a]`，包括母离子token；之后沿用parent的Transformer、
均值或已晋升的注意力汇聚、decoder与共享空间投影。两张参数表均零初始化，初始为恒等
变换且不消耗额外随机数；在继承宽度512时新增2×2×512＝2,048个参数。

固定词表顺序为`[M+H]+`、`[M+Na]+`，未知/缺失使用显式ID 2并固定为恒等变换，不增加
第三组可学习参数；所有未知来源和数量单独记录，不丢query，不从SMILES/候选/真值
推断加合物。若实际train出现与已审计输入不符的字段或顺序，先排查来源。
未提供adduct张量或出现非法ID应拒绝，不能默认为某个已知类型。

不同时加入碰撞能量、仪器编码、额外token、母离子质量换算、专家模型或辅助损失。
分子塔仍独立编码候选，最终只比较两塔归一化嵌入；SpecEmbedding预训练共享结构不改。

## Parent、训练与验收

Parent固定为已审计A04 epoch20，继承原两塔、均匀CE16采样和权重1；只新增此条件层
与观测输入。下文旧A12后继的绑定组件记录保留作历史准备，不作为新队列的直接部署依据。
从随机初始化完整训练，parent checkpoint仅用于fresh完整验证比较。

固定seed42、batch128、质量block32、CE16、学习率1e-4、100轮上限/patience5，保留
原增强、优化器、调度和裁剪。MassSpecGym v1.5全部194,119条train、既定六条排除后的
19,423条val、Mass自然候选与二维身份不变；不提供真值分子式，不用test选参数。

先验证恒等初始化下旧权重/RNG及输出、已知类型的非零调制与梯度、未知恒等/零梯度、
padding、排列、分块、严格重载及缺失输入拒绝。再接通两类分子输入的实际训练/验证/
完成审计，逐query绑定加合物并核验全量计数；正式接入前构造器必须拒绝未支持的开关。
完整审计后按现有整体Top-k/MRR晋升规则判断；加合物分组只作描述，不替代整体验收。
失败尝试加删除线并保留结果，本轮不承诺改善幅度或SOTA达标。

## 缓存、来源与资源

新建带源数据/映射/构造代码指纹的谱图元数据输入，按原始query索引绑定adduct ID，
准备目录在`/data1/${USER}/SpecEmbedding/spectrum_metadata_cache/`。保留原峰token数值
和顺序，不能把没有adduct来源的旧完整输入回执当作本轮已验证输入。固定图、Morgan、
候选索引及数值未变的峰token载荷可核验后复用；涉及新字段的组合输入需新版本及回执。
权重改变后全部学习embedding重新编码，不复用前轮学习表示。

元数据构造或映射变化时停用失效版本，确认无活跃引用后按既有授权清理载荷并保留审计
记录。当前活跃源码和缓存不修改。新层不增加候选图编码数量；GPU峰值、耗时和门槛在
实际准备中评估，不能以参数少宣称实测零成本。继续显式cuda:N及双GPU择一等待。

当前结果：**无**。

## 组件准备进展

2026-09-11已实现独立`AdductFiLM`和谱图包装器，并在`params.yaml`登记未激活预设；
当时正式构造器仍拒绝该开关，训练/验证尚不接收新字段。原SpecEmbedding预训练源码未改。
新增24项及相关既有检查合计95通过，覆盖Q/K与注意力汇聚组合、恒等初始化及原权重/
RNG、独立仿射公式和梯度、两步优化、未知恒等、padding、排列/分块、严格保存重载、
缺失输入与未支持正式配置拒绝；宽度512的新增参数数目核验为2,048。

Ruff、全仓compileall和diff检查通过，测试与临时缓存均在外部项目存储根。测试回执的
自文件SHA误记录为空文件哈希，原件保留；另行更正回执固定实际原件字节，并重算测试
日志和7个源码文件，共11项一致，无需重跑已成功且源码未变的测试。来源与更正SHA集中
于阶段计划第10节。FiLM官方论文PDF及来源元数据已归档和核验。

在上述独立组件节点，全量query元数据绑定、训练/验证与完成审计接入及真实条件预检
仍待；后续进展见下文，没有本轮固定训练worktree、队列或模型成绩。

## 完整输入路径准备

随后实现版本化train/val加合物缓存：按原始query保留观测值、显式ID、标识及峰token
指纹，来源包含数据manifest、完整split、构造/Tokenizer源码、配置和依赖版本；独立
重新读取全部源query后才发布可用回执，拒绝已有输出及读取期间的文件变化。
`prepare_adduct_cache.py`只进行CPU输入准备，不生成学习embedding或启动模型。

训练按原Dataset的二维身份分组反查原始query，并核验对应峰token；加合物只以ID张量
进入谱图塔，不将SMILES/身份或候选标签作为条件特征。原图/谱增强和自然负例采样/RNG
保持，每轮另记实际query—ID顺序哈希；验证保留原完整分母与顺序，每次重新编码两塔。
保存的分数须显式提供对应元数据来源才能通过快照审计，未知/缺失query继续保留。

新增输入检查覆盖缓存失效/篡改、原始行与分组行错配、真实spawn跨epoch、GINE及
GINE+指纹在有/无注意力汇聚下两轮合成训练、梯度、完整计数、错配输入拒绝和逐query
直接余弦重算。初次相关223项通过；补充读入期间变更检查后，全仓832通过、1跳过、
15失败，其中14项由测试继承的存储环境冲突引起，隔离对应环境后14项复核全部通过，
未改生产/测试源码，余下为相同四份历史记录的既有路径审计失败；Ruff、compileall和
diff检查通过。原失败和复核回执均保留，路径/SHA集中于阶段计划。

此节点尚未将新字段接入正式CLI编排、checkpoint构造或完整完成审计；正式构造器仍
拒绝该开关。正式条件预检仍待执行，不能把小型CPU实现检查当作模型实验。

## 全量元数据缓存

输入准备源码已冻结为`9cf01af83e9467580623354130de08048cf25ea2`，独立CPU准备及fresh
源数据审计完成；06:18:59另行验证全部194,119条train和19,423条有效val。独立程序未
调用项目Tokenizer/元数据构造函数，逐条从原始峰重建token，并核对观测加合物、原始行号、
标识和已有完整验证索引，结果全部一致。未知类型计数为0；这不改变已登记的未知保留策略。

缓存位于`/data1/${USER}/SpecEmbedding/spectrum_metadata_cache/massspecgym_v15_adduct_20260911_topk256`，
总计60,301,625字节，只含固定输入及来源，不包含模型embedding。原图、Morgan及峰载荷
未修改，A10继续使用其原固定输入。构造/映射/来源变化时，新读取器拒绝旧版本；不能
以本缓存已完成替代正式CLI、构造重载、完整完成审计和最终parent绑定。
输入源码位置、准备/独立回执及SHA集中于阶段计划第10节。

## 正式路径接入

2026-09-11以`23d05c9`接通显式模型构造、正式CLI、fresh训练、严格checkpoint重载和
完整完成审计。训练入口必须同时提供条件层配置和`--spectrum-metadata-cache`，完整
train/val来源进入预检及模型选择记录；每轮按实际原始query顺序独立重建加合物ID哈希。
完成审计同时检查源文件指纹、训练计数、自然负例重放、验证快照和选优权重。

候选模型和比较基线分别绑定各自的元数据；基线根据自身checkpoint构造，不能因候选
配置启用条件层就改变旧基线输入。支持保留的GINE及GINE+指纹分支、已晋升的Q/K归一化
或注意力汇聚，继承原候选采样和CE权重。未知query保留，预训练共享模型源码和已有缓存
构造源码均未改变。

相关221项检查通过，含两类分子输入两轮实际合成训练、非零条件参数更新、全量小样本
计数与缺失正例分母、来源错配/权重损坏拒绝及严格重载。最初6项失败来自新测试中的谱图
数量与ID数量不符、合成指纹池遗漏配对验证正例；修正测试数据后通过，没有修改真实
候选或弱化生产检查。原失败记录保留。全仓876通过、1跳过，唯一失败仍为同四份历史
记录的既有路径审计；Ruff、compileall和diff检查通过。全部为CPU实现检查，无模型成绩。

固定源码`23d05c9`上的A04分支真实全量CPU条件预检通过，07:42:51独立读回23项输入和
6个准备文件SHA一致。覆盖194,119条训练query、19,423条有效验证query和827,600个
验证分子；原自然候选训练输入回执保持，新增元数据单独绑定。实际构造总参数9,049,033，
比A04增加2,048；原参数初值与RNG保持，条件参数为零。没有GPU训练或模型运行目录。

准备脚本首次改写时误改预期SHA常量，指纹检查在预检前拒绝；原配置未变，失败脚本与
原因已保留，修正常量后完成上述检查。最终parent仍须等A12审计，不能用A04条件预检
提前选定parent；该节点单次衔接组件和队列仍待。检查及预检工件统一记录于阶段计划第10节。

## 最终绑定与单次衔接准备

随后归档仅CPU的最终parent绑定脚本，176项检查通过。覆盖GINE/GINE+指纹、原均匀或
结构采样、CE权重1或2及Q/K/注意力汇聚的完整继承，只新增已登记加合物条件层。
模型构造、训练候选回执和CLI各自核对；条件层元数据只传给新模型，原比较基线按自身
checkpoint重新构造。已完成状态但原进程仍活跃时继续等待；前序失败、来源改变或未选中
Pareto候选的晋升歧义均停止，不能用准备回执或部分结果决定parent。

单次衔接组件31项检查通过，包含全新隔离tmux server的真实参数/环境传递，以及同时
改变预检和派发命令时仍拒绝遗漏、重复或误传加合物参数。永久claim、原始日志、失败
工件及已有输出保护保留；一次最终全量预检通过后才exec正式入口，逐GPU阶段重新等待。

真实缺A12依赖绑定探测确认不会创建队列、最终配置、专用socket或模型目录；25项准备
及固定来源文件另行重算SHA一致，Ruff和脚本编译通过。这是组件准备，不是实际队列
部署。最终仍须等真实A12入口、trainer与独立完成审计出现后绑定其PID/starttime和
固定工件，再执行归档`deploy_queue.py --binding-sha256 <实际SHA> --check-only`及单次
部署流程。不能预填未来PID，也不能根据A10中途成绩改变已登记的parent继承规则。

准备路径、源码及回执SHA集中于阶段计划第10节；A13仍未训练，没有检索收益或GPU成本结果。
